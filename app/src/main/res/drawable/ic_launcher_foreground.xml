<?xml version="1.0" encoding="utf-8"?>
<!--
  Vaultix 应用图标 · 前景层（自适应图标的 `foreground`）

  ── 图形 ────────────────────────────────────────────────────────────────
  **盾牌 + 钥匙孔**（原来的占位图是"盾"，未启用的旧前景是"锁"；这里把两者的语义
  合成一个更准确的符号：盾 = 安全，钥匙孔 = 密码库）。

  ── 为什么这么画（自适应图标的硬约束）────────────────────────────────────
  前景层画布 108×108，但**系统只保证中心 66dp 圆（半径 33）以内的内容可见**：
  各 OEM 的遮罩形状不同，圆遮罩会切掉四角、squircle 会切得更多。
  ⇒ 所有图形必须收进半径 33 的圆内。本图实测最远点 **30.00dp**（盾的最低点），
     留 3dp 余量，任何遮罩都不会切到图形。
  ⚠️ 这条约束是**旧占位图被替换掉的原因之一**：那个盾画到 y 16..100（离中心最远 44dp），
     当自适应前景用会被切掉上下两端，所以它只能当 legacy 图标、被系统缩放使用。

  ── 钥匙孔为什么要"一条轮廓" ─────────────────────────────────────────────
  挖空靠 `fillType="evenOdd"` + 两条子路径（盾在外、孔在内）。但**孔若由"圆 + 槽"
  两条子路径叠加而成，重叠区会按 evenOdd 重新被填上**，边缘出现一道接缝。
  ⇒ 所以钥匙孔是"圆与锥形槽求并集"算出的**单条不重叠轮廓**
     （接点由右/左槽边与圆求交得出：P_r=58.45,53.02 / P_l=49.55,53.02）。
     这样 monochrome 层可以原样复用同一条路径。
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFFFFFFF"
        android:fillType="evenOdd"
        android:pathData="M40,31 L68,31 Q74,31 74,38 L74,56 C74,69 66,78 54,84 C42,78 34,69 34,56 L34,38 Q34,31 40,31 Z M58.45,53.02 A6,6 0 1 1 49.55,53.02 L51.7,68 Q54,70.5 56.3,68 Z" />
</vector>
