<?xml version="1.0" encoding="utf-8"?>
<!--
  Vaultix 应用图标 · 前景层（自适应图标的 `foreground`）

  ── 图形：徽章盾 + V 带 ──────────────────────────────────────────────────
  三层结构（自外向内）：**白描边盾 →（留一道缝）→ 白色内场 → 金色 V 带**。
  - 描边 + 缝 + 内场 = 徽章/珐琅的层次感，避免"一块白板"；
  - **V** 呼应项目名 Vaultix，而盾形本身是 U ⇒ **"U 中含 V"**；V 也是全图标唯一的第二色相。

  ── 硬约束：所有内容收进半径 33dp 的安全圆 ───────────────────────────────
  前景画布 108×108，但系统**只保证中心 66dp 圆（半径 33）内的内容可见**：
  圆遮罩切四角、squircle 切得更多。本图实测：外盾最远点 **30.00dp**（含 3.2 描边
  外缘 = 31.60，余量 1.40）、内场 24.47、V 带 —— 均在安全圈内。

  ── ★ 内场必须由外盾「等比缩放 + 平移」生成，禁止手工拼曲线 ★ ──────────────
  这是本项目图标改造中**最贵的一课**（2026-09-14）：
  - 早期版本（V1）内场是外盾等比 0.78 缩放 ⇒ 形状同构，但缝宽不均
    （横 2.80 / 顶 4.12，差 47%，视觉上读成"盾在往下掉"）。
  - 修正版（V2）为了让缝宽相等，改为**手工按每条边偏移拼曲线** ⇒ 缝宽确实
    均匀了，但拼出来的 Q 角在纵横方向被非等比拉伸
    （横占 0.4145 / 纵占 0.1460；外盾是 0.3500 / 0.1321），**左右角不再一致**
    ⇒ **内场面积质心横向偏了 -0.29（53.71 vs 外盾 54.00）**，真机一看就是
    "内盾漂移了"。用户当场发现。
  - 现版本（V3）回到**等比缩放**（k=0.815，单一缩放比 ⇒ 形状严格同构），
    再平移使**面积质心精确等于外盾质心**：
      内场质心 (54.0000, 54.0899) vs 外盾 (54.0000, 54.0893)，偏差 **0.0006**；
      左右留白差 **-0.0000**（严格对称）。
  ⇒ **要改内场尺寸，只改 k，然后用 solve_v3c.py 重算 path，不要手改顶点。**

  ── 缝宽为什么四边不等（0.815 时：左 2.10 / 顶 2.67 / 底 3.93）─────────
  这是**几何必然，不是缺陷**：外盾顶边是直线、底尖是 C 曲线收口，
  两种边界的"等距偏移"性质不同 —— 等比缩放下横纵缝不可能同时相等。
  取舍是"选一个 k 让最小缝（2.10dp ≈ 0.93dp@48）不至于糊"。
  ⚠️ 想强行四边等缝就只能手工拼曲线，而那会破坏左右对称（见上）。

  ── V 带的"视觉居中"为什么要算，不能靠眼 ────────────────────────────────
  V 的臂在上方（顶部最厚），**面积质心天然高于包围盒中心**。
  实测 V 质心 (54.0000, 54.0891)、内场质心 (54.0000, 54.0899)，偏差 0.0008；
  边采样越界 0/3842。
  ⚠️ 后续若再动 V，务必重跑"质心对齐 + 越界采样"，别凭目测挪。

  ── V 带为什么圆角化 ───────────────────────────────────────────────────
  48dp 下 V 臂宽仅 2.83dp —— 直角转折在这么细的笔画上会显"脆"，24dp 下易糊成块。
  现给：顶两外角 r=1.2、底尖 r=1.6、内 V 尖 r=1.2。
  ⚠️ 圆角化会改变面积分布，**必须重新做质心对齐**。

  ── 为什么这里不用 evenOdd 挖洞 ─────────────────────────────────────────
  V 在本层是**填色**元素（金色渐变）。但**单色层（monochrome）里 V 必须改成洞** ——
  系统只给单色层一种着色，填色的 V 会与白盾同色而消失。见 ic_launcher_monochrome.xml。
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:aapt="http://schemas.android.com/aapt"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- 描边盾（徽章外圈）。质心 (54.0000, 54.0893)，画布中心 (54,54)，偏差 0.089 ⇒ 已居中。
         ⚠️ 勿整体平移：按包围盒中心（y=57.50）去移会把顶角推到 r=33.20，超出安全圈。 -->
    <path
        android:fillColor="@android:color/transparent"
        android:pathData="M40,31 L68,31 Q74,31 74,38 L74,56 C74,69 66,78 54,84 C42,78 34,69 34,56 L34,38 Q34,31 40,31 Z"
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="3.2" />
    <!-- 内场：外盾等比 0.815 缩放 + 平移（质心对齐）。缝 左2.10 右2.10 顶2.67 底3.93 -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M42.59,35.27 L65.41,35.27 Q70.30,35.27 70.30,40.98 L70.30,55.65 C70.30,66.24 63.78,73.58 54.00,78.47 C44.22,73.58 37.70,66.24 37.70,55.65 L37.70,40.98 Q37.70,35.27 42.59,35.27 Z" />
    <!-- V 带：全面圆角化（外角 1.2 / 底尖 1.6 / 内尖 1.2），质心已对齐内场（偏差 0.0008）。
         填充用线性渐变（左下深金 → 右上亮金）模拟金属反光：
         ⚠️ 必须用 aapt:attr 内联，`android:fillColor` 不能直接指向 <gradient> 资源。 -->
    <path
        android:pathData="M43.50,43.30 L47.10,43.30 Q48.30,43.30 49.50,44.50 L52.68,61.08 Q54.00,62.40 55.32,61.08 L58.50,44.50 Q59.70,43.30 60.90,43.30 L64.50,43.30 Q65.70,43.30 65.70,44.50 L55.15,69.84 Q54.00,71.00 52.85,69.84 L42.30,44.50 Q42.30,43.30 43.50,43.30 Z">
        <aapt:attr name="android:fillColor">
            <gradient
                android:startX="42.30"
                android:startY="71.00"
                android:endX="65.70"
                android:endY="43.30"
                android:type="linear">
                <item android:offset="0.0" android:color="@color/ic_launcher_accent_dark" />
                <item android:offset="1.0" android:color="@color/ic_launcher_accent" />
            </gradient>
        </aapt:attr>
    </path>
</vector>
