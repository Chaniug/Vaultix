<?xml version="1.0" encoding="utf-8"?>
<!--
  Vaultix 应用图标 · 前景层（自适应图标的 `foreground`）

  ── 图形：徽章盾 + V 带 ──────────────────────────────────────────────────
  三层结构（自外向内）：**白描边盾 →（留一道缝）→ 白色内场 → 金色 V 带**。
  - 描边 + 缝 + 内场 = 徽章/珐琅的层次感，避免"一块白板"；
  - **V** 呼应项目名 Vaultix，而盾形本身是 U ⇒ **"U 中含 V"**；V 也是全图标唯一的第二色相。

  ── 硬约束：所有内容收进半径 33dp 的安全圆 ───────────────────────────────
  前景画布 108×108，但系统**只保证中心 66dp 圆（半径 33）内的内容可见**：
  圆遮罩切四角、squircle 切得更多。本图实测：外盾最远点 **30.48dp**、内场 26.09dp、
  V 带 19.10dp —— 均在安全圈内（留 2.5dp 余量）。
  ⚠️ 旧占位图被换掉的原因之一就是它画到 y 16..100（最远 44dp），当自适应前景会被切掉上下两端。

  ── V 带的"视觉居中"为什么要算，不能靠眼 ────────────────────────────────
  V 带的重量集中在上面两条臂（顶部最厚），所以它的**面积质心天然高于包围盒中心**。
  上一版只按包围盒摆，实测质心比内场高 **3.84dp** —— 看着就是"偏上"。
  现按**面积质心对齐**求解（下移 3.84 并略收窄到半宽 11）：
  V 质心 y=54.73 == 内场质心 y=54.73，偏差 0.000；且边采样越界数 0。
  ⚠️ 后续若再动 V，务必重跑这个"质心对齐 + 越界采样"校验，别凭目测挪。

  ── 为什么这里不用 evenOdd 挖洞 ─────────────────────────────────────────
  V 在本层是**填色**元素（金色），不是洞。但**单色层（monochrome）里 V 必须改成洞** ——
  因为系统只给单色层一种着色，填色的 V 会与白盾同色而消失。两层结构允许不同，
  但形状语义必须一致（都是"盾 + V"），见 ic_launcher_monochrome.xml。
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- 描边盾（徽章外圈） -->
    <path
        android:fillColor="@android:color/transparent"
        android:pathData="M40,31 L68,31 Q74,31 74,38 L74,56 C74,69 66,78 54,84 C42,78 34,69 34,56 L34,38 Q34,31 40,31 Z"
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="3.2" />
    <!-- 内场（与描边之间留缝，露出背景渐变） -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M43.08,36.72 L64.92,36.72 Q69.60,36.72 69.60,42.18 L69.60,56.22 C69.60,66.36 63.36,73.38 54.00,78.06 C44.64,73.38 38.40,66.36 38.40,56.22 L38.40,42.18 Q38.40,36.72 43.08,36.72 Z" />
    <!-- V 带（质心已对齐内场质心 y=54.73） -->
    <path
        android:fillColor="@color/ic_launcher_accent"
        android:pathData="M43,45 L49,45 L54,63 L59,45 L65,45 L54,71 Z" />
</vector>
