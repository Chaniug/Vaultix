<?xml version="1.0" encoding="utf-8"?>
<!--
  Credential Provider 能力声明：系统据此把 Vaultix 列为可供应凭据的提供方。
  Android 14+（API 34）起由 Credential Manager 读取；低版本忽略。

  ⚠️⚠️ **不声明 TYPE_PASSWORD_CREDENTIAL**（对齐 Bastion，关键根因修复）。

  如果声明了 TYPE_PASSWORD_CREDENTIAL，Chromium 系浏览器（Edge / Chrome / Brave 等）
  会把**密码请求全部路由到 Credential Manager（CP 通道）**，绕过 Autofill 框架。
  而 Vaultix 的 CP 密码分支依赖 Hilt 注入 + 库解锁 + origin 过滤，任一环节失败就
  返回空 → Edge 密码框什么都不弹。同时老 AutofillService.onFillRequest 也被系统
  绕过（CP 声明 password 能力后系统把密码字段交给 CP 独占）——两条路全废。

  解决：CP 只管通行密钥（TYPE_PUBLIC_KEY_CREDENTIAL），密码填充回到 Autofill 框架
  路径（VaultixAutofillService.onFillRequest），该路径已完整实现（AssistStructureParser
  + BitwardenLikeAutofillMatcher + FillPlanner）。两条路径各司其职，互不阻断。

  Bastion 原文注释（credential_provider_config.xml）确认了同一结论：
  > NOTE: TYPE_PASSWORD_CREDENTIAL intentionally omitted.
  > On Android 14+ (API 34+), declaring this capability causes Chromium-based browsers
  > to route password requests through the Credential Manager API instead of the
  > Autofill framework. Bastion's CredentialProviderService only handles passkeys,
  > not password credentials — so password requests would silently fail and
  > onFillRequest would never be invoked, breaking password autofill in all
  > Chromium browsers while WebView-based browsers (Via, etc.) remain unaffected.

  settingsActivity 语义：系统凭据管理器（设置 → 密码和账号 → Vaultix）的「管理」入口。
  现指向 CredentialProviderSettingsActivity（承载自动填充/凭据提供商设置页）。
-->
<credential-provider
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:settingsActivity="io.vaultix.vaultix.passkey.CredentialProviderSettingsActivity"
    android:settingsSubtitle="@string/setting_credential_provider">
    <capabilities>
        <capability name="androidx.credentials.TYPE_PUBLIC_KEY_CREDENTIAL" />
        <capability name="android.credentials.TYPE_PUBLIC_KEY_CREDENTIAL" />
    </capabilities>
</credential-provider>
