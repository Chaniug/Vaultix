package io.vaultix.domain

/**
 * 领域层：UseCase / Interactor / 策略（纯 Kotlin）。
 * 依据 Docs/01 规则 R2：不得依赖 data、core:database、core:platform 或具体 Android SDK
 * （仅可使用 javax.crypto 等 JVM 标准库）。
 *
 * 首批骨架仅占位，后续按 Docs/02~05 填充（解锁、同步编排、冲突解决等）。
 */
