# 08 · 界面设计规格

> 视觉规范以 [07 · Material 3 设计系统](./07-Material3设计系统.md) 为准。本章给出**逐屏**的结构、状态与交互规格。
> 尺寸单位 dp；`H`=高度，`W`=宽度。

## 1. 导航结构

> 参考 Monica 的「可自定义 Dock 底栏」与 Keyguard 的「双栏响应式」。

```
                   ┌──────────────┐
                   │  Splash/Route│  已配置库 → LockScreen
                   └──────┬───────┘  未配置 → Onboarding
        ┌─────────────────┼──────────────────┐
        ▼                 ▼                  ▼
  Onboarding(3页)   AddVault(选类型)     VaultList(库列表)
                          │                  │
        ┌─────────────────┴───────┐          │
        ▼                         ▼          ▼
  AddBitwardenVault        AddKdbxVault   LockScreen(每库)
        └────────────┬────────────┘
                     ▼
   ┌──────────────────────────────────────────────────┐
   │  MainShell（可自定义 Dock，默认 5 项）              │
   │  ① 密码库  ② 验证器  ③ 安全中心  ④ 生成器  ⑤ 设置 │
   │  可选：卡包 / 证件 / 安全笔记 / 回收站 / 总览       │
   └──┬────────┬─────────┬──────────┬─────────┬───────┘
      │        │         │          │         │
  VaultHome  Authen-  Security   Gener-   Settings
  (密码库)   ticator   Center    ator     (二级页)
  ├ 全部/单库 (验证器)  (安全中心) (生成器)
  ├ 顶部胶囊
  ├ Search
  ├ ItemDetail
  │   └ Edit
  ├ 归档(独立页)
  └ 文件夹/群组树
```

**顶部胶囊**（参考 Monica，常驻于每个列表页顶部）：

```
┌─────────────────────────────────────────────────┐
│ ┌──────────────┐              ┌────┐ ┌────┐    │
│ │ 全部 ▾  🔍   │              │ ⋮  │ │ ＋ │    │   ← 胶囊：存储源 + 搜索 + 更多
│ └──────────────┘              └────┘ └────┘    │
│ (全部)(登录)(卡片)(身份)(笔记)(★收藏)(⏱有验证码) │   ← 类型筛选芯片
└─────────────────────────────────────────────────┘
```

- 点击胶囊左侧「全部 ▾」→ 底部抽屉列出所有库 + 「全部」（跨库聚合）
- 点击胶囊内 🔍 → 展开搜索（也支持列表顶部**下拉约 40 dp** 展开，带阻尼回弹与触感反馈）
- 「更多 ⋮」收纳：排序、分组视图、多选、导入导出、归档、设置入口

路由采用类型安全路由（`@Serializable data class ItemDetail(val vaultId: String, val itemId: String)`）。
外部入口 deeplink：`vaultix://search?q=<host>`、`vaultix://generator`、`vaultix://lock`、`vaultix://authenticator`。

## 2. 屏幕清单

| ID | 屏幕 | 主要组件 | 关键状态 |
|---|---|---|---|
| S1 | 启动 / 路由 | 品牌图标 + 进度 | Loading → 分流 |
| S2 | 引导（3 页） | 插画 + 标题 + 说明 + 按钮 | 翻页指示 |
| S3 | 库列表 | 大卡片列表 + FAB | 空态 / 多库 / 锁定态徽标 |
| S4 | 添加 Bitwarden 库 | 表单 + 高级选项折叠 | 输入校验 / 2FA 步骤 / 加载 |
| S5 | 打开 / 新建 KDBX | 表单 + SAF + Argon2id 参数 | 版本不支持 / 解密失败 |
| S6 | 解锁 | 主密码 + 生物识别按钮 + 库切换 | 错误计数 / 生物识别不可用 |
| S7 | 密码库首页 | 顶部胶囊 + 筛选芯片 + 分组 + 列表 + FAB | 空/加载/错误/离线/同步中 |
| S8 | 搜索结果 | 结果列表 + 分组筛选 | 无结果 |
| S9 | 条目详情 | LargeTopAppBar + 分区卡片 | 已锁定/重验证/编辑 |
| S10 | 条目编辑 | 表单 + 自定义字段 + 附件 | 校验/脏数据离开确认 |
| S11 | 密码生成器 | 结果卡 + 选项 + 历史 | 强度反馈 |
| S12 | **验证器**（底栏项） | 验证码卡片流 + 倒计时 | 空/复制/即将刷新 |
| S13 | 设置 | 分组列表 | — |
| S14 | 同步状态 | 状态卡 + 日志 | 成功/失败/离线队列 |
| S15 | 导入 / 导出 | 文件选择 + 目标库 + 预览 | 进度/结果报告 |
| S16 | 自动填充覆盖层 | 建议条 / 解锁卡 / 搜索面板 | 匹配/无匹配/需解锁 |
| S17 | **安全中心**（底栏项） | 评分环 + 告警分组 + 角标 | 空/扫描中/有未读 |
| S18 | 卡包 / 证件（可选底栏项） | 卡片列表 + 正反面照片位 | 空/详情/编辑 |
| S19 | 归档（独立页） | 归档条目列表 | 空/恢复/永久删除 |
| S20 | 底栏管理 | 拖拽排序 + 开关 | — |
| D1 | 冲突解决 | 并排字段选择 | — |

## 3. 逐屏规格

### S3 · 库列表（Vault List）

```
┌───────────────────────────────────────────┐
│ [≡] 我的密码库                    [?] [⚙] │  LargeTopAppBar（折叠为"我的密码库"）
├───────────────────────────────────────────┤
│ ┌───────────────────────────────────────┐ │
│ │ [☁] Bitwarden · alice@mail.com        │ │  高 88 卡片
│ │     142 项 · 上次同步 3 分钟前  [已锁定]│ │  surface-container-low
│ └───────────────────────────────────────┘ │
│ ┌───────────────────────────────────────┐ │
│ │ [🔒] personal.kdbx                    │ │
│ │     86 项 · 本地文件  [已解锁]         │ │
│ └───────────────────────────────────────┘ │
│                                    (( + ))│  FAB：添加库
└───────────────────────────────────────────┘
```

- 卡片点击 → S6 解锁 → S7。
- 长按/尾部菜单：重命名、修改服务端地址、移除库（二次确认，明确"仅移除本地记录" vs "同时删除本地文件"）。
- 空态：居中图示 + "还没有密码库" + 两个按钮（连接 Bitwarden / 打开 KDBX 文件）。

### S6 · 解锁（Lock Screen）

```
┌───────────────────────────────────────────┐
│            [库图标 64dp]                   │
│          Bitwarden · alice@mail.com        │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │ 主密码                          [👁]  │ │  OutlinedTextField
│  └──────────────────────────────────────┘ │
│  密码提示：我的第一辆车                    │  若有 hint
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │           使用生物识别解锁             │ │  TonalButton（有凭据时）
│  └──────────────────────────────────────┘ │
│  ┌──────────────────────────────────────┐ │
│  │                解锁                   │ │  FilledButton，全宽
│  └──────────────────────────────────────┘ │
│         忘记主密码？         切换库        │  TextButton
└───────────────────────────────────────────┘
```

- 状态：`输入中`（按钮可用需非空）、`计算中`（LinearProgressIndicator + 百分比 + 取消；PBKDF2/Argon2 耗时提示"正在派生密钥，约需 1–3 秒"）、`错误`（输入框 `error` 态 + 支持文本 + 抖动动画 300 ms）。
- KDBX 额外：密钥文件行（选择/移除）、"记住密钥文件路径"。
- 失败 5 次 → 冷却 30 s，按钮禁用并倒计时。
- 生物识别不可用时隐藏该按钮并在设置中说明原因（无硬件 / 凭据失效 / 设备未设置锁屏）。

### S7 · Vault 首页（主列表）

```
┌───────────────────────────────────────────┐
│ ┌────────────┐              ┌───┐┌───┐   │  顶部胶囊（常驻，不随滚动隐藏）
│ │ 全部 ▾  🔍 │              │ ⋮ ││ ＋│   │  存储源 / 搜索 / 更多 / 新建
│ └────────────┘              └───┘└───┘   │
├───────────────────────────────────────────┤
│ (全部)(登录)(卡片)(身份)(笔记)(★)(⏱)      │  FilterChip 横滚
├───────────────────────────────────────────┤
│ 全部 (142)                    [分组视图]  │  分组头 sticky
│ ┌───────────────────────────────────────┐ │
│ │ [G] GitHub                     ★      │ │
│ │      alice@example.com                │ │
│ ├───────────────────────────────────────┤ │
│ │ [M] 招商银行               [123 456 ⏱]│ │  TOTP 芯片
│ │      alice_w                          │ │
│ └───────────────────────────────────────┘ │
│                                    (( + ))│  FAB：新建条目
└───────────────────────────────────────────┘
   列表顶部下拉 ~40dp → 展开搜索（带阻尼回弹 + 触感反馈）
   继续下拉 → 触发同步（PullRefresh）
```

| 元素 | 规格 |
|---|---|
| **顶部胶囊** | 圆角 `Full`，`surface-container-high` 底色；左侧「存储源 ▾」+ 搜索图标，右侧「更多 ⋮」与「新建 ＋」。点击存储源 → 底部抽屉（全部 + 各库 + 库管理入口） |
| 下拉搜素 | 下拉约 40 dp 展开搜索框（参考 Monica）；展开后仍可继续下拉触发同步 |
| 筛选芯片 | 类型 + 收藏 + 有验证码 + 有告警；横向滚动，选中为 `secondaryContainer` |
| **筛选去重** | 横排筛选条已存在时，「更多 ⋮」菜单内**不再**重复放置快捷筛选（参考 Bastion）；仅在收拢/无筛选条时作为唯一入口 |
| **全部（聚合）视图** | 跨库展示；每行右下以小徽标标示来源库（云图标 = Bitwarden，锁图标 = KDBX）；切换回单库时保留上次位置 |
| 底部留白 | 列表底部内边距 = 底栏/悬浮层实测高度 + **12 dp** 呼吸空间（参考 Bastion），避免最后一项贴住手势条 |
| 排序 | 名称 / 最近使用 / 最近修改 / 自定义（KDBX 不支持自定义排序时隐藏） |
| 分组视图 | Bitwarden：文件夹折叠；KDBX：树形（缩进 16 dp/层，最多显示 4 层提示"展开更多"） |
| 侧栏抽屉 | 从左边缘滑出 `ModalDrawerSheet`：库列表 + 全部/收藏/回收站/生成器的导航目标 + 同步状态 |
| 空态 | 图标 + "还没有保存的密码" + 主按钮（新建/导入）+ 次按钮（开启自动填充引导） |
| 离线态 | 顶部 `surface-container-high` 条：云图标 + "离线 · 3 项待同步" |
| 同步中 | 顶部细 `LinearProgressIndicator`（4 dp，不确定态） |
| 多选 | 长按进入，应用栏变 `secondaryContainer`：已选 N / 移动 / 收藏 / 删除 |

**回收站**：独立分组入口；条目显示删除时间；提供"恢复"与"永久删除"（后者用 `error` 对话框）；Bitwarden 默认 30 天服务端清理，UI 提示。

### S9 · 条目详情（Item Detail）

```
┌───────────────────────────────────────────┐
│ [←]                            [★] [⋮] [编辑]│
│  ＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿  │
│  [图标 56]  GitHub                         │  LargeTopAppBar 展开态
│             alice@example.com              │
├───────────────────────────────────────────┤
│  登录信息                                  │  分区标题 labelLarge
│  ┌───────────────────────────────────────┐ │
│  │ 用户名   alice@example.com      [复制] │ │  行 + 尾部 IconButton
│  │ 密码     ••••••••••••   [👁] [复制]   │ │  等宽 + 分段
│  │ 验证码   123 456      ⏱28s   [复制]   │ │  环形倒计时
│  └───────────────────────────────────────┘ │
│  网站                                      │
│  ┌───────────────────────────────────────┐ │
│  │ https://github.com         [匹配:基础域名]│
│  └───────────────────────────────────────┘ │
│  自定义字段 / 备注 / 附件 / 历史           │  可折叠分区
└───────────────────────────────────────────┘
```

| 交互 | 说明 |
|---|---|
| 复制 | 点击行或复制按钮 → Snackbar"已复制用户名（60 秒后清除）"；密码复制可配置"立即显示明文" |
| 密码显隐 | 尾部眼睛图标；显示时禁止截屏已由 `FLAG_SECURE` 兜底 |
| TOTP | 环形进度 + 剩余秒数；点击复制；< 5 s 变 `error` 色 |
| 重验证（`reprompt`） | 复制密码/查看时弹主密码确认对话框 |
| 历史 | 展开显示历史版本列表（KDBX：`History`；Bitwarden：`passwordHistory`），支持"恢复此版本" |
| 附件 | KDBX 可"导出到…"（FileProvider）；Bitwarden 仅下载（需 Premium） |
| 危险操作 | 菜单中的"移至回收站"用 `error` 色文本项 |
| 大屏 | ≥ 840 dp 使用 `ListDetailPaneScaffold`：左列表右详情，详情在该 pane 内独立导航 |

### S10 · 条目编辑（Editor）

```
分区顺序：图标与类型 → 名称 → 登录信息（用户名/密码/验证码/网站）→
          卡片/身份（按类型） → 自定义字段 → 所属文件夹/群组 → 备注 → 附件 → 高级（重验证/是否收藏）
```

| 元素 | 规格 |
|---|---|
| 类型切换 | 顶部 `SegmentedButton`（登录 / 卡片 / 身份 / 笔记） |
| 密码行 | 输入框 + 显隐 + 生成器按钮（跳转 S11 并把结果回填） |
| 强度条 | 密码下方实时显示（4 段，语义色） |
| 自定义字段 | 动态列表：类型下拉（文本/隐藏/布尔）+ 名称 + 值 + 删除；"添加字段" TextButton |
| 网站 | 多行，每行带匹配规则下拉（基础域名/主机/开头/精确/正则/永不） |
| 保存 | 顶部应用栏"保存"（仅在脏数据时可用）；返回时若有未保存修改 → 确认对话框（丢弃/保存/取消） |
| 校验 | 名称必填（红色错误文本 + 滚动到该字段）；URL 格式校验（警告级别） |
| 密码历史 | 修改密码时自动写入历史（可在设置关闭） |

### S11 · 密码生成器

```
┌───────────────────────────────────────────┐
│ ← 密码生成器                     [历史]   │
├───────────────────────────────────────────┤
│ ┌───────────────────────────────────────┐ │
│ │  Xy7$mKq2!vLp9@Rz                     │ │  等宽 headlineSmall
│ │  ▓▓▓▓▓▓▓▓ 强                          │ │  强度条 + 熵估算
│ │              [复制] [🔄 重新生成]       │ │
│ └───────────────────────────────────────┘ │
│ 类型  ( 密码 | 短语 )                      │  SegmentedButton
│ 长度  ──────●──────  20                   │  Slider（4–128）
│ 大写 [A]  小写 [a]  数字 [0]  符号 [!@#]  │  FilterChip
│ ☑ 避免易混淆字符（l1IO0）                 │  Switch
│ ☑ 每类至少包含一个                        │
├───────────────────────────────────────────┤
│  ┌─────────────────────────────────────┐ │
│  │           使用此密码                  │ │  FilledButton（从编辑页进入时）
│  └─────────────────────────────────────┘ │
└───────────────────────────────────────────┘
```

- 短语模式：单词数 Slider（3–10）、分隔符、首字母大写、包含数字（EFF 长词表，附许可说明）。
- 熵估算：`log2(字符集大小 × 长度)`，展示"约 N bit"。
- 历史：仅本地保存最近 20 条（加密存储），一键清除。

### S12 · 验证器（底栏独立页，参考 Monica / Bastion）

```
┌───────────────────────────────────────────┐
│ ┌────────────┐              ┌───┐┌───┐   │  同样有顶部胶囊
│ │ 全部 ▾  🔍 │              │ ⋮ ││ ＋│   │  ＋ = 扫码 / 手动录入
│ └────────────┘              └───┘└───┘   │
├───────────────────────────────────────────┤
│ ┌───────────────────────────────────────┐ │
│ │ ⏱        GitHub                       │ │  卡片：环形倒计时（剩余 18s）
│ │  ⟳        alice@example.com            │ │
│ │          123 456            [复制]     │ │  等宽大字 + 分段
│ └───────────────────────────────────────┘ │
│ ┌───────────────────────────────────────┐ │
│ │ ⏱        招商银行            ⚠ 重复    │ │  重复/弱告警角标
│ │           482 915          [复制]      │ │
│ └───────────────────────────────────────┘ │
└───────────────────────────────────────────┘
```

| 规则 | 说明 |
|---|---|
| **绑定型优先** | 条目自带的 TOTP 在此页直接展示（不重复占位）；独立验证码（无对应登录条目）也在此管理 |
| 排序 | 按"即将刷新"优先（剩余秒数升序），刷新后自动重排并带位移动画 |
| 卡片 | 环形倒计时（< 5 s 转 `error` 色）+ 等宽分段验证码 + 复制按钮；点击卡片跳转所属条目 |
| 批量导入 | 支持扫描 **Google Authenticator 迁移二维码**，导入前预览、勾选、识别重复项 |
| 其他格式 | 兼容 `otpauth://`、Steam 令牌、KeePassXC / KeePass2Android 的 otp 字段写法 |
| 搜索 | 复用顶部胶囊搜索，按发行方/账户名匹配 |

### S17 · 安全中心（底栏独立页，对齐 Keyguard Watchtower）

```
┌───────────────────────────────────────────┐
│ 安全中心                          [⚙ 规则] │
├───────────────────────────────────────────┤
│         ╭───────╮                          │
│         │  82   │   评分环（0–100）         │
│         │ 良好  │   ERROR 0 · WARN 2 · INFO 5│
│         ╰───────╯                          │
├───────────────────────────────────────────┤
│ ⛔ 需要处理（2）                            │  ERROR 组
│ ┌───────────────────────────────────────┐ │
│ │ 🔴 密码已泄露        3 项   ─────────▶│ │
│ │ 🔴 密码重复使用      2 项   ─────────▶│ │
│ └───────────────────────────────────────┘ │
│ ⚠ 建议改进（2）                            │  WARNING 组
│ │ 🟠 网站支持 2FA 但未存 TOTP    5 项   ▶│ │
│ │ 🟠 使用 HTTP 而非 HTTPS         1 项   ▶│ │
│ ℹ 提示（5）                                │  INFO 组
│ │ 🔵 网站支持 Passkey 但未存     12 项  ▶│ │
│ │ 🔵 条目不完整                   4 项   ▶│ │
└───────────────────────────────────────────┘
```

| 机制 | 说明 |
|---|---|
| 扫描时机 | 库解锁后后台增量扫描；条目变更时按类型 debounce（变更类 1 s，全局类如"重复密码"3 s）；单线程串行避免抢占 CPU |
| 严重度 | ERROR（红）/ WARNING（琥珀）/ INFO（中性蓝）三级，分组折叠展示，组内按条目名排序 |
| 未读角标 | 新发现的告警带"新"标记，底栏图标显示未读数；进入安全中心后清除通知与角标 |
| 逐条忽略 | 除「弱密码」外均可对单条目标记忽略（弱密码属密码本身属性，不可忽略）；忽略记录在条目级 |
| 处理动作 | 每条告警直达修复：弱密码/重复/泄露 → 跳转生成器并回填；缺 2FA → 跳转编辑页 TOTP 字段；HTTP → 一键改 HTTPS |
| 联网项 | 「密码已泄露」「网站曾泄露」走 HIBP **k-匿名**（仅发送哈希前 5 位），**默认关闭**，需用户显式开启并说明 |
| 回收站 | 回收站内的条目**不产生**告警与通知 |

### S18 · 卡包 / 证件（可选底栏项，参考 Monica）

- 卡包：卡号（分段遮罩 `**** **** **** 1234`）+ 持卡人 + 有效期 + CVV（默认隐藏）；**正反面照片位**（存为 KDBX 附件，其他 KeePass 客户端可读）。
- 证件：证件类型、号码、签发/有效期、正反面照片；支持多通用附件。
- 两类的照片位与通用附件列表互不重复展示。

### S19 · 归档（独立页，参考 Monica）

- **独立页面**而非筛选状态，避免状态互相污染。
- 进入前记录当前存储源与文件夹位置，返回时完整恢复（含本地 / KDBX / Bitwarden 与文件夹筛选）。
- 归档内只展示已归档的密码条目，不混入验证码、卡包、证件、笔记。
- 返回键依次退出：多选 → 搜索 → 返回上一级（不误退应用）。
- 加载完成前显示加载态，确认为空才显示空提示（避免闪烁）。

### S20 · 底栏管理

- 拖拽排序 + 开关；已启用项最多 5 个，最少 3 个。
- 默认：密码库 / 验证器 / 安全中心 / 生成器 / 设置。
- 可选：卡包、证件、安全笔记、回收站、总览（跨类型时间线）。

**底栏样式**（参考 Bastion v1.0.325 胶囊底栏改版，默认仍为 Material 3 官方形态）：

| 样式 | 形态 |
|---|---|
| **贴底（默认）** | 官方 `NavigationBar` 通栏贴底 + 内容右下 FAB |
| **悬浮胶囊** | 自绘胶囊：高 60 dp，距底 20 dp；选中为色块整体包住图标 + 文字；**中间为添加按钮（左 2 + 中 + 右 2），不显示 FAB** |

```
  贴底模式                          悬浮胶囊模式
┌──────────────┐              ┌──────────────┐
│              │              │              │
│   内容区      │              │   内容区      │
│              │              │              │
│         ((+))│              │              │
├──────────────┤              │ ╭──────────╮ │  ← 胶囊 60dp
│ 🏠 🔑 🛡 ⚙  │              │ │🏠🔑(＋)⚙🛡│ │     距底 20dp
└──────────────┘              ╰─╯──────────╯  │
                              └──────────────┘
```

- 切换样式时同步切换 FAB 的显隐，**不允许**两者共存。
- 胶囊模式为自绘组件，必须补齐 `semantics` 的 `Role.Tab` 与 `selected` 语义。

### S13 · 设置（分组）

| 分组 | 条目 |
|---|---|
| 安全 | 锁定超时、切后台锁定、生物识别解锁、启用"离线解锁"、清除剪贴板时间、主密码更改（Bitwarden）/ 修改主密码与 Argon2id 参数（KDBX）、允许截图（默认关，需二次确认）、**安全规则**（安全中心各告警类型的开关） |
| 自动填充 | 启用 Autofill 服务、启用 Credential Manager（API 34+）、已关联的 App/网站管理、兼容模式白名单、"此 App 不再询问"重置、内联建议开关 |
| 外观 | **主题**（自然 / Material You / 暗色 / 纯黑 / RG 护眼）、动态取色、对比度、语言、**底栏管理**（排序/开关/贴底或悬浮胶囊样式）、列表密度（紧凑/舒适/堆叠）、显示网站图标（联网） |
| 同步 | 后台同步周期、仅在 Wi-Fi 下同步、同步时下载图标、立即同步、查看同步日志 |
| 数据 | 导入、导出、**归档**、回收站设置、清除缓存、清除图标缓存 |
| 关于 | 版本、开源许可（OSS Licenses）、隐私政策、源码地址、反馈与崩溃报告开关 |

### S16 · 自动填充覆盖层

| 形态 | 触发 | 内容 |
|---|---|---|
| 内联建议（Autofill） | 聚焦输入框 | 键盘上方 chip：条目名 + 用户名；点击展开完整选择面板 |
| 选择面板 | 点击 chip / "密码"图标 | BottomSheet 风格列表：匹配项置顶（带 "github.com" 说明），下方"搜索全部条目" |
| 解锁卡 | 库未解锁 | 图标 + "解锁 Vaultix" + 生物识别按钮 + "使用主密码" |
| 搜索面板 | 无匹配 / 用户点搜索 | 全屏搜索页：SearchBar + 结果列表 + "新建并保存" |
| 保存提示 | 提交表单 | BottomSheet：识别到的 用户名/密码 + 目标库下拉 + 保存 / 更新 / 忽略 |

- 所有覆盖层**必须**显示目标 App 名称与域名（防钓鱼关键）：`"正在填充到 · github.com"`。
- 未通过关联校验时，覆盖层顶部加 `errorContainer` 警示条："无法验证此应用与 github.com 的关联"。

## 4. 大屏与折叠适配

| 宽度等级 | 布局 |
|---|---|
| Compact（< 600 dp） | 单栏 + `NavigationBar`；详情为独立页面 |
| Medium（600–839 dp） | 单栏 + `NavigationBar`（M3 Expressive flexible，更矮）；详情可为全屏对话框 |
| Expanded（≥ 840 dp） | `NavigationRail` + `ListDetailPaneScaffold`（列表 360 dp 固定 + 详情自适应，最大 720 dp 居中） |
| 折叠展开态 | 使用 `WindowSizeClass` 与 `FoldingFeature`；铰链处避免放置可点击元素（双栏各占一屏） |

## 5. 关键流程线框

### 5.1 首次添加 Bitwarden 库

```
Onboarding → 选择"连接 Bitwarden" → 服务器地址（默认 bitwarden.com，可展开自托管）
          → 邮箱 + 主密码（+ 显示"忘记？"） → [2FA 步骤，必要时]
          → 派生密钥（进度）→ 登录 → 同步（进度/条数）→ 设置锁定时长 → 完成 → Vault 首页
```

### 5.2 新建 KDBX 库

```
选择"创建本地库" → 文件名与保存位置（SAF）→ 主密码（强度校验，禁止空）
          → 重复确认 → 密钥文件（可选，可跳过）→ 高级（Argon2id 内存/迭代/并行，默认 64 MiB × 3）
          → 基准测试（展示预计解锁耗时）→ 创建 → 空库首页（引导新建/导入）
```

### 5.3 导入

```
设置 → 导入 → 选择格式（Vaultix 备份 / Bitwarden JSON / KeePass XML / CSV…）
     → 选择文件 → 选择目标库（或"新建 KDBX 库"）→ 冲突策略（跳过/覆盖/合并）
     → 进度 → 结果报告（成功 N 条 / 跳过 M 条 / 失败 K 条，可导出失败明细）
```

## 6. 文案与本地化

- 中文（简体，默认）、英文；文案放 `strings.xml`，支持 ICU 复数（如"已选 %1$d 项"）。
- 敏感操作文案必须具体："永久删除将不可恢复，确定删除「GitHub」吗？"
- 不在 UI 中展示任何密钥、密文或完整邮箱之外的账号标识（日志同理）。
