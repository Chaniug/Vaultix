# ⚠️ 本文件是「GitHub 通道」的核心配置，改动前先读完注释。
#
# 沙箱有两个坑叠在一起，导致 push 会莫名其妙失败：
#
# 【坑 1】/etc/hosts 会被重置
#   重置后 `ssh.github.com` 解析到 198.18.0.39（保留网段占位），SSH 会报
#   `kex_exchange_identification: Connection closed by remote host`。
#   ⇒ 用 `github-channel-check` 修复（它从阿里 DoH 查真实 IP 并写回 hosts）。
#
# 【坑 2】★ 名字解析会绕过 hosts，但「直连 IP」不会
#   实测：即使 hosts 已把 ssh.github.com 指到 20.205.243.160，
#   `ssh -o HostName=ssh.github.com github.com` 仍会连 198.18.0.39。
#   原因是 OpenSSH 对 HostName 的处理与 hosts 文件存在解析顺序差异。
#   ⇒ 唯一稳定做法：**HostName 直接写 IP**，不写域名。
#
# 于是这里把 IP 写死。`github.com` / `api.github.com` / `codeload.github.com`
# 同属 GitHub 自有 AS（20.205.243.160-168），实测长期稳定，可以写死。
#
# 如果哪天 push 又失败：
#   1) 先跑 `github-channel-check`（它同时会校验 IP 是否还活着，死了会自动用 DoH 重查）
#   2) 再跑 `ghp`（封装了「摘 URL 重写 → SSH 推 → 还原」）
#
# ⚠️ 不要用 `git@github.com:...` 直推：~/.gitconfig 里有
#   [url "https://github.com/"] insteadOf = git@github.com:
#   会把它劫持成 HTTPS，而沙箱的 git-credential-helper 不可用（404）。
#   这就是 `ghp` 存在的原因。

Host github.com
  HostName 20.205.243.166
  User git
  IdentityFile /root/.ssh/id_ed25519_vaultix
  IdentitiesOnly yes
  StrictHostKeyChecking no
  UserKnownHostsFile /dev/null
  LogLevel ERROR

# 备用别名：直连 IP，完全不经过 DNS。
# 用法：git push github-direct:Chaniug/Vaultix.git main
Host github-direct
  HostName 20.205.243.166
  User git
  IdentityFile /root/.ssh/id_ed25519_vaultix
  IdentitiesOnly yes
  StrictHostKeyChecking no
  UserKnownHostsFile /dev/null
  LogLevel ERROR

# 443 端口通道（防火墙只放行 443 时用）。
# ⚠️ HostName 必须是 IP：ssh.github.com 这个名字在本沙箱会解析到保留网段。
Host github-443
  HostName 20.205.243.160
  Port 443
  User git
  IdentityFile /root/.ssh/id_ed25519_vaultix
  IdentitiesOnly yes
  StrictHostKeyChecking no
  UserKnownHostsFile /dev/null
  LogLevel ERROR
