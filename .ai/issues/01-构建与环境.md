# ISSUES 分篇 · 工程 / 构建 / CI / 工具链 / 环境

> 索引：[`../ISSUES.md`](../ISSUES.md)（编号 → 分篇）· 共 **26** 条：#1、#2、#4、#5、#6、#7、#9、#10、#11、#12、#13、#14、#17、#18、#39、#40、#41、#42、#44、#45、#46、#55、#65、#74、#75、#115

---

## 1. Git Bash 与原生 git 的路径映射错位（2026-09-07）

- **现象**：Bash 中 `git clone /d/Vaultix` 报"目录非空"（实际为空）；`git clone /tmp/xxx` 退出码 0 但目录不存在
- **根因**：MSYS 与原生 Windows git 对 `/d/`、`/tmp` 解析不一致，`/tmp/xxx` 被落到 `D:\tmp\xxx`
- **解法**：涉及 git/Gradle 的目录操作一律用 **PowerShell + 原生 `盘符:\路径`**

## 2. AGP 9 迁移五连坑（2026-09-07）

| # | 坑 | 解法 |
|---|---|---|
| 1 | 不能 apply `kotlin-android` | AGP 9 内置 Kotlin，apply 会报 `Cannot add extension with name 'kotlin'` |
| 2 | `kotlinOptions` 废弃 | 改用顶层 `kotlin { compilerOptions { jvmTarget = JvmTarget.JVM_17 } }`（枚举，非 `JavaVersion`） |
| 3 | catalog 别名不能叫 `kotlin-jvm` | 与内置 `kotlin {}` DSL 撞名；改用 `kgp`，仅根工程 `apply false` |
| 4 | 类型安全项目访问器未启用 | `settings.gradle.kts` 加 `enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")` |
| 5 | Hilt 与 AGP/Kotlin 双绑定 | AGP 9 要求 ≥2.59；Kotlin 2.4.x 要求 **2.60.1**（metadata 版本不匹配会报 `Provided Metadata instance has version 2.4.0`） |

⚠️ Bastion 注释称"升 Kotlin 2.4 被 KSP 阻塞"**已过时**，真正在卡的是 Hilt。

## 4. 误用 `New-Item -Force` 覆盖已有文件（2026-09-07）

- **现象**：3 个已存在的测试文件被清空为 0 字节
- **教训**：创建文件前**必须先确认是否已存在**；切勿对可能已存在的文件用 `-Force`

## 5. 本机 SDK 没有 android-36（2026-09-07）

- `C:\AndroidSDK` 只有 35 / 37 / 37.0，故 `compileSdk` 必须 ≥ 37
- `D:\AndroidSDK` 是空壳，不可用

## 6. PowerShell 生成含 `$` 的代码时的转义陷阱（2026-09-07）

- **现象**：用 here-string 生成 Kotlin 代码时，`"bw_access::$server"` 被写成
  `"bw_access::server"`（`$` 被插值吞掉）——**所有库的凭据共用同一个 key，
  多库场景下 token 会互相覆盖**。这是真实上线级 Bug。
- **试过的错误转法**：`"`${'$'}server"` 产出 `$` 消失；单引号 here-string 里 `` `$ `` 保留反引号
- **解法**：代码里改用**常量前缀 + 字符串拼接**（`PREFIX + server`），完全避开 `$`
- **教训**：①生成脚本写完必须**校验输出文件内容**，不能只看脚本本身；
  ②需要插值变量时优先拼接而非模板；③写含 `$` 的代码用单引号 here-string `@'...'@`

## 7. 覆盖安装前提（记录，避免日后误改）

debug(preview) 与 release 可互相覆盖安装的**硬性前提**：
- 两者 applicationId 相同（debug **不加** applicationIdSuffix）
- 两者签名相同（已统一走 SIGNING_* Secret，jks 在 `D:\vaultix-release.jks`，密码用户自留）

## 9. KDoc 里写字面 `/**` → 注释嵌套直到 EOF（2026-09-08，排查最久的一个）

- **现象**：KSP 报一连串误导性错误——`ModuleProcessingStep ... 'BitwardenAuthInterceptor' could not be resolved`，且对**所有** @Provides 方法重复（连无参方法也是）；clean/重启 daemon/清缓存均无效
- **根因**：新文件 KDoc 写了「仅 `/api/**` 数据端点预挂」——Kotlin 块注释**支持嵌套**，`/**` 即 `/*` 开启一层嵌套注释，文件里再无第二个 `*/` 闭合 → 注释一直未闭合到 EOF → 整个文件解析失败 → 所有引用它的符号不可解析
- **解法**：注释文案避开 `/*` 序列（如改述「路径以 /api/ 开头」）
- **教训**：KSP 报「类无法解析」先怀疑**被引用文件本身有没有解析错误**（尤其是注释/编码），不要只在引用侧找原因

## 10. KSP2 对 @Provides 签名里的具体新类解析失败（2026-09-08）

- **现象**：`@Provides fun x(...): MyNewClass`（同模块新类）→ KSP「could not be resolved」；改为返回接口类型（`okhttp3.Interceptor`）后在函数体内构造具体类 → 通过
- **解法**：@Provides 签名一律用接口/已有类型；具体类只出现在函数体
- **备注**：疑似 KSP2 增量/新文件符号注册缺陷；2.0 稳定后复测可移除该限制

## 11. mockk：普通函数用 `every`，suspend 用 `coEvery`（2026-09-08）

- **现象**：`coEvery { repo.logout(any()) }` 配普通（非挂起）函数 → 运行时报 `no answer found for ... logout(...)`，编译不报错
- **解法**：非 suspend 函数 stub 用 `io.mockk.every`；仅 suspend 用 `coEvery`
- **关联**：DAO 是 suspend（Room）用 coEvery；领域普通方法（lockVault/logout）用 every

## 12. 测试 fixture 用占位字符串掩盖真实路径（2026-09-08）

- **现象**：引入「更新=合并上传」后 `updateItem_preservesIdAndRevision` 失败——`IllegalStateException: 本地密文损坏`
- **根因**：既有 fixture 的 `encryptedPayload = "stale"`（非 JSON）——以前 update 不读 payload，现在要解码合并
- **解法**：fixture 重建为真实链路产物（`mapper.toRequest(...).toStoredCipherDto(...)` 再 encode）
- **教训**：测试数据要贴近真实格式；存储层语义变更后旧占位 fixture 是最先暴露的假阳性

## 13. Dagger 依赖环新形态：拦截器引入认证仓库（2026-09-08）

- **现象**：给 OkHttp 加「预挂 Bearer 拦截器」后 Hilt 报
  `Retrofit.Builder → BitwardenApiFactory → ... → OkHttpClient` 构造期环
- **根因**：拦截器构造即拉 `AuthRepository`，而 AuthRepository → ApiFactory → Builder → OkHttpClient
- **解法**：拦截器注入 `Provider<AccessTokenProvider>` 请求时再 `get()`（与 BitwardenAuthenticator 同款断环；**eager 会死锁**）
- **判据**：网络栈出现 client→auth→client 自环一律 Provider 懒断环

## 14. Gradle daemon 大任务批崩（2026-09-08）

- **现象**：一次跑 6 模块单测+detekt，daemon 直接消失（heap 2GiB 不够）「daemon disappeared unexpectedly」
- **解法**：拆批执行（编译一批 / 单测一批 / detekt 单独）；单测失败先从 XML 报告定位再重跑

## 17. Detekt 门禁常见三连（2026-09-09）

- `MatchingDeclarationName`：文件里只有**一个**顶层类/对象声明时，文件名须与其同名
  → 把 data class 拆到独立文件（如 `AppInfo.kt` 与 `AppPickerDialog.kt` 分离）
- 给表单 / 页面新增 UI 后 `LongMethod` / `CyclomaticComplexMethod` 超阈值 → 把
  `if (showX) { Dialog { ... } }` 抽成 `XHost(...)` 私有 composable（比事后重构便宜得多）
- `MagicNumber`：`fillMaxWidth(0.95f)` 这类比例值也要常量化（Compose 的 `.dp` 数字不受影响）
- `Unresolved reference 'Bolt'`：`Icons.Filled.*` 并非全量 Material 图标都可用，
  换用项目里已验证存在的图标（如 `ContentCopy`）——编译前先确认图标存在

## 18. Kotlin 默认参数不能调用 suspend 函数（2026-09-09）

- **现象**：`suspend fun f(clearMs: Long = prefs.preference.first())` 编译失败
  （默认参数表达式不是挂起上下文）
- **解法**：默认参数只能是非挂起表达式 → 在函数体内读取偏好，或由调用方显式传值

## 39. 沙箱无法推送 GitHub（2026-09-11，P1，已解决）

**现象**：`git push` 报 `could not read Username`；`git fetch` 报 `gnutls_handshake() failed`；
`ssh` 报 22 端口超时。

**根因**：沙箱把 GitHub 域名解析到 `198.18.0.x`（保留测试网段，被网关劫持），HTTPS TLS 被断；
SSH 22 端口被墙；`git-credential-helper` 对 github.com 返回空、非交互环境取不到 HTTPS 凭证。

**解决**：阿里 DoH 查真实 IP → 写 `/etc/hosts` **并同步 `~/.user_hosts`** →
`~/.ssh/config` 令 `github.com` 走 `ssh.github.com:443` → `git remote` 改 SSH URL。
推送成功。**恢复步骤见 `.ai/MEMORY.md` 第三十三轮「可复用配方 1」。**

---

## 40. detekt 圈复杂度「越拆越高」——同文件 helper 被累加进调用方（2026-09-11，P0）

**现象**：`VaultixCredentialProviderService.resolvePasskeys` 报 `CyclomaticComplexMethod`
complexity **41**（阈值 14）。把它拆成若干**同文件** private helper 后，复杂度不降反升
（原内联版 19 → 拆后 41）。

**根因**：detekt **2.0.0-alpha.6** 的 `CyclomaticComplexMethod` 会把**同一文件内**被调用的
私有函数的复杂度**累加**进调用方（非标准 McCabe 行为）。同时该版本
`ignoreNestingFunctions` 默认 **false**（1.x 为 true）→ 作用域函数
（`let`/`run`/`with`/`apply`/`also`/`forEach`/`use`）每个 +1。

**实证方法（可复用）**：把可疑函数的**函数体临时 stub** 成 `return emptyList()` 再跑 detekt。
- 违规**消失** ⇒ 复杂度来自「调用」（本情况）；
- 违规**仍在** ⇒ 复杂度在自身函数体。
> 不要凭"标准 McCabe 是 per-function"的常识推断 —— 本版本就是不走标准。

**解法**：把 helper 拆到**独立文件**（detekt 逐文件分析、**不跨文件累加**）。
本轮新建 `app/src/main/java/io/vaultix/vaultix/passkey/PasskeyResolution.kt`：
- `PasskeyMatch` / `PasskeyCounts` 改 `internal` 顶层；
- `collectPasskeyMatches(itemRepository, unlocked, rpId)` / `applyAllowedFilter(rpMatched, allowed, log)`
  / `collectStoredRpIds(itemRepository, unlocked)` 顶层函数（inject 依赖以参数传入）；
- `resolvePasskeys` 主函数仅编排（自身 ~5），各 helper 独立 < 14。

**副作用**：`PasskeyMatch` 从 service 内 private 嵌套类移到同包顶层 `internal`（`publicKeyEntry`
同包引用，无需 import）；`WebAuthn` / `VaultFido2Credential` import 随之从 service 移除。

---

## 41. CI 1 分钟就红 → 其实是 detekt 先于 compile 失败，掩盖了编译错误（2026-09-11，P0）

**现象**：`main` 推送后 CI ~1 分钟即失败（正常约 3 分钟）。表面只见 detekt 报错，
容易误以为"修完 detekt 就好"。

**根因**：workflow 步骤顺序 = **detekt → (lint) → compile → assemble**。detekt 一失败即中断，
**编译步骤永不被执行** ⇒ 上一提交引入的 3 处 `Int?` 空安全**编译错误**被完全掩盖
（`d6f3409` 引入，但它同时把 detekt 弄红，编译错误一直没暴露）。

3 处编译错误与修法：
| 文件 | 错误 | 修法 |
|---|---|---|
| `core/datastore/.../VaultTimeout.kt` | `associateBy { it.vaultTimeoutInMinutes }` 键实为 `Int?` | `mapNotNull { t -> t.vaultTimeoutInMinutes?.let { it to t } }.toMap()` |
| `app/.../ui/settings/SettingsScreen.kt` | 组合 `when` 分支不做智能转换，`timeout.vaultTimeoutInMinutes` 仍 `Int?` | `requireNotNull(timeout.vaultTimeoutInMinutes)` |
| `app/.../passkey/CredentialProviderIntentUtils.kt` | 返回类型写成 `BeginCreateCredentialRequest?`，实际是 `CreateCredentialRequest?` | 改返回类型为 `CreateCredentialRequest?`（见下） |

**类型陷阱细节**：`BeginCreateCredentialRequest`(`androidx.credentials.provider`) 与
`CreateCredentialRequest`(`androidx.credentials`) 是**两个互不相关**的类（无继承关系）。
`PendingIntentHandler.retrieveProviderCreateCredentialRequest(intent)` 返回
`ProviderCreateCredentialRequest`，其 `callingRequest` = **客户端侧** `CreateCredentialRequest`
（credentials 1.6.0 `javap` 实证）。⇒ 由 Intent **无法**还原服务端侧的 `BeginCreateCredentialRequest`。
连带把 `CredentialProviderRequestManager.createCredentialRequest` / `setCreateCredentialRequest`
也改为 `CreateCredentialRequest`（该字段 **write-only**、无读取方，改动安全）。

**教训**：
1. **detekt 只做静态检查、不做类型检查** ⇒ 用"去魔法数字"等手段改代码后，
   **必须真跑一次 compile**（本地 `:app:compileFullDebugKotlin`）。
2. CI 步骤顺序会让**前置步骤失败掩盖后续步骤的问题** —— 排障先看"哪一步真的跑了"，
   别被"最后一条报错"误导（本次真正待修的还有编译错误）。

---

## 42. 本机（Windows/WorkBuddy）与早前沙箱的环境差异（2026-09-11）

早前多轮记录的"沙箱无 Android SDK / JDK17、`gradlew` 跑不起来、`ghu_` token 对
`api.github.com` 401" **仅适用于沙箱**。本机（用户 Windows + WorkBuddy）**全部可用**：
- JDK 17 + Android SDK（`C:\AndroidSDK`）齐全 → 可本地 gradle 构建/跑测试；
- `gh` CLI 可正常 `gh run list/view/watch`（查 CI 状态无需再读网页端 job 页）；
- `git push` 经 SSH（`git@github.com:Chaniug/Vaultix.git`）正常。

**唯一坑**：Git Bash 下 `./gradlew` 报「找不到或无法加载主类 GradleWrapperMain」。绕过：
```bash
java -classpath "D:/Vaultix/gradle/wrapper/gradle-wrapper.jar" \
     org.gradle.wrapper.GradleWrapperMain <task> [--no-configuration-cache]
# 长时间任务/daemon 配额耗尽：先 ... GradleWrapperMain --stop
```

**参考源码本地副本（不纳入 git，位于仓库外 `D:\Vaultix-refs\`）**：
| 项目 | 路径 | HEAD |
|---|---|---|
| Bitwarden Android | `D:\Vaultix-refs\bitwarden-android` | `74c0e04` |
| Keyguard | `D:\Vaultix-refs\keyguard-app` | `f95c865` |

---

## 44. 沙箱构建环境三处修复（2026-09-12，长期收益，非项目代码问题）

> 本轮把沙箱从「**完全跑不了 Gradle**」恢复到「**可真实编译 / 跑单测 / 跑 detekt**」，
> 长期收益显著（此后无需仅靠 CI 试错）。

| # | 问题 | 修法 |
|---|---|---|
| 1 | `/root/.gradle/init.gradle` **语法错误**（`mavelCentral()` 拼写错 + url 未加引号）→ **所有** Gradle 调用初始化即失败 | 重写为 `beforeSettings { settings -> ... }` 注入 Aliyun/腾讯镜像。**必须同时注入 `pluginManagement` 与 `dependencyResolutionManagement` 两处** —— 项目设了 `RepositoriesMode.FAIL_ON_PROJECT_REPOS`，往 `allprojects.repositories` 塞仓库会被直接拒绝 |
| 2 | AGP 插件解析失败（`com.android.application:9.3.2` not found） | 镜像**必须在 `pluginManagement` 层**注入（`settingsEvaluated` 太晚） |
| 3 | `SDK location not found` → `sdkmanager` 报 `Failed to find package 'platforms;android-37'` | **platform 37 的目录名带扩展版本号（`android-37.0`），在线 `repository2-3.xml` 里根本没有 `platforms;android-37` 这个包**（最高只到 android-36）⇒ `sdkmanager` 必然失败。只能按已知包名直取 `platform-37.0_r01.zip` + `build-tools_r37_linux.zip` 解压安装 |

### 附带
- **hosts 补 `dl.google.com → 113.108.239.161`**（此前被污染到 fake-ip `198.18.0.13`，
  导致 manifest 拉取静默失败）。`sed` 写 hosts 报 `Device or resource busy` → 改用 python 重写。
- **Gradle wrapper 分发包**：`services.gradle.org` 302 → GitHub，TLS 抖动报
  `SSLHandshakeException` → 经 `ghfast.top` 镜像下载 `gradle-9.5.1-bin.zip`（140MB），
  装入 `~/.gradle/wrapper/dists/gradle-9.5.1-bin/<hash>/` 并 `touch gradle-9.5.1-bin.zip.ok`。
- **Gradle daemon OOM 被杀**（cgroup `memory.max` = 8GB）→
  加 `-Dorg.gradle.jvmargs="-Xmx3g" -Dkotlin.daemon.jvmargs="-Xmx2g"`。
- `api.github.com` 真实 IP `20.205.243.168`（`gh` CLI 有 TLS 抖动 → 改用
  `curl --resolve api.github.com:443:20.205.243.168` 直连；响应含控制字符需
  `json.loads(..., strict=False)`）。

### ⚠️ 唯一未能完成的步骤
`assembleFullDebug` 的 **native 符号剥离**（`stripFullDebugDebugSymbols` 报
`Cannot access output property 'outputDir'` / `Failed to create MD5 hash`）—— 沙箱**缺 NDK**，
**与代码改动无关**；该步在 CI 上正常。替代验证用 `:app:compileFullDebugKotlin`。

---

## 45. CI「Run unit tests (non-blocking)」长期报红 2 处（2026-09-12，`59b57e8`）

**现象**：GitHub 每次构建都挂一个 annotation —— `Annotations: 1 error and 7 notices`，
但 run 结论仍是 **success**（用户反馈"出包成功但有报错"）。

**根因**：该步骤是 **`continue-on-error`（non-blocking）**，所以**从不阻塞出包**、长期无人发现。
里面有两个失败：

| 测试 | 性质 | 根因 |
|---|---|---|
| `VaultixCryptoTest.removePkcs7PaddingStripsTrailingRandomPadding` | **陈旧用例（必挂）** | 实现 2026-09-11 已收紧为**严格 PKCS#7**（末 pad 个字节须**全等于** pad），用例却仍断言旧的**宽松**行为（喂 6 字节随机填充、末字节 0x06，期望剥 6 字节） |
| `ActiveVaultStoreTest.selectTakesEffectImmediatelyAndPersists` | **真竞态（偶发）** | `init` 的收集协程跑在**进程级 `Dispatchers.Default`** 上，若首帧**晚于** `select()` 到达，就会把刚选的库**覆盖回** `pick()` 的结果 |

**修法**：
- crypto：用例改为断言「随机填充**原样返回**」并重命名 `removePkcs7PaddingKeepsTrailingRandomPadding`。
- ActiveVaultStore：测试先 `store.activeVaultId.first { it != null }` **等首帧落地**再 `select`。

**★ 教训**：
1. **non-blocking 步骤的失败不会让 CI 变红，但会一直污染信号** —— 应作为常规清理项定期巡检
   （`gh run view <id> --log | grep "tests completed, .* failed"`）。
2. 「测试挂了」先分清**陈旧用例**（实现已变、用例没跟上）与**真缺陷/竞态**（实现有问题）——
   前者改用例，后者改代码或让测试确定化，**不要一律改断言迁就实现**。

---

## 46. Android 16+ `Settings.Secure` 对第三方 App 受限 → 状态检测恒报"未启用"（2026-09-12，`9fca5ab`）

**现象**：荣耀（Android 17）上设置页「凭据提供商（通行密钥）」**恒显示"未启用"**，但功能其实是通的。

**根因**：`CredentialProviderStatus.isEnabled` 读 `Settings.Secure:credential_service`，
判据是 `!raw.isNullOrBlank() && raw.contains(类名)`。
而 **Android 16（API 36）起 `Settings.Secure` 对第三方 App 受限**
（Bitwarden `data/autofill/accessibility/util/ContextExtensions.kt` 注释点名：
"required for Android 16+ where Settings.Secure is restricted for third-party apps"）
⇒ App 内读到 `null` ⇒ 恒判"未启用"。
（**adb shell 权限更高**，所以 `adb shell settings get secure credential_service` 仍能看到值 ——
用 adb 验证过 ≠ App 内读得到，两者权限不同。）

**修法**：取向改为与 `AutofillStatusChecker.isSystemEnabled` **完全一致：读不到 → 按"已启用"处理**。

**★ 教训**：**"读不到" ≠ "未启用"**。凡读系统设置判状态，都要先想"这个 App 读得到吗"；
项目里 `autofill_service` 早就采用了"读不到按已启用"的取向，新加检测项**要沿用同一取向**，别各写一套。

---

## 55. `./gradlew` 在本机失效 + 本机 Gradle 发行版直调（2026-09-12 复核）

`./gradlew` 报 `ClassNotFoundException: org.gradle.wrapper.GradleWrapperMain`（jar 完好）。
本机直接调用磁盘发行版即可（PowerShell）：
```powershell
$env:JAVA_HOME="C:\Program Files\Microsoft\jdk-17.0.20.8-hotspot\"
& "C:\Users\chani\.gradle\wrapper\dists\gradle-9.5.1-bin\iq79hdu3mqx29lgffhp8bfmx\gradle-9.5.1\bin\gradle.bat" `
  --offline :app:compileFullDebugKotlin :app:testFullDebugUnitTest :data:repository:testDebugUnitTest detekt
```
⚠️ app 模块有 `full` / `offline` 两个 flavor，`compileDebugKotlin` 有歧义，必须写
`:app:compileFullDebugKotlin`。
⚠️ **`onFillRequest` 不是挂起函数**：在里面直接 `prefs.xxx.first()` 会编译失败
（`Suspend function 'first' can only be called from a coroutine`）——读取偏好必须放进
`scope.launch { }` 内（本轮踩过，已在 `VaultixAutofillService` 内注明）。

---

## 65. KDoc 里写「星号 + 斜杠」→ 整个文件变语法错误（2026-09-12，第四十八轮）

**现象**：`AddKdbxScreen.kt` 编译报一连串 `Syntax error: Expecting a top level declaration`
（都在同一行），文件后半段全部失效。

**根因**：KDoc 里写了 MIME 通配符的字面序列（星号紧跟斜杠）——
Kotlin 块注释**支持嵌套**，那个序列被解析成**注释结束符**，于是后半段代码跑到了注释外面。

**这是 #9 的同一个坑的第二次出现**（当年是 KDoc 里写 `/**`）。

**解法**：改述为「通配 MIME（星号斜杠星号）」，并在注释里明文警告后来者。

**判据**：注释里不要出现任何「斜杠 + 星号」的相邻组合（两个方向都算）。
`grep -n '\*/\|\/\*' <新写的注释>` 是个 5 秒就能做的自检。

---

## 74. detekt 不会对 @Composable 网开一面 —— 抽块时参数必须 ≤8（2026-09-13）

**现象**：本轮加完多选与美化后 detekt 报 5 项越线（`LongMethod` 151/171/182、
`CyclomaticComplexMethod` 15/16）。

**根因**：项目只给 `FunctionNaming` 配了 `ignoreAnnotated: ['Composable']`
（放行 PascalCase 命名）。**`LongMethod` / `CyclomaticComplexMethod` /
`LongParameterList` 不豁免**，`ignoreDefaultParameters` 也是 `false` ⇒
「把大块 UI 抽成一个 13 参数的 composable」这种常见做法**会踩 `LongParameterList`**。

**解法**：按「参数 ≤8」拆，行数不够就把**多行 lambda 收敛成文件级 helper**：
- 抽出 `TotpSelectionBar` / `BoxScope.TotpOverlayTopBar` / `ItemRowSubtitle` /
  `ItemRowTrailing` / `ItemFormTail` / `AddRequestEffect` / `ItemsEmptyState`；
- 把「选中集合翻转」收敛成 `ui.common.toggleSelection(set, key)`（两页共用，
  一处 6 行三元 → 一行）；`contentWindowInsets` 的三元压成一行。

**判据**：**Compose 页面天然长，抽块是常态，但抽取物本身也要过门禁。**
先想「这块的参数有几个」，超过 8 就继续拆或传 ViewModel 而不是一堆 lambda。

---

## 75. 给函数加「带默认值的尾部参数」会吃掉调用点的尾随 lambda（2026-09-13）

**现象**：`FillPlanner.plan()` 新增 `serverOrigin: String? = null` 之后，
单测 `FillPlannerTest` 编译失败：`No value passed for parameter 'totpProvider'` +
`actual type is '() -> String', but 'String?' was expected`。

**根因**：原调用写成 `plan(a, b, c, d) { "123456" }` —— 尾随 lambda 绑定的是
**最后一个参数**。新增的 `serverOrigin` 插在了 `totpProvider` **后面**，
于是 `{ "123456" }` 被当成 `serverOrigin`（类型 `String?`），`totpProvider` 反而没传。
**默认值让它在调用点静默地改变了语义**，编译器只在类型不匹配时才报出来。

**解法**：调用点改用具名参数 `totpProvider = { "123456" }`。

**判据**：**新参数加在末尾时，务必全局搜一遍该函数的尾随 lambda 调用点。**
「有默认值所以不会破坏兼容性」只对具名调用成立。

---

## 100. 🔴 **CI 的 `Run lint` 步骤从未真正跑过** —— 首次非 push 触发即失败（2026-09-15）

**现象**：本地推送全绿，我手工 `gh workflow run ci-debug.yml` 想补一次编译验证，
结果 run `34920991273` 在 **`Run lint`** 步骤失败，后续 detekt / 编码检查 /
**Build Debug APK** 全部 `skipped`。改动本身**只是两个 .md 文档**，代码一行没动。

**根因**：`ci-debug.yml` 里 lint 步骤带条件

```yaml
- name: Run lint
  if: github.event_name != 'push'
```

即 **push 不跑、只有 PR / 手动触发才跑**。而本仓库历史上（最近 50 次运行）**全部是 push**；
这次是我**第一次**非 push 触发 ⇒ **lint 步骤是第一次真正执行**，一执行就红。

⇒ 换句话说：**这条门禁此前从未被验证过一次**，它是否可用**无人知道**。
所以「本地/CI 全绿」这个结论里，**不包含 lint**（push 链路本来就不跑它）。
这是**盲区**，不是回归 —— 但它同时说明：`ci-debug.yml` 的注释里写着
「PR / 手动触发时额外跑 Lint 作为质量门」，这道门**实际上从来没关上过**。

**附带发现（同一盲区）**：`ci-debug.yml` 把 Gradle 固定成 **8.11.1**
（`gradle/actions/setup-gradle` 的 `gradle-version: 8.11.1`，
且 `validate-wrappers: false`），而仓库 wrapper 是 **9.5.1**
（`gradle/wrapper/gradle-wrapper.properties`）。AGP 9.3.2 配 Gradle 8.11.1
在版本兼容矩阵上是**说不通**的组合。⚠️ **但 push 链路的 `./gradlew` 用的是 wrapper 9.5.1**
（`setup-gradle` 固定的是它自己那个 Gradle，不影响 `./gradlew`）⇒ 所以 push 构建能过。
**待确认**：lint 这一步到底是因为「Gradle 8.11.1 与项目/AGP 不兼容」失败，
还是因为真的存在 lint 违例。**两者处置完全不同**，不要急着改代码。

**处置（本轮）**：**不猜、不改 lint 规则、不压代码**。理由：
① 失败原因未取证（沙箱拿不到 job 日志，见下）；
② 它是**历史遗留的盲区**，不属于本轮「设置页 + M3E」的任务范围；
③ 改 lint 配置属于**降低门禁**，必须知道削弱的是什么才能改。

**但要立刻止血**：lint 失败会 **skip 掉 detekt / 编码检查 / Build Debug APK**，
等于一个空转的门禁把真门禁全堵死。所以 lint 步骤应改为
`continue-on-error: true`（或在未定性前先不影响后续步骤）—— 让它**先只报告、不阻断**。

**沙箱限制（值得记住）**：`gh api .../actions/jobs/<id>/logs` 会 302 到
`productionresultssa1.blob.core.windows.net`，**沙箱网络不可达**（下载得到 0 字节）。
⇒ 沙箱里**看不了 CI 原始日志**。替代手段（按可用性排序）：
1. **check-run annotations**：`gh api repos/<o>/<r>/check-runs/<id>/annotations`
   —— 能拿到编译器报错文本（上一轮就是靠它抓到 3 个编译错误）；
2. `gh run view --json jobs --jq '.jobs[].steps[] | {name,conclusion}'` —— 定位**哪一步**失败；
3. **手动触发对应 workflow**，在失败前把怀疑对象改成 `continue-on-error` 逐步缩小范围。

**判据**：**「CI 全绿」必须问清是"哪条链路"的绿。**
带 `if: github.event_name != 'push'` 的步骤，push 全绿**完全不代表**它通过。
凡是要声明「门禁通过」，先确认这条步骤在**本次事件类型**下真的执行了
（`gh run view --json jobs` 看步骤 conclusion，`skipped` 不等于通过）。



### 100.1 更正：lint 本身是**好的**；第一次失败是**我自己的 workflow 结构问题**（同日）

给 lint 加 `continue-on-error: true` 之后再触发一次（run `34921953234`），
**`Run lint` 报 success**、且 detekt / 编码 / Build Debug APK / 单测全部**真的跑起来了**。

⇒ 所以 #100 前半段「一次都没通过」这个判断**下早了**（我确实只看到 "第一次执行就红"，
但**第一次执行也在 `018df60`、上一次真实 lint 在更早的代码状态上**，
这两次代码差异极小，不足以解释"这次绿"）。**真正的解释是这两次的结构不同**：

| run | commit | 产物 | 结果 |
|---|---|---|---|
| `34920991273` | `018df60` | 文档 | lint **failure** → 后续全 skip |
| `34921953234` | `45ead7b` | 文档 + `.github/workflows/ci-debug.yml` | lint **success** → 后续全跑 |

**机制**（看注解里的 `build_failed=false` 这条 notice 与 workflow 逻辑）：

1. `Collect debug APK metadata` 步骤带
   `if: github.event_name == 'push' && github.ref == 'refs/heads/main'`；
2. 步骤体里 `apk_count == 0` 时 `exit 1`；
3. **非 push 时该步骤被 skip ⇒ 它的 `exit 1` 不会发生**。

⇒ 这解释了一个**反直觉**的现象：**非 push 触发时，语义上"更轻"的那次反而能跑通**。
上一次非 push 失败，是因为它**同时命中了代码检查与 workflow 自身变更**这条路径，
与 lint 本身无关。

**结论（要改的是 #100 的定性，不是 lint）**：
- ✅ **lint 步骤可用**（`34921953234` 已证明既能跑又能通过）；
- ⚠️ 但 #100 的**方法论**依然成立且重要：**带 `if:` 的步骤，push 绿不代表它通过** ——
  `34921886414`（push）的步骤列表里 `Run lint` 明明白白写着 **`skipped`**。
  这一条**不会再改**。
- ❓ **仍未定性**：`ci-debug.yml` 固定 Gradle **8.11.1** 而 wrapper 是 **9.5.1**，
  以及非 push 时 `Collect debug APK metadata` 会因产物目录布局（push 出 ARM64 单包）
  而 `exit 1` 的**结构性问题** —— 后者才是非 push 链路真正需要修的。
  ⇒ 下一轮：要么给该步骤也加 `if` 守卫/宽松处理，要么让非 push 也产出 APK。

**判据（这次真正的教训）**：**看到"第一次执行就失败"，别急着给被执行的步骤定罪。**
先分清「这一步本身坏了」与「这一步的背景条件变了」——
我这次差点就顺着"lint 从未验证过"的叙事去动 lint 配置（**那是在改错东西**）。
取证顺序应该是：先让后续步骤能跑（隔离），再判断失败者是谁。**

---

---

## 101. 🔴 `import` 的**包路径**写错：detekt 与自检工具**双双查不出**，只有 CI 编译能炸（2026-09-15，`e3f6f05` 已修）

**现象**：CI run `34929618286`（commit `0f83efe`）在 `Build Debug APK` 步骤失败：

```
e: .../ui/passkeys/PasskeysScreen.kt:47:35  Unresolved reference 'WindowInsets'.
e: .../ui/passkeys/PasskeysScreen.kt:137:73 Unresolved reference 'WindowInsets'.
```

**根因**：`WindowInsets` 属于 `androidx.compose.foundation.layout`，
而该文件误写成了 `androidx.compose.material3.WindowInsets`（**该包下并不存在此符号**）。

**为什么两个本地工具都没拦住 —— 这是本次真正的收获**：

| 工具 | 它查什么 | 为什么漏掉 |
|---|---|---|
| detekt | 静态规则集（复杂度/行长/命名/未用参数…） | **不做符号解析、不做类型检查**，"符号名写对、包名写错"它对不上号 |
| `check_signature_types.py` | 函数**签名里**的类型名是否存在 | 只取名字去查表，**从不读 `import` 语句本身** |

⇒ **`import` 行是一条无人看守的缝**。而 Compose 的符号恰恰横跨
`foundation.layout` / `material3` / `ui` / `ui.graphics` 多个包、大量同名，
是最容易"凭印象写"的地方。

**取证要点（可复用）**：沙箱**下不到 CI 的 raw log**（302 跳 blob 存储、取不到）。
用 **check-run annotations** API 直接拿编译错误行：

```bash
gh api repos/Chaniug/Vaultix/check-runs/<job_id>/annotations \
  --jq '.[] | .path, .start_line, .message'
```

两条错误一次拿全，比翻日志快得多。

**修复**：`material3.WindowInsets` → `foundation.layout.WindowInsets`，
位置放在 `...layout.Spacer` 之后、`...layout.fillMaxSize` 之前，
与 `TotpCodesScreen.kt` / `ItemsScreen.kt` / `SettingsScreen.kt` 的导入序一致。

**验证**：run `34930096397` → `conclusion: success`，
`Build Debug APK (build gate)` 与 `Publish Debug APK to preview Release` 均 success，
APK 已产出：`preview` Release / tag 带 commit 短 sha（`dev-e3f6f056…`）。

### 101.1 新增守护工具：`.ai/tools/check_import_packages.py`

为堵上这条缝，新增了 import 包路径校验器。**判据用仓库自己当基准，不维护 Compose 符号表**：

- **规则 A · 独占包一致性**：某简单名若在全仓库**只从一个包**导入过（且样本 ≥2），
  则该符号的包已形成约定；任何一处从别的包导入即报错。
  （`WindowInsets` 全仓库 8 处、全部来自 `foundation.layout` ⇒ 独占 ⇒ 可判定）
- **规则 B · 陷阱表**：少数硬知识符号（`WindowInsets` / `Modifier` / `LazyColumn`…）直接查表。

**⚠️ 设计过程中的一次自我纠正（重要）**：规则 A 的**首版用的是"多数包"**，
结果产生 2 处**误报**，且都是**合法**用法：

| 误报名 | 实际情况 |
|---|---|
| `lerp` | `ui.graphics.lerp`（颜色插值）与 `ui.unit.lerp`（`Dp` 插值）**是两个真函数**；`ExpressiveTopBar.kt` 同时导入二者，各自都用到了 |
| `LocalLifecycleOwner` | `compose.ui.platform`（旧）与 `lifecycle.compose`（新）**都存在**，属 API 迁移期共存 |

⇒ 改为**"独占包"判据**：只要该名字在 ≥2 个包里出现过，**一律跳过不查**。
**宁可漏报，不可误报** —— 误报会让人开始怀疑工具，最终弃用。

**回归验证（证明它真能拦住原 bug）**：把 `material3.WindowInsets` 重新注入副本文件后：

```
L47: `WindowInsets` 导入自 `androidx.compose.material3`，但本仓库约定为 `androidx.compose.foundation.layout`
```

**报错行号 `L47` 与 CI 报的 `47:35` 完全吻合。** 若当时有它，可省掉整整一轮 CI。

退出码：`0` 干净 / `1` 有可疑项（可直接接进 CI 或 pre-commit）。

### 101.2 同一条缝的第二次发作：实验性 API 的 `@OptIn`

`check_import_packages.py` 上线同一天，**又踩了同一类坑**：引入 `BasicAlertDialog`
重做对话框外壳（设置页「当前密码库 / 快速解锁」+「添加密码库」三处），
三个调用点**全漏了 `@OptIn(ExperimentalMaterial3Api::class)`**。
CI `Build Debug APK` 直接失败，**后续步骤（含 APK 发布）全被 skip**：

```
e: .../ui/settings/SettingsScreen.kt:979:5  This material API is experimental and is likely to change or to be removed in the future.
e: .../ui/settings/SettingsScreen.kt:517:5  This material API is experimental and is likely to change or to be removed in the future.
e: .../ui/common/AddVaultTypeDialog.kt:85:5 This material API is experimental and is likely to change or to be removed in the future.
```

**为什么又是本地工具的死角**：三个工具全都不看注解 ——

| 工具 | 它查什么 | 为什么漏掉 |
|---|---|---|
| detekt | 静态规则集 | 不做符号解析，**不判断某个符号是否带 `@Experimental*`** |
| `check_signature_types.py` | 签名里的类型名 | 只看名字，不看注解 |
| `check_import_packages.py` | import 的包路径 | `BasicAlertDialog` 的包路径**完全正确**（确实在 `material3`） |

而且本工程**没有任何工程级 opt-in 编译参数**——`app/build.gradle.kts` 的
`kotlin { compilerOptions }` 里只有 `jvmTarget`，所以**每个调用点都得手写**，
不存在"漏了也能过"的兜底。**只能靠真实编译器。**

**取证**同 #101：annotations API，一次拿全三行，无需日志。

**修复**：三处补 `@OptIn(ExperimentalMaterial3Api::class)`（commit `fc1fa6c`），
CI run `34938509113` → `conclusion: success`。

### 101.3 新增守护工具：`.ai/tools/check_experimental_optin.py`

**判据必须模拟「注解作用域」，不能是文本搜索**。Kotlin 的 `@OptIn` 作用于
**被它注解的那个函数**；而 `@Composable` 函数会被**内联**进调用方，
所以只要**调用链上有一层**带了 `@OptIn`，实际就是合法的
（典型结构：顶层 `Screen` 带 opt-in，内部子 composable 直接用）。

于是判据是「迭代到不动点」：

1. **自证**：函数自己的注解块覆盖了它用到的全部实验性符号 ⇒ 合法；
2. **继承**：有另一个**已合法**的函数**调用了它** ⇒ 合法（内联向下传递）。

⚠️ 第 2 条是**反向边**——合法性从「调用方」流向「被调方」。
实现时**第一次把方向写反了**（去查"我调用了谁"），自测立刻抓出来了。

**⚠️ 头一版更糟，直接在干净仓库上报了 17 处误报**：当时用"`fun` 之前 4000 字符内
出现过 `@OptIn` 就算过"这种懒判据，结果 `TopAppBar` / `ExposedDropdownMenuBox` /
`CircularWavyProgressIndicator` 这些**内联继承的合法用法**全被误判，
连它本该抓的那 3 处都淹没了。**宁可漏报，不可误报**（#101 的教训）。

**已知漏报（接受了）**：跨文件的"调用了一个已 `@OptIn` 的 composable"
判不了（需要真解析，超出单文件正则的能力）。实测本仓库无此结构；
漏报代价只是"CI 再炸一次"，误报代价是"工具被弃用"。

**回归验证（证明它真能拦住原 bug）**：删掉三处 `@OptIn` 后扫描，精确报出且**零噪声**：

```
SettingsScreen.kt:516   `ActiveVaultDialog()` 里用了 `BasicAlertDialog`…
SettingsScreen.kt:978   `QuickUnlockManageDialog()` 里用了 `BasicAlertDialog`…
AddVaultTypeDialog.kt:86 `AddVaultTypeDialog()` 里用了 `BasicAlertDialog`…
```

与 CI 的三处**一一对应**；还原文件后立刻回到 `[OK]`。内置 `--selftest` 五项
（漏注解 / 已注解 / KDoc 里的 `@OptIn` 不算数 / 内联继承 / 跨文件漏报）全绿。

**贯穿 #101 的可复用结论**：detekt + 三个自检脚本覆盖的都是**语法层**，
而**符号解析层**（包是否存在、注解是否齐、API 是否实验性）**只有真实编译器在看**。
每被炸一次，就该问一句：「这条缝能不能用 20 行 Python 补上？」
能补就补，补不了就**明确写下漏报边界**，别假装覆盖了。

退出码：`0` 干净 / `1` 有可疑项。

### 102. ⚠️ 交付前「资产存在」≠「能下下来」（2026-09-15 我自己的误判）

差点把一件没验证的事当成已验证的交付给用户。

**经过**：修好 `@OptIn` 后 CI success、APK 发布。我用 `gh release view preview`
查到 `app-full-debug.apk` / 32193189 bytes，就**在给用户的结论里写了「APK 可下载」**。
随后真去 `curl` 下载 —— **`http=000`、`size=0`，17 分钟后超时失败**。

**根因**：`gh release view` 走的是 **API**（`api.github.com`，hosts 已修好，通）；
而真实下载会 **302 到 `release-assets.githubusercontent.com`**，
该域名在沙箱被解析到 **`198.18.0.22`（保留网段，代理占位）** ⇒ 连接 EOF。
**两个域名，两条链路，一好一坏。**

**教训（重要，跨领域适用）**：

- **「元数据正确」永远不能替代「端到端跑通一次」。**
  大小、tag、checksum 都是从 API 读的，**和能不能下载是两件事**。
- 交付物但凡要用户去取，**交付前必须真取一次**。
  没取成功就别写「可下载」——要么写清楚未验证，要么先修通。
- 排查这类"域名被劫持"的**必要动作**：`getent hosts <域名>`。
  看到 **`198.18.0.x` 就该警觉** —— 这是 RFC 2544 基准测试保留段，
  真实公网服务不可能用，出现即代表被代理/DNS 接管。
  （GitHub 相关域名本沙箱已踩到三个：`github.com`、`api.github.com`、`release-assets`）

**修复**：`/etc/hosts` 写入 `release-assets.githubusercontent.com` 与
`objects.githubusercontent.com` → `185.199.108~111.133`
（真实 IP 用阿里 DoH 查得）。详见 `.ai/conventions/8.7-环境.md`。

---

### 103. 🔴 手改大文件时「整块复制」→ CI 报 `Conflicting overloads`，七道本地门禁全绿（2026-09-16）

**现象**：诉求 C（一个指纹解锁所有库）推送 `a164829` 后，CI 在 **`Build Debug APK`** 红：

```
e: domain/src/main/java/io/vaultix/domain/VaultRepository.kt:390:13 Conflicting overloads
e: domain/src/main/java/io/vaultix/domain/VaultRepository.kt:335:13 Conflicting overloads
```

**根因**：我手改这个 700 行 domain 文件时，**一整块 `KDoc + suspend fun enrollPinForVaults(...)`
声明被复制了一份**（第 341–394 行）。两份**逐字节相同**，Kotlin 重载只看类型 ⇒ 直接报冲突。

**为什么七道门禁一道都没拦住**（这是本条最重要的部分）：

| 门禁 | 为什么漏 |
|---|---|
| detekt（含两级探针） | **不做类型检查** —— 重复声明不是它任何规则的输入 |
| `check_signature_types` | 只看**类型名能否解析**；两份签名**都合法** |
| `check_import_packages` | 只看 import 的包路径 |
| `check_experimental_optin` | 只看 `@OptIn` 作用域 |
| `check-encoding` | 只看字节 / 编码 |
| `check_compile_smells`（当时） | 只查图标导入 / 顶层常量顺序 / `R.string` 悬空引用 |
| 自测用例 | 当时还不存在 |

⇒ 这是与 #101（import 包路径）同一类的**「本地无编译器」盲区**。

**为什么会发生「整块复制」**：`VaultRepositoryImpl` 有函数数顶格约束
（detekt `TooManyFunctions`，**40 个**），逼我"加一个必须删一个"，
于是频繁在大文件里做**块级搬运** ⇒ **大文件 + 块级搬运 = 复制粘贴事故高发区。**

**解法（两条）**：

1. **当场规矩**：往大文件里贴块之后，**立刻 `grep -c` 数一遍该声明出现几次**。
2. **新增第 4 类本地门禁** `check_duplicate_declarations`（接在 `check_compile_smells.py` 内，
   与图标导入 / 顶层常量顺序 / `R.string` 并列）。判据：类成员级（4 空格缩进）的 `fun`，
   **同名 + 同接收者 + 同参数类型** ⇒ 报（Kotlin 重载只看类型，不看参数名）。

⚠️ 刻意**只查 `fun`、不查 `val`/`var`**：属性重复在纯文本上无法与
「局部变量 / 命名参数 / 构造实参」区分 —— 实测 **264 处全误报**。
而真实事故是函数整块被复制，查 `fun` 足够。

---

### 104. 🔴 写启发式探针必须配正反用例 —— 新探针连坏两版，每版都「看起来很对」

**背景**：#103 的补救工具 `check_duplicate_declarations` 的正则**连坏两版**：

| 版本 | 结果 | 怎么坏的 |
|---|---|---|
| 第 1 版 | **264 处误报** | `^[ \t]+...(fun\|val\|var)` 把局部变量、命名参数、构造实参全抓了 |
| 第 2 版 | **5 处误报** | 收紧成类成员级 `fun`，但 `fun\s+(\w+)` 对**扩展函数**抓到的是**接收者类型**：`fun JsonElement?.asObject()` ⇒ 名字被认成 `JsonElement`，于是同一接收者上的多个扩展函数归一成同一个 key |
| 第 3 版 | ✅ | 正则跳过可选接收者，并**把接收者纳入 key** |

**第 2 版误报的定位方法（值得复用）**：

> 那 5 处全落在**本轮根本没改动**、CI 也从没报过的两个文件里
> （`FillAssistJsonParser.kt` 的 `JsonElement`、`CredentialProviderIntentUtils.kt` 的 `Intent`）。
> ⇒ **「探针报了一个我不可能引入的问题」= 探针坏了，不是源码坏了。**
> 先去核对报错文件的真实签名，**别急着改源码**。

**第 3 版的残留坑（靠用例才抓到）**：若只跳过接收者而**不把接收者放进 key**，
则 `fun Intent.foo(x: Int)` 与 `fun Bundle.foo(x: Int)` 会被误判为重复 ——
它们在 Kotlin 里是**两个不同签名**。这个坑光看真实仓库看不出来（库里没这种写法）。

**解法**：永久回归集 `.ai/tools/tests/selftest_duplicate_declarations.py`，
5 个用例：真重复 2 个**必须报**；合法重载 / 同接收者不同扩展函数 / 不同接收者 3 个**不能报**。
**改这个正则前先跑它。**

**一般化教训**：**启发式探针的「OK」只有配上「必须报的负例」才有意义。**
只测正例会写出恒返回"无问题"的探针，那比没有探针更危险 —— 它给的是假安心。

---

### 105. 🔴 沙箱「下不动 APK」是两层问题 —— hosts 只是第一层

**背景**：2026-09-16 收尾时想按 issues #102 的规矩真下载一次 APK 验证，
连续三次失败，而且**每次失败的原因都不一样** —— 很容易误判成同一个问题。

| 次 | 现象 | 真实原因 |
|---|---|---|
| ① | `http=302 size=0` | `release-assets.githubusercontent.com` → `198.18.0.34`，**hosts 记录丢了** |
| ② | 240s 只到 3.4MB | hosts 已修好，**带宽极低 ⇒ 超时截断** |
| ③ | `http=000`，90s 放弃，0 字节 | hosts 正常，**整文件连续传输不可靠** |

**关键诊断（区分第二层和第一层）**：

> 用 **range 请求** `curl -r 0-0` 对同一 URL —— 拿到 **`http=206` + 正确 1 字节**，
> 落点 `185.199.108.133`；但**不带 range 的整体下载反复 `000`**。
> ⇒ **CDN 可达、有数据，问题在「整文件持续传输」这一环节。**

**如果没做这个对比**，看到 `000` 会以为又是 hosts 问题，去反复改 hosts —— **白折腾**。

**有效解法：1MB 分块 + 重试 + 回滚**
```bash
# ① 探总长（range 请求读 Content-Range）
curl -sL --max-time 45 -r 0-0 -D - -o /dev/null "$URL" | grep -i '^content-range'   # → 32329710
# ② 逐块追加，每块 5 次重试；失败时 truncate -s <上一次确认成功的偏移>
curl -sL --max-time 60 -r ${off}-${end} "$URL" >> "$OUT"
# ③ 每块后校验 stat -c%s "$OUT" >= end+1，不满足就回滚重试
```

**已沉淀为可复用脚本**：`.ai/tools/fetch_release_asset.sh <url> <out>`
（三段式：探长 → 分块续传 → `zipfile` 校验，全自动）。

**⚠️ 实测证据（说明重试是必需的，不是过度设计）**：脚本跑通后再单独连测 3 次探长，
结果是 `''` / `''` / `'32329710'` —— **前两次空返回，第三次才拿到**。
⇒ 同一 URL、同一时刻，**失败是常态而非偶发**，任何单次尝试都不足为凭。

**两条必须记住的判断口径**：

1. **`gh release view` 能看到资产 ≠ 能下下来。** 前者只走 API，后者要过 CDN，
   **两者必须分开验证**（issues #102 已记一次，本次是再次验证）。
2. **不许因为 release 页面看得到就写「已验证可下载」。**
   拿不到完整 zip，结论就只能写「release 已发布，沙箱未能完整验证」。

**「完整」的判据**（与 #102 配套）：

> ❌ **别用 `file` 判断 APK 完整性。** 它只看头几个字节 ——
> 本次那个 3.4MB 的截断文件照样被报成 "Android package"。
> ✅ **用 `zipfile` 打开**：能列 `namelist()` 且含 `AndroidManifest.xml` 才算完整。

**一般化教训**：**同一个外部依赖（CDN）上的失败，可能是多层不同的原因叠在一起。**
定位时不要假设"还是上次那个原因"，要用**能区分层次的对比实验**（range vs 整体）
一次把层次分开。否则会在错误的那一层反复修。

---

## 112. 🔴 Windows 版 Python 写文件把 `\n` 变成 `\r\n` ⇒ 行式数据（id 列表）**全部失效**（2026-09-21）

**现象**：用 Windows Python 生成一份"待删缓存 id"清单（每行一个 id），
再用 bash 循环 `gh cache delete` ⇒ **80 次全部失败**，而第一轮我把 stderr 吞了
（`>/dev/null 2>&1`），看起来像"跑完了、删了 79 条"。

**根因**：Windows Python 的 `open(path, 'w')` 默认**文本模式**，会把 `\n` 写成 `\r\n`
⇒ bash `read -r id` 读回时每行**尾部带 `\r`** ⇒ 拼进命令后 id 非法 ⇒ 每次都失败。

**修法**：读侧 `id=$(printf '%s' "$id" | tr -d '\r\n')`；
或写侧用 `open(path, 'w', newline='')` / 二进制模式。

> **可复用纪律**：**跨 Linux 工具与 Windows Python 传"行式数据"时，先剥 `\r`。**
> 同族纪律（已有）：**别吞 stderr** —— 这一轮正因为它，"80 次失败"变成了"看起来成功"，
> 让我多做了一轮无用功。同类陷阱：`python 是 Windows 版、不认 /tmp 与 /d/... 路径`
> （用 Windows 路径或 `cd` 进目录再用相对路径）。

## 115. CI 红的定位方法，与 detekt `MatchingDeclarationName` 的两记坑（2026-09-22）

- **背景**：push `c97449a`（自动填充 P0 修复）后 CI 在 `Run detekt (quality gate)`
  一步失败，日志只给到 `Analysis failed with 1 issues`，不含具体违规。
- **坑一：沙箱取不到 CI 日志**（两层的 ~/.hosts 屏蔽 + 大文件传输失败，老毛病的第三层）
  1. `gh api .../logs` 与 curl 直连 **都失败**（`gh` 输出不到控制台直接原因）。
  2. 根因是 **Azure blob 域名被解析到假 IP**：`productionresultssa3.blob.core.windows.net`
     → `198.18.0.27`（与 github.com 的 `198.18.0.26` 同族）。
      ⇒ **DoH 解析真实 IP 再写 `/etc/hosts`**：`https://dns.alidns.com/resolve?name=<host>&type=A`，
     实测真值 `57.150.86.161`。
  3. ⚠️ **GitHub 的 `/logs` 端点不支持 Range**（返回 415），但 **Azure blob 直链支持**
     （`206`）。故正确顺序是：先拿 API 的 `Location` 重定向 URL（`-w "%{redirect_url}"`），
     **再**对 blob 直链做 **1MB 分块 + 重试**（同 #105 的手法）。
  4. 直链有效期很短（带 `se=` 过期参数，约 20 分钟）—— 拿到就立刻下，别先去干别的。
- **坑二：detekt `MatchingDeclarationName`（2.x 里叫 `Filename`）**
  - **规则**：文件里若**只有一个顶层类 / object 声明**，文件名必须与它同名。
  - 踩法：策略文件叫 `FillTargetResolver.kt`，里面唯一的顶层**类**却是
    `FillTargetNode`（其余是顶层函数与 `const val`，**不计入**）
    ⇒ `The file name 'FillTargetResolver' does not match the name of the single
    top-level declaration 'FillTargetNode'`。
  - **解法**：把那组顶层函数收进 `object FillTargetResolver`。这样既满足文件名匹配，
    又给策略带了命名空间（调用处 `FillTargetResolver.xxx` 比裸函数更好读）。
    ——**不要用 `@Suppress` 绕过**：这是一条能实际改善结构的门禁。
- **可用替代验证手段**（无 Android SDK 时也很实用）：
  detekt **2.0.0-alpha.6 未进 Maven Central**，但**默认规则绝大多数与 1.23.8 同构**。
  用 `detekt-cli-1.23.8-all.jar --build-upon-default-config` 对改动文件单独跑，
  **能复现 CI 那条违规**（实测行号与文案完全一致）。
  ⚠️ 但 1.23.8 **不认本仓库 detekt.yml 的属性名**（2.x 的 `allowedLines` vs 1.x 的
  `threshold`）→ 跑的时候**不要传项目配置**，只能拿默认规则做交叉验证。
- ⇒ **纪律**：本地无法跑完整 Gradle 时，push 前至少做「默认规则 detekt 交叉验证」；
  且 **push 后必须回来盯 CI**，不能假定通过。
