# 设置页信息架构 —— 定稿（2026-09-15）

> **这份文档是「设置页怎么重排 + 文案怎么统一」的定稿。**
> 用户原话：「设置界面还有哪些可以合并成一个大设置的，你安排一下，
> 感觉设置页面很凌乱，表述也很杂」。
>
> **交接状态**：结构已定稿 ✅ / **代码已实施** ✅（2026-09-15 第六十六轮，见 §6.1）
> ⇒ **不要再重做一遍**；有分歧看 §6.1 的「实施偏差」（两处刻意偏离本定稿，已说明理由）。
>
> ⚠️ **未在本机编译 / 未真机验收**：本轮在沙箱内完成（拿不到 Android SDK），
> 只跑了 detekt 全量门禁 + 字符串引用核对。**装机验收清单见 §8.1。**

---

## §0 一句话总结

> **7 组 → 5 组**（消灭单行组、修三处错位）+ **文案三条规则**（名词短语标题 /
> 副标题不指路 / 标点统一）。**只动 `strings.xml` 与组顺序，不碰任何行为。**

---

## §1 现状全量清单（改之前先对照这份表）

分组在 `app/src/main/java/io/vaultix/vaultix/ui/settings/SettingsScreen.kt`，
顺序由 `SettingsScreen` 主函数里的调用顺序决定（`VaultSection` → `SecuritySection`
→ `AppearanceSection` → `DataSection` → `AutofillSection` → `OthersSection`）。

| # | 组（group_*） | 行（title key） | 副标题（desc key） | 右侧值 |
|---|---|---|---|---|
| 1 | **密码库** `group_vaults` | `settings_active_vault` 当前密码库 | 值即库名；无库时 `settings_active_vault_none` | 库名 |
| | | `vault_add_fab` 添加密码库 | `settings_add_vault_desc` | → |
| 2 | **安全** `group_security` | `setting_auto_lock` 自动锁定 | 档位文案 | 档位 |
| | | `setting_clipboard_clear` 复制后清除剪贴板 | 档位文案 | 时长 |
| | | `setting_screen_security` 防截屏 | `setting_screen_security_desc` | Switch |
| | | `setting_exit_database` 退出数据库 | `setting_exit_database_desc` | ⚠️ 破坏性 |
| | | `settings_quick_unlock` 生物识别快速解锁 | `settings_quick_unlock_desc` | → 对话框 |
| 3 | **外观** `group_appearance` | `setting_theme_mode` 主题模式 | 当前模式 | 模式名 |
| | | `setting_oled_pure_black` OLED 纯黑 | `setting_oled_pure_black_desc` | Switch |
| | | `setting_dynamic_color` 动态取色 | `setting_dynamic_color_desc` | Switch |
| | | `items_display_options` 显示选项 | `setting_display_options_desc` | → 弹层 |
| 4 | **数据管理** `group_data` | `setting_trash_auto_delete` 回收站自动清理 | 档位文案 | 天数 |
| | | `import_export_entry` 导入 / 导出 | `import_export_entry_desc` | → 二级页 |
| 5 | **自动填充** `group_autofill` | `autofill_settings_entry` 自动填充设置 | `autofill_settings_entry_desc` | → 二级页 |
| 6 | **其他** `group_others` | `permission_management_title` 权限管理 | `permission_management_subtitle` | → 系统页 |
| 7 | **关于** `group_about` | `about_version` 版本 | 版本号 | 值 |
| | | `about_source` 源码与反馈 | GitHub 地址 | → 浏览器 |
| | | `about_license` 开源许可 | — | 对话框 |

**合计 7 组 / 14 行。**

---

## §2 三处病因（**不是"组太多"**，别开错药方）

### ① 错位：破坏性动作混在「安全」里

`setting_exit_database`（清空本地缓存 + 锁定）是**唯一不可逆**的动作，
却夹在「防截屏」与「快速解锁」之间 —— 语义上也**不是安全设置，而是数据动作**。
⇒ 移到「数据」，并**置底**（M3 的建议：破坏性动作不挨着常用动作）。

### ② 单行成组

`group_others`（权限管理）**只有 1 行**，却独占一个组标题 —— 这是"凌乱"最直接的来源。
⇒ 并入「关于」（它本来就是"关于这个 App"的元信息）。

### ③ 语义重叠

- 「安全」里其实混了**三类**：解锁方式（快速解锁）、锁定策略（自动锁定）、隐私（防截屏 / 剪贴板）；
- 「显示选项」（**条目列表**的分组 / 密度 / 图标）挂在「外观」里，与主题 / 取色不是一类；
- 「自动填充」自成一组但只有 1 行（二级页入口）。

⇒ 合并为「**解锁与隐私**」「**显示与填充**」两组，语义各自收拢。

---

## §3 目标结构（5 组，每组 ≥2 行）

| 新分组 | 行（按此顺序） | 来源 |
|---|---|---|
| **密码库** | 当前密码库 · 添加密码库 | 原「密码库」 |
| **解锁与隐私** | 快速解锁 · 自动锁定 · 防截屏 · 剪贴板清除 | 原「安全」去掉退出数据库 |
| **显示与填充** | 主题模式 · 动态取色 · 纯黑背景 · 条目显示 · 自动填充设置 | 原「外观」4 项 + 原「自动填充」 |
| **数据** | 导入与导出 · 回收站清理 · **退出数据库**（error 色，置底） | 原「数据管理」+ 从「安全」搬来 |
| **关于** | 权限管理 · 版本 · 源码与反馈 · 开源许可 | 原「其他」+「关于」 |

**组内顺序原则**：入口类（→ 二级页）在前，开关类在中，破坏性动作置底。

---

## §4 文案三条规则（**新写的文案一律照此**）

1. **行标题 = 名词短语**，≤ 6 字，**不带标点**、不混英文缩写
   （例外：`SSH`/`PIN`/`OLED` 这类已是通用缩写，但出现在标题里的应尽量下沉到副标题）。
2. **副标题只说"这条设置做什么 / 何时生效"**；
   ⛔ **禁止指路语**：`点右侧「启用」即可` / `关闭入口见下方列表` / `在……页查看`。
   指路语是"界面没做好"的症状 —— 用户点进去自然看得到。
3. **标点统一**：中文全角括号（`（）`）；斜杠最多 1 个，否则改顿号或"与"；
   冒号只在"值：说明"结构里用。

---

## §5 逐条改动表（**照抄即可**）

### 5.1 行标题

| string key | 现在 | 改为 | 理由 |
|---|---|---|---|
| `settings_quick_unlock` | 生物识别快速解锁 | **快速解锁** | 原标题最长；"生物识别"是实现细节 → 副标题 |
| `setting_clipboard_clear` | 复制后清除剪贴板 | **剪贴板清除** | "复制后"是触发条件，不是名称 |
| `setting_oled_pure_black` | OLED 纯黑 | **纯黑背景** | 中英混排；OLED 下沉副标题 |
| `items_display_options` | 显示选项 | **条目显示** | 与"显示与填充"组名消歧，且点明对象是"条目" |
| `import_export_entry` | 导入 / 导出 | **导入与导出** | 去空格与斜杠 |
| `setting_trash_auto_delete` | 回收站自动清理 | **回收站清理** | 简化（"自动"在副标题里） |
| `permission_management_title` | 权限管理 | 权限管理 | 保持 |

### 5.2 副标题

| string key | 现在 | 改为 | 理由 |
|---|---|---|---|
| `settings_quick_unlock_desc` | 锁定后免主密码/免 2FA 重新打开；**关闭入口见下方列表** | 锁定后用生物识别重新打开，免主密码与两步验证 | 删指路语；`2FA`→中文；斜杠→顿号味 |
| `autofill_settings_entry_desc` | 系统服务 / 通行密钥 / 填充行为 / 验证器 / 保存行为 | 系统服务、通行密钥与填充行为 | 5 个斜杠 → 顿号 |
| `permission_management_subtitle` | 在系统应用信息页查看 / 调整 **Vaultix** 的权限 | 在系统设置页查看与调整应用权限 | 去自指"Vaultix"（页面里自指很绕）；斜杠→与 |
| `setting_exit_database_desc` | 清除本地缓存（条目 / 登录凭据 / 待上传改动）并锁定；库保留在列表中，远程数据不受影响 | 清空本地缓存并锁定；库与远程数据保留 | 太长 + 3 斜杠；两句足矣 |
| `setting_screen_security_desc` | 阻止截屏与最近任务缩略图显示内容 | 阻止截屏与最近任务缩略图 | 收紧 |
| `settings_add_vault_desc` | 连接 Bitwarden 云端，或打开本机 .kdbx 文件 | **添加另一个密码库**（已有一个库时）/ 保留原文（无库时） | 库里已有 Bitwarden 时，"连接 Bitwarden"是错措辞 |

### 5.3 组名

| string key | 现在 | 改为 |
|---|---|---|
| `group_security` | 安全 | **解锁与隐私** |
| `group_appearance` | 外观 | **显示与填充** |
| `group_data` | 数据管理 | **数据** |
| `group_others` | 其他 | **删除**（并入 `group_about`） |
| `group_autofill` | 自动填充 | **删除**（并入"显示与填充"） |
| `group_vaults` / `group_about` | 密码库 / 关于 | 保持 |

---

## §6 实施步骤（照这个顺序做）

1. **只改文案**（`app/src/main/res/values/strings.xml`）：§5.1 + §5.2 + §5.3。
   这一步零行为变化，可以先跑一遍门禁确认全绿，再继续。
2. **改结构**（`SettingsScreen.kt`）：
   - `SecuritySection` 里移除「退出数据库」那一行；
   - `DataSection` 末尾加回「退出数据库」（保留 `error` 色与 `ExitDatabaseDialog`）；
   - `AutofillSection` 的入口行挪进 `AppearanceSection`（或把两个 Section 合成一个
     `DisplaySection`）；`OthersSection` 的权限管理行挪进「关于」段。
   - ⚠️ 这几个 Section 当初是为了**守住 detekt `LongMethod ≤150`** 才拆出来的
     （见文件里各处的注释）—— 合并时**可能再次撞线**，必要时把「条目显示」的宿主
     也拆成一个独立 composable。
3. **门禁**：`detekt` → `:app:compileFullDebugKotlin` → `:app:testFullDebugUnitTest`
   （⚠️ detekt 先于编译，会**掩盖编译错误**，改完必须再真跑一次 compile）。
4. **真机**：`assembleFullDebug` → 发布密钥重签 → `install -r` → 逐页看一遍。

---

## §6.1 实施记录（2026-09-15 第六十六轮）

**实测结构**（脚本按出现顺序抓 `SettingsGroupTitle` → `SettingsRow`）：

| 组 | 实际行序 | 与 §3 对照 |
|---|---|---|
| 密码库 | 当前密码库 · 添加密码库 | ✅ |
| 解锁与隐私 | 快速解锁 · 自动锁定 · 防截屏 · 剪贴板清除 | ✅ |
| 显示与填充 | 主题模式 · 动态取色 · 纯黑背景 · 条目显示 · 自动填充设置 | ✅ |
| 数据 | 导入与导出 · 回收站清理 · **退出数据库** | ✅ |
| 关于 | 权限管理 · 版本 · 源码与反馈 · 开源许可 | ✅ |

**5 组 / 无单行组 / 破坏性动作置底 —— 与定稿完全一致。**

### 实施偏差（两处，**刻意**偏离定稿，理由如下）

1. ⚠️ **`group_autofill` 没有删除** —— 定稿 §5.3 写「删除」，但 grep 发现
   `AutofillSettingsScreen.kt:129` 把它当作**二级页的顶栏标题**在用
   （`title = { Text(stringResource(R.string.group_autofill)) }`）。
   删掉会让二级页编译失败 ⇒ **保留该 string**，只从设置首页移除对它的引用。
   （`group_others` 确实只此一处用，已删。）
2. ⚠️ **`settings_add_vault_desc` 的条件分支，新增的文案比定稿多带了类型说明** ——
   定稿写「添加另一个密码库」，实际落地为
   **「添加另一个密码库：Bitwarden 云端或本机 .kdbx 文件」**（新 key
   `settings_add_vault_desc_another`）。
   理由：定稿要消掉的是「连接 Bitwarden」这个**误导性措辞**（暗示要改当前库），
   而"能加哪两种库"是**有用信息**、不是指路语；只写"另一个密码库"会丢掉它。
   判据仍是「是否已有库」（`switchable.isEmpty()`）。

**顺带修掉的两条**（定稿 §5.2 表格里没列，但 §8 验收标准覆盖到了）：
- `quick_unlock_disabled`：「未启用（点右侧「启用」即可）」→ **「未启用」**
  （括号内纯指路语；另两条 `*_kdbx` / `*_device_unsupported` 的括号是**有用信息**，保留）。
- ⚠️ **未修**：`detail_totp_hidden` =「点右侧眼睛显示验证码」也是指路语，
  但它在 `ItemDetailScreen`（详情页），**不在本轮"设置页"范围** ⇒ 留给下一轮，见 §7。

### 工程侧

- 为守 detekt `LongMethod ≤150`，把顶栏抽成了 `BoxScope.SettingsTopBar`
  （必须是 `BoxScope` 扩展：`Modifier.align(TopCenter)` 只在 Box 作用域内可用）。
- ⚠️ 期间踩到：改到 **151 行**超了 1 行，而**精简注释救不回来** ——
  实测 detekt 的 `LongMethod` **不计注释行**（见 `conventions/8.6-工程质量.md` 的勘误块）。

## §7 边界（**不要做**）

- ❌ 不新增任何设置项、不改变任何开关的默认值或行为 —— 本轮是**信息架构**，不是功能。
- ❌ 不动 `AutofillSettingsScreen`（二级页）**内部**的分组（那是另一套 IA，等这一轮验收后再单独评估）。
- ❌ 不给「退出数据库」改语义（它仍然是"清本地缓存 + 锁定"，不是"删除库"）。
- ❌ 不做自动化测试 —— 本页没有 ViewModel 逻辑变化，靠真机目视 + 现有单测兜底。

---

## §8 验收（用户视角）

1. 设置页从上到下读一遍，**组标题只有 5 个**，没有单行组；
2. 破坏性动作「退出数据库」在「数据」组**最底部**、红字；
3. 逐条读文案：**没有"点右侧…/见下方…"这类指路语**，标题都是名词短语。
4. 快速解锁对话框（`QuickUnlockManageDialog`）行为不变（那是 2026-09-14 已修的排版）。

## §8.1 装机验收清单

> ✅ **门禁已由 CI 完成**（2026-09-15 run `34874282413` **success**）：
> detekt ✓ / 编码检查 ✓ / **Build Debug APK ✓** / 单测 ✓，签名 APK 已发布到
> preview Release（证书一致 ⇒ **覆盖安装不丢数据**，可直接装）。
> ⇒ **只需做下面的真机目视**。

1. ~~门禁~~ ✅ 已完成（见上）。
2. **真机逐页目视**（改动面 = 设置页 + **全部 4 个搜索页** + 全 App 间距）：
   - 设置页：5 个组标题；「退出数据库」在**数据组最底部**、红字；无指路语
   - 四个搜索页各进一次搜索态（`ItemsScreen` / `CardWalletScreen` / `PasskeysScreen` /
     `TotpCodesScreen`）：搜索框应变成**胶囊填充容器 + 左侧放大镜**，
     且**搜索态的沉浸/让位不能退化**（对比改前；`ISSUES.md` #67 / #76 就在这一带）
   - 间距：**版式应当与改前完全一致**（已用反演法证明等价）。
     若发现某处观感变了 ⇒ 说明有非标尺值被误替换，**先跑反演脚本定位**（见 `8.8` §4 P0-1）
   - 已有一个库时，设置页「添加密码库」的副标题应为「添加另一个密码库：…」；
     无库时为原文「连接 Bitwarden 云端，或打开本机 .kdbx 文件」

---

## §9 相关文件

| 文件 | 关系 |
|---|---|
| `app/src/main/res/values/strings.xml` | 本轮的**主要改动物** |
| `.../ui/settings/SettingsScreen.kt` | 组顺序与 Section 划分 |
| `.../ui/settings/SettingsComponents.kt` | `SettingsGroupTitle` / `SettingsRow` 的规格（改布局时看这里） |
| `.../ui/settings/AutofillSettingsScreen.kt` | 二级页（本轮**不动**） |
| `.ai/issues/06-界面与交互.md` | #84「三种空」等界面纪律 |
| `.ai/decisions/库选择与快速解锁-逻辑定稿.md` | 同批"逻辑定稿"的写法可参照 |

---

## §10 第二轮修订（2026-09-15 · 密码库管理合并）

> 用户原话：「设置页面的密码库，添加密码库，快速解锁设置，这几个好像都属于密码库管理的内容，
> 我在想这些能不能放到一起管理。然后这些界面的ui，按钮，设置的逻辑是否需要重新优化，
> 你按照material3最新的设计理念，来优化这些内容。可以吗」
>
> ⚠️ **这一节是「修订」不是「推翻」**：§2 的三处病因（破坏性动作错位 / 单行成组 /
> 语义重叠）与 §4 的文案三规则**继续有效**，§7 的边界除本处显式说明者外也都继续有效。

### 10.1 改了什么

| 项 | §3 原定稿 | §10 修订后 |
|---|---|---|
| 「密码库」组 | 当前密码库 · 添加密码库（2 行） | **只留 1 行**「密码库管理」→ 二级页 |
| 「快速解锁」 | 在「**解锁与隐私**」组 | 移到**密码库管理二级页** |
| 「解锁与隐私」组 | 快速解锁 · 自动锁定 · 防截屏 · 剪贴板清除 | 自动锁定 · 防截屏 · 剪贴板清除（3 行） |

新增文件 `app/src/main/java/io/vaultix/vaultix/ui/settings/VaultManagementScreen.kt`，
新增路由 `VaultManagementRoute`。

### 10.2 为什么这不算推翻 §2③

§2③ 的结论是「"安全"里混了三类东西」，于是分出「解锁与隐私」——
**这个诊断依然对**。本轮只是发现「快速解锁」被分错了边：它是**按库**登记的
（`SettingsViewModel.quickUnlockVaults` 的形态就是"每库一行"），
属于「库怎么打开」，不属于「隐私」。

判据（可复用）：**一条设置项如果它的主语是"某个库"而不是"这个 App"，它就属于库管理。**

### 10.3 为什么二级页而不是"在首页摊开"

首页的职责是**分流**，不是干活。三件事摊在首页会：
1. 让「密码库」组变成 5~6 行（选库 / 加库 / 每个库的指纹与 PIN…）—— 与 §2② 要消灭的
   "体量失衡"是同一类问题；
2. 把 `ActiveVaultDialog` / `QuickUnlockManageDialog` 及其连带的 KDBX 待验证 id、
   PIN 两步流程等一串状态重新压回 `SettingsScreen` ⇒ 必然顶破 detekt `LongMethod ≤150`
   （这正是当初把它们拆出去的原因）。

⇒ 与 `AutofillSettingsScreen` / `ImportExportScreen` 保持同一层级：**首页一行 → 二级页干活**。

### 10.4 §7 边界的更新

- §7「❌ 不新增任何设置项、不改变任何开关的默认值或行为」—— **仍然有效**。
  本轮**没有**新增 / 删除任何设置项，也没有改任何默认值；只是把三个已有入口
  从两个组挪到同一页，并**保留全部原有对话框与分支逻辑**（含未解锁库 → 直连解锁页）。
- §7「❌ 不动 `AutofillSettingsScreen` 内部」—— **仍然有效**。
- ⚠️ **新增边界**：`VaultManagementScreen` 与 `SettingsScreen` **共用同一批对话框
  composable**（`ActiveVaultDialog` / `QuickUnlockManageDialog` / `PinDialogHost` /
  `KdbxQuickUnlockPasswordDialog` 已从 `private` 提升为 `internal`），
  **不要**在任一侧复制一份出来改——两份必然漂移。

### 10.5 装机验收（在 §8.1 基础上补）

1. 设置首页从上到下：**5 个组**，其中「密码库」组**只有 1 行**「密码库管理」，
   副标题显示当前库名；
2. 点进「密码库管理」：3 行（当前密码库 / 添加密码库 / 快速解锁），
   点「当前密码库」的选库对话框行为与改前一致：
   - 点**已解锁**的库 → 直接切过去；
   - 点**未解锁**的库 → **去解锁页**（不能切过去，否则空白页 bug 复发）；
   - 「设为默认」→ 只改冷启动目标，不要求当下解锁；
3. 「快速解锁」点开的对话框与改前**完全一致**（每库两行：指纹 / 应用内 PIN，
   KDBX 勾选后先要主密码）；
4. 「解锁与隐私」组里**不再有**「快速解锁」，只剩自动锁定 / 防截屏 / 剪贴板清除。

---

## §11 第三轮修订（2026-09-17 · 密码库管理：按库卡片 + 三动作分级）

> **触发**：用户原话「退出密码库的设置应该再优化一下，能不能在设置里面的每一个密码库上增加退出的按钮…
> 管理页参考 bastion…分为两类 bitwarden 和 kdbx，每一个库相当于一个卡片，卡片上有同步、备份、退出」。
>
> ⚠️ **本轮的直接起因是一次真实事故**：用户测试 OneDrive 时误点「退出数据库」，
> 它清掉了**所有库**的本地缓存 + token，界面只显示「还没有保存的密码」⇒ **用户以为数据丢了**。
> （数据其实在服务端，重新登录即回；但**界面上无法推断这一点** —— 详见 `../README.md` 里的
> 「要么改状态、要么留日志」约定。）

### 11.1 改了什么（相对 §10 的二级页）

1. 「当前密码库」单行 → **按两类分组的库卡片**：`Bitwarden` / `KDBX`（用户拍板的两类）。
2. ★ **「锁定 / 退出 / 移除」下沉到每个库卡片**，并**分级**：

   | 动作 | 后果 | 界面表达 |
   |---|---|---|
   | 锁定 | 丢弃内存密钥，秒解 | 普通按钮 |
   | 退出 | 清**该库**本地缓存 + token（需重新登录），**远程不动** | 二次确认，文案写明"需重新登录" |
   | 移除库 | 只删本地那一行，**远程不动** | 收进 ⋮，红色 + 确认 |

3. ★ **全局「退出数据库」移除**（建议）—— 它正是本轮事故的入口：**范围不可见**。
   「清本地缓存」必须**逐个库做**，让范围与后果都看得见。
4. ★ **新建「网盘账号」区**（账号级，与库分开）：NAS(WebDAV) 凭据 / OneDrive 登录态 + 换号 / 注销 / 自检。
   理由：**凭据是账号级的、且被多个库共用**（同一 NAS 的 `credentialId`；OneDrive 同账号下的多个库）
   ⇒ 挂到单库卡片上会**连带废掉同账号的其他库，而用户看不见**。
5. ★ **按钮按能力显示**（本地 KDBX 与网盘 KDBX 的能力矩阵不同）：

   | 能力 | 本地 KDBX（`content://`） | 网盘 KDBX（`webdav:`/`onedrive:`） |
   |---|---|---|
   | 同步 | ❌ **无此概念** | ✅ |
   | 备份 | ✅ | ✅（云端历史 **≤3 份**） |
   | 离线 | ✅ | ❌ 需网络 |

   ⇒ 本地 KDBX 卡片**不显示「同步」**（否则用户以为它有云端副本）。
   判据用现成的 `origin` 前缀；⚠️ **不要新加 `sourceType` 列**（方案 §2：一加就有两处真相）。

### 11.2 ★ KDBX 卡片**不要「退出」**（与 11.1.5 同源）

对 KDBX，「退出」与「锁定」**同义**：条目**不在 Room**（在内存会话）、无 token、无待推送队列
⇒ 没有"本地缓存"可清；不解锁本来就读不到、条目也不落盘。
放一个语义重复的按钮只会让用户更分不清"哪个安全" —— 而这正是事故的病根。

### 11.3 与 §2 / §7 边界的关系（**没有被推翻**）

- §7「不要在首页摊开」**继续有效**：本轮改的是**二级页内部**的结构，不是把内容搬回首页。
- §2③「消灭单行组」**继续有效**：库卡片是"每库一张卡"，不是"每库一行设置项"。
- §4 文案三规则**继续有效**，本轮**新增一条硬约束**：
  ★ **破坏性动作的文案必须写清"对远程有没有影响"**（今天的事故就是范围不可见造成的）。

### 11.4 备份（≤3 份）与「导出 ≠ 备份」

- 备份**最多保留 3 份**（用户拍板），超出自动删最旧。
- ⚠️ **导出 ≠ 备份**：导出是**一次性格式转换**（一个文件，通常立刻拿走）；
  备份是**时间序列**（带时间戳、可列出/还原/删）。
  把"导出到网盘"当成备份 ⇒ 用户以为有历史版本、而实际只有一个最新文件 = **"看起来有保障"型危险**。

### 11.5 装机验收（在 §8.1 / §10.5 基础上补）

- [ ] 每个库卡片上只有它**该有**的按钮（**本地 KDBX 没有「同步」**）
- [ ] 「退出」只影响**那一个库**（另一个库的条目仍在），且文案写明"需重新登录"
- [ ] 「移除库」之后远程数据仍在（用另一设备 / 网页端确认）
- [ ] 「网盘账号」区能注销账号；注销后同账号的**多个**库都表现为"需重新连接"（而不是静默失败）
- [ ] 全局「退出数据库」已移除

### 11.6 实施记录

⏳ **未开工**（本节先落设计；施工从本节开始，顺序：先回填本节 → 再改页面）。

### 11.7 施工顺序与「添加库」入口的定位（2026-09-17 20:08 用户确认）

> **用户明确指令**：「先把 onedrive 和 webdav 这些能联网的功能独立出来，**不在添加库里面放着**」。

**为什么这条是病因级的（机制）**：

| | 生命周期 |
|---|---|
| OneDrive 登录态 / WebDAV 凭据 | **长**（跨进程跨页面，活在 MSAL 缓存 / `SecureCredentialStore`） |
| 「添加密码库」这一页 | **短** —— 导航路由，**一返回 ViewModel 即销毁** |

⇒ **把长寿命状态存在短寿命页面里，必然"返回就没了"。**
用户实测：「根本不用划掉后台，返回到添加密码库界面，登录状态就没了」。
⚠️ 当时的 `restoreOneDriveSessionIfAny()`（进页面时向 MSAL 要缓存账户）是**症状补丁**，
**不是修复** —— 可留作安全网，但不得据此认为问题已解决。

**施工顺序（★ 本节的顺序就是下一轮的执行顺序）**：

1. **网盘账号独立**（先做这个 —— 它直接解今天所有 OneDrive/WebDAV 现象的根）
   - 设置里新增二级页**「网盘账号」**（入口与「密码库管理」并列）
   - 把 `AddCloudVaultScreen` 里的**账号配置**整段搬过去：
     WebDAV（服务器地址 / 账号 / 密码 / 连接自检）+ OneDrive（登录 / 换号 / 注销 / 当前账户）
   - 落点建议：复用现有 `AddCloudVaultViewModel` 的 `connectWebDav` / `connectOneDrive` /
     `switchOneDriveAccount` / `signOutOneDrive` / `validateWebDav`，**只把 UI 换宿主**；
     若要拆 ViewModel，注意 `WebDavCredentialStore` 的写入必须仍只有一处入口。
2. **「添加密码库 → 从网盘添加」保留，但降级为"选文件"**
   - ✅ **保留入口**（用户确认）—— 从"已配置的账号"里浏览并挑一个 `.kdbx`
   - ❌ **不再承担**登录 / 凭据填写 / 连接自检
   - 无任何网盘账号时：入口**引导去设置里配账号**（而不是原地摊开一个登录表单）
3. **密码库管理页卡片化 + 三动作分级**（§11.1 / §11.2）

**验收（这一步）**：
- [ ] 在「添加密码库」里进进出出、切换 Tab，**OneDrive 登录态不再"返回就没"**
- [ ] 账号只在设置里配；添加页**不出现**服务器地址 / 密码框 / 登录按钮
- [ ] 冷启动后进「网盘账号」，**已登录的账户直接显示**（不再是"未登录"）
- [ ] 没有网盘账号时，添加页给出"去设置里配置"的引导，而不是一个空表单

### 11.8 实施记录（2026-09-17 深夜）

**已做（全部入库，逐步编译通过）**：

| 提交 | 内容 |
|---|---|
| `bdd7a72` | `CloudAccountInventory` —— 账号清单**从库的 origin 反推**（不另存一份）；并把 accountId 的"形状容错"规则收口成 `matchesStoredAccountId`，由 `getAccount` 与清单**共用** |
| `eb0799f` | `CloudAccountsScreen` + `CloudAccountsViewModel`（已连接状态**每次进页面重读**；空态**分两态**） |
| `6e035ac` | 接线：`CloudAccountsRoute` + 「密码库管理」组内入口 |

**★ 实施偏差（一处，已按定稿自身规则纠正）**：
§11.7 原文写「入口与**密码库管理并列**」（即设置首页新开一组的同级入口）。
实现时改为**收在「密码库管理」组内** —— 因为首页新开一个单行组会**违反 §2③「消灭单行组」**。
语义上也更顺：它就是"库的来源怎么连"。**本节即对该偏差的回填**（同 §10.2 的做法）。

**行业侧踩到的三个坑（都付了时间，记下避免重走）**：
1. `app` 模块**没有** `core:database` 的编译类路径 ⇒ 注入 `VaultDao` 时 KSP 报
   `could not be resolved`。**不要为此加一层依赖** —— 改用 domain 的
   `VaultRepository.observeVaults()`（`VaultSummary` 已带 `origin`）。
2. `CloudAccountsViewModel` 与 Screen 同文件 ⇒ detekt `MatchingDeclarationName`
   （一个文件只放一个顶层类，名字须与文件名一致）⇒ 拆两个文件。
3. 状态行的"状态点"原本用 `Icons.Filled.Folder` —— 图标语义不对，改纯文字。

**下一步（§11.7 第 2 步，**尚未开工**）—— 注意它是一处流程重设计，不是删几行 UI**：

「添加密码库 → 从网盘添加」要去掉登录/凭据，只留"选文件"。缺的不是 UI 删减，而是
**"账号从哪来"这条数据流**：

```
现在的流程：填服务器/账号/密码（或点登录） → 自检 → 列目录 → 选文件 → 输主密码 → 建库
目标流程：  选一个**已配置账号**（来自 CloudAccountInventory） → 列目录 → 选文件 → 输主密码 → 建库
```
⇒ 需要在 `AddCloudVaultViewModel` 里加：注入 `CloudAccountInventory`、把账号作为**选择项**而非输入，
并让 `onPickConfiguredAccount(account)` 直接构造来源去列目录（WebDAV 按 `credentialId` 取凭据；
OneDrive 走已缓存账户）。⚠️ 没有账号时给"去设置里配置"的引导，**不要原地摊开登录表单**。

**验收（做完第 2 步才成立）**：
- [ ] 在「添加密码库」里进进出出、切 Tab，OneDrive 登录态**不再"返回就没"**
- [ ] 账号只在「网盘账号」里配；添加页**不出现**服务器地址 / 密码框 / 登录按钮
- [ ] 冷启动后进「网盘账号」，已登录的账户**直接显示**

### 11.9 ★ 真机验收结果（2026-09-17 21:15）—— 那个 bug 已闭环

**用户实测反馈（原话）**：
> 「已经显示了 onedrive 的密码库了。**而且清掉后台也没丢**」

对照 §11.7 的三条验收：

- [x] 在「添加密码库」里进进出出、切 Tab，OneDrive 登录态**不再"返回就没"** → ✅ 实测通过
- [x] **冷启动 / 划掉后台**后进「网盘账号」，已登录的账户**直接显示** → ✅ 实测通过（"清掉后台也没丢"）
- [~] 「账号只在设置里配；添加页不出现服务器地址 / 密码框 / 登录按钮」→ **部分**：
  已配过账号时添加页只列账号 ✅；但**还没账号时仍会落到原表单**（见下）

**结论：今天最初那个 bug（"返回到添加密码库界面，登录状态就没了"）已修复并真机验证。**
根因不是 Doze、不是电池优化、不是 MSAL 缓存丢失（那三项都被实证排除了），而是
**把长寿命状态（登录态）存在了短寿命页面（导航路由）里** —— 换宿主后自然消失。

**仍待做（最后一点，很小）**：把添加页在"没有账号"时的那段**原表单整段摘掉**
（服务器地址 / 账号 / 密码 / 「连接并浏览」/「使用 Microsoft 账号登录」），
改成就地给"去设置里配置"的引导。当前实现是**插入式**（有账号走新路径并 early-return），
为的是每步都能独立编译通过、也不制造 `UnusedPrivateMember`。

**过程中被实证排除的假设（省得下次重走）**：
- ❌ 「电池优化未豁免导致静默刷新被拒」—— 用户已设豁免仍复现；
- ❌ 「MSAL 缓存丢了」—— `account_credential_cache.xml` 里 refresh token 始终在，mtime 还随刷新变化；
- ❌ 「后台被系统回收」—— **不用划后台，仅仅返回上一页就已经复现**（这一句直接把范围缩到"页面生命周期"）。

### 11.10 修订：库**不用卡片**，用「行 + trailing ⋮」（2026-09-17 21:21 用户提出、我同意）

**用户提问**：「设置界面的密码库管理界面的，库的卡片形式展示有实现的必要吗」。

★ **这是个好问题，而且它推翻了我自己前面的设计（§11.1）** —— §11.1 的"卡片"是照
用户最初的说法（"每个库相当于一个卡片一样"）定下的，**但当时没有回头验证它是否真需要**。

**回到病因**：
| | |
|---|---|
| 真病根 | 「退出数据库」是**全局动作**、**范围不可见** ⇒ 误点后以为数据丢了 |
| 修它需要什么 | **每个库有自己的动作** —— 只此一条 |
| 卡片多给什么 | 只多一件事：让**多个动作常驻可见**（同步/备份/退出 全摊在卡面上） |

⇒ **决定：**
1. 库用 **「行 + 右侧 ⋮」**（⋮ 里放 锁定 / 同步 / 退出 / 移除）；
2. **状态放副标题**（已锁定 / 已连接 / 同步失败）—— 那才是真正要"看得见"的信息；
3. **不为卡片而卡片**：若将来某个库确实需要 3–4 个常驻动作，再升级成卡片 ——
   那时卡片是**被需要的**，不是默认。另：卡片会让列表迅速变长（5 个库 = 5 张大卡），
   与设置页其余"紧凑行"的形态也**不一致**。

⚠️ 本节是对 §11.1 的**修订**（同 §11.3 的写法：说明"谁没有被推翻"，此处 §11.1 的
**动作分级**继续有效，只换承载形式）。

### 11.11 修订：「解锁方式」合并精简（用户："逻辑很麻烦、操作很复杂"）

**先把"复杂"拆成四条，区分"可优化"与"硬约束"**：

| # | 现状 | 性质 |
|---|---|---|
| 1 | **逐库一行**，要逐个点开 | 可优化 |
| 2 | **两步对话框**（先选库 → 再确认） | 可优化 |
| 3 | ★「一个 PIN 打开多个库」**必须一次配齐** | ⚠️ **硬约束**（PIN 只在输入那一刻存在，包裹只能同流程完成） |
| 4 | 指纹与 PIN 是**两条独立路径**，用户得先搞清"我在配哪种" | 可优化 |

**目标形态（按我的方案，用户已同意"按你的意思改"）**：
```
设置 → 密码库管理 → 「解锁方式」
  ┌───────────────────────────────────┐
  │ 快速解锁（指纹）         [开关]    │
  │ ● 3 个库已启用                     │  ← 一行汇总，不再逐库列行
  │ 应用内 PIN                [开关]   │
  │ [管理解锁方式]                     │
  └───────────────────────────────────┘
                ↓ 一次流程
  ① 列出所有库（★**默认全勾**）→ ② 需要主密码的就地填 → ③ 一次认证/PIN → 全部完成
```
1. ★ **默认全勾** —— 用户 99% 想要"所有库都能快速解锁"，"逐个勾选"是纯负担（**最大的省事点**）；
2. ★ **硬约束要如实告知**：PIN 场景必须**一次做完** ⇒ UI **不能**让用户以为"可以事后再给某个库补 PIN"
   （那会得到一个**永远解不开的信封** —— 与今天那类"看起来配好了、实际打不开"同族）；
3. ★ **指纹与 PIN 合成一个「解锁方式」入口**：它们是同一问题的两种答案，
   不该让用户先去理解二者关系。

### 11.12 新需求（待定，未开工）：新建 SSH 条目时「自己生成公钥/私钥」

**用户原话**：「新建 SSH 的时候，能不能增加一个自己生成公钥和私钥的按钮呀，方便我添加和管理」。

**⚠️ 三个必须先定的点（成本差别很大）**：
1. **算法**：Ed25519（现代、短、KeePassXC 默认）vs RSA（老服务器兼容）。建议 **Ed25519 优先、RSA 可选**。
2. ★★ **私钥导出格式 —— 这才是工作量所在**：
   - 「**生成密钥对**」本身很便宜（Android `KeyPairGenerator` / API 33+ 的 `Ed25519`）；
   - 但 **OpenSSH 新格式私钥**（`-----BEGIN OPENSSH PRIVATE KEY-----`）**不是标准 PKCS#8**，
     它需要 bcrypt-kdf + chacha20-poly1305 自己封装 ⇒ **是一块独立实现**；
   - 若只需「PKCS#8 PEM」，则几乎零成本，但**很多 SSH 客户端与老工具不认**。
   ⇒ **必须先定"导出成哪种格式"**，否则会做出一个"生成了但用不上"的按钮。
3. **存哪 / 与 KeePassXC 对齐**：KeePassXC 把 SSH 密钥放在条目的自定义字段里
   （`publicKey` / `privateKey`）。**字段名必须逐字对齐**，否则在 KeePassXC 里看不到。
   ⚠️ 私钥字段必须是**受保护（Hidden）**类型。

**安全提示（必须做，不是可选）**：生成的私钥**不可再生** ⇒ 生成后要明确提示
"立即备份 / 立刻用一次"；这一点与今天"passkey 私钥是唯一不可再生的东西"是同一类。

### 11.13 实施记录（2026-09-17 深夜，第 2 批）—— §11.11「解锁方式」合并精简已落地

本节与代码在**同一个提交**里（提交信息：`设置页 §1：解锁方式合并精简`）。

**已做**：

- 「解锁方式」**内联**进「密码库管理」页：两个开关 + **一行汇总**（「已对 N 个库生效」）
  + 「管理解锁方式」入口（`QuickUnlockSettingsRows`）。删掉了原来的设置对话框
  （`QuickUnlockSettingsDialog`）与那张**逐库勾选**的范围表 —— 定稿 §11.11 图里的形态。
- 新增**配置向导** `Dialog.Configure`：一次问清「对哪些库 + 用哪些方式」，之后是
  `主密码 →（PIN 当场落盘）→（指纹备料 + 一次认证）→ 结果`。
- **默认全勾**：靠新增的 `isQuickUnlockScopeConfirmed()` 实现（见 §11.13.1）。
- 结果页把两种方式的结论**合并**成一份（`buildReport`）。

#### 11.13.1 ★ 一处设计修正：「默认全勾」不能用"范围为空"来表达

§11.11 要求"默认全勾"，但 `quickUnlockScope()` 的空集**已经有确定含义** ——「一个库都不要」。
一个值背两种含义的后果是：用户主动全部取消勾选之后，界面会反过来告诉他"全都勾上了"。
这正是「空有三态」那条纪律要防的塌缩（同族：读路径解密失败被渲染成空列表）。

⇒ 拆成两个事实，新增独立标记 `quick_unlock_scope_confirmed`：

- `false`（从未配置）⇒ **所有库都在范围内**，向导默认全勾；
- `true` ⇒ 范围就是存储值，空集如实表示"一个都不要"。

这个拆法顺带修掉一个新库的坑：新加的库在"未确认"阶段**天然就在范围内**，
不必等用户回来手动勾一次。

#### 11.13.2 ★ 为什么"两法一次配完"不是修饰性的合并

KDBX 的主密码**两种方式都要用**（代码里的两处 `passwordFor`）：

```
enrollment.prepareForVaults(targets) { id -> passwordFor(id) }             // 指纹要
vaultRepository.enrollPinForVaults(targets, pin) { id -> passwordFor(id) } // PIN 也要
```

⇒ 分开跑 = **每个 KDBX 库的主密码要输两遍**。把两者合进一次流程（主密码只收一次）
才是真正砍掉那个重复次数的做法；只把"选库"那一步缩短，重复次数一点都不会少。

#### 11.13.3 顺手修掉的两处"沉默 / 假状态"（今天反复出现的同族病根）

1. **旧 `Notice` 通道**根本没人消费 —— `notices` / `noticeShown()` 在整个 `app/src/main` 里
   **零引用**，「库被移出生效范围」那条提示**从来没有显示过**。⇒ 删掉该通道，
   把"取消勾选会清掉该库凭据"改成**动手之前**写在向导里
   （事后的提示追不回已经删掉的东西）。
2. **指纹被取消、但 PIN 段已落盘却不报** —— PIN 段先执行；若用户随后取消指纹验证，
   旧逻辑直接回设置界面，用户以为"什么都没配成"，实际 PIN 已经可用
   （与"看起来没配好、实际能打开"同族）。⇒ 现在只要 PIN 段有结论就照样出结果页，
   如实写「已取消指纹验证，指纹未启用」。

#### 11.13.4 门禁踩到的一处真违规

`buildReport` 把三段结论合到一处后，圈复杂度 **15 > 上限 14**（detekt `CyclomaticComplexMethod`）。
⇒ 抽出 `matchingNames` / `addFailures` 两个泛型工具（顺带消掉了重复的
`.filterValues{}.keys.map{}`）。**这不是为了过门禁而拆**：三段本就是三个独立关注点。

#### 11.13.5 门禁相关（**不新增正文，见索引**）

- `check_compile_smells.py` 对 `sha256Hex` 的**假阳性**：今天已诊断并记在
  `.ai/SESSION-2026-09-17.md` **§11.5**（含根因与修法），本节不复述。
  ⚠️ 施工时若看到这 3 处 FAIL，**先确认是不是它**，别当成自己改坏了。
  > **2026-09-18 下半场补正**：09-17 那份诊断里说"调用落在 `SafKdbxFileSource.kt:195`
  > 的 companion 成员上"是**错的**（那一份 `private`，实例侧根本看不见）。
  > 真原因是**跨 Gradle 模块**同名匹配。详见 `.ai/SESSION-2026-09-18.md` §5.1。

---

## §12 第四轮修订（2026-09-18 · 「关于」段的权限管理行改为进应用内页）

> 起因：用户当天口头提「权限管理界面能优化一下。加一点规范的引导，打开什么权限之类的」。
> 提交 `a4c8686`。这一节只记**对本定稿的改动**；页面本身的实现说明在
> `.ai/SESSION-2026-09-18.md` §8。

### 12.1 改了什么

| 项 | §5.2 定稿（09-15） | 现在 |
|---|---|---|
| 点击行为 | **直接跳系统应用信息页** | **先进应用内权限引导页**（`PermissionsRoute`） |
| `permission_management_subtitle` | 在系统设置页查看与调整应用权限 | **看看这个应用要什么权限、拿去做什么** |
| 行的位置 / 组 | 「关于」段 | **不变** |
| `permission_management_title` | 权限管理 | **不变** |

### 12.2 ★ 为什么 §5.2 定下的副标题**必须重写**

§5.2 当时把原副标题「在系统应用信息页查看 / 调整 **Vaultix** 的权限」改成
「在系统设置页查看与调整应用权限」—— 这**在当时是对的**（那行的确跳系统页，
是在**指路**）。

但 §4 文案三条规则**明令禁止指路语**（"关闭入口见下方列表"就是被这条删掉的）。
所以那一版是个**妥协**：功能上需要指路，规则上不许指路。

⇒ 现在那一行**不再跳系统页**，指路的必要**消失了** ⇒ 副标题正好可以回到规则的
正解：**说清"这一行能让你看到什么"**，而不是"点下去会去哪"。

「看看这个应用要什么权限、拿去做什么」正是这个意思 —— **它描述的是内容，不是跳转。**

> ⇒ 这条修订**没有推翻 §4**，恰恰是**让 §4 收回了它本该有的作用**。
> 同时它印证了 §4 那条规则的价值：只要行还在跳系统页，就永远写不出合规的副标题。

### 12.3 与 §7 边界的关系

§7 里"不要在这一页增加新的一级分组"等边界**没有被推翻** —— 权限页是**二级页**，
不进设置首页。设置首页那一行仍是**同一个位置、同一个标题、同一段**。

### 12.4 新增的边界（**不要做**）

1. **不要**把合并清单里的 `NFC` / `USE_FINGERPRINT` /
   `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION` 列进权限页。
   它们是传递依赖（NFC ← MSAL → YubiKit）与签名级内部权限，
   用户既看不见也管不着 —— 列进去只会稀释真正要管的四项。
   证据与完整推导见 `.ai/SESSION-2026-09-18.md` §8.2。
2. **不要**给 `SettingsScreen.onOpenPermissions` 加默认值。
   设置页有两个调用点，默认空实现会让主界面设置 Tab 那一处**静默失效**。
3. **不要**用 `shouldShowRequestPermissionRationale` 控制扫码页那条
   "去系统设置里开启"出口的显隐（它是二义值，见 §8.3）。

### 12.5 装机验收（在 §8.1 基础上补）

- [ ] 设置 → 关于 → 权限管理：**进的是应用内页**，不是系统页
- [ ] 状态卡在有未授予项时显示「有 N 项待允许」，全授予后变「一切就绪」
- [ ] 点相机「授予」→ 就地弹系统框；授予后返回，**状态卡立刻更新**（不留旧状态）
- [ ] 拒绝相机 → 从系统设置手动开 → **回前台状态卡跟着变**（`ON_RESUME` 重算）
- [ ] 无指纹录入的设备：指纹行显示「设备不支持」且**没有按钮**
- [ ] 扫码页把相机权限选「拒绝且不再询问」→ 页面仍有「去系统设置里开启」，点了能跳
- [ ] **Android 13 以下**：通知行显示「已允许」，且**没有**按钮（单测覆盖不到这一支）
