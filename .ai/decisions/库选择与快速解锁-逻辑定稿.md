# 库选择与快速解锁 —— 逻辑定稿（2026-09-14）

> **这份文档是「逻辑层」的定稿，不含实现细节。** 用户（九哥）明确要求
> 「**先把逻辑理清楚再动手**」，本文件就是那个「理清楚」的结果。
>
> **交接状态**：逻辑已闭合 ✅ / **一行生产代码都没动** ❌
> ⇒ 新会话**直接从这里开始编码**，不需要重新论证。
>
> ⚠️ 本文件写于上下文即将耗尽时，是**唯一完整的逻辑结论源**。
> 与 `.ai/issues/05-KDBX本地库.md` 的 #93–#96 互为参照：
> 那几篇记的是**根因**，本篇记的是**用户拍板的方向**。

---

## §0 一句话总结

> 库选择：**首次平等挑选**（选中的即默认库）→ 默认库可随时改（**唯一写入点 = 设置**）
> → 临时切库只改活跃库 → **冷启动默认库优先**，为空则按 `createdAt` 最早
> → 解锁页有**换库出口** → 快速解锁**一把 KEK 管多库**
> （KDBX 需包裹「主密码 + keyfile」，**首次接入时登记**）。

---

## §1 六条定稿（用户逐条认可 ✅）

| # | 条目 | 关键约束 |
|---|---|---|
| ① | **首次打开：平等问「接入哪个库」** | Bitwarden / KDBX **地位完全平等**，不预设、不建库。判据 = `vaults.isEmpty()`（已有逻辑，见 `RootNavViewModel.kt:118`） |
| ② | **接入 KDBX**：选文件（SAF）→ 输主密码 → 打开 | 是「**输入**」不是「设置」；选文件天然含选目录，**不额外做「选文件夹」步骤** |
| ③ | **快速解锁：首次接入时顺手登记** | **一把指纹（KEK）管多个库**；KDBX 侧包裹「主密码 + keyfile」 |
| ④ | **默认库：设置里可改；临时切库不写默认库** | ⚠️ **当前代码违反此条** —— `ActiveVaultStore.select()` 会写 `defaultVaultId`（见 §3） |
| ⑤ | **冷启动：默认库优先；解锁页有换库出口** | 默认库为空 ⇒ 走原有逻辑（`createdAt` 最早），**老用户行为零变化** |
| ⑥ | **App 目录：只做导入导出中转，不托管库文件** | 库文件靠 SAF `takePersistableUriPermission` 持久化，**不搬进 App 目录**（否则断同步） |

---

## §2 用户放弃「迁移老用户」（重要！）

**问**：这些改动会不会影响已在用 Bitwarden 的老用户？
**答**：不受影响（引导页只在 `vaults.isEmpty()` 出现；默认库为空走原逻辑）。

我原本给出 A/B 两个选项（保留旧 `defaultVaultId` 值 / 加「已显式设置」标记）。
**用户选择：「我选择让用户重新装」。**

⇒ **取消迁移逻辑**。A/B 选择题**不存在了**。
⇒ 后续可直接用**新键名**，旧值自然失效，**不需写任何迁移代码**。

⚠️ **但这不能免除 §3 的修复** —— 共键冲突是**新装用户同样会踩**的独立 bug。

---

## §3 ⚠️ 本轮发现的结构性冲突（**必修**，与迁移无关）

**现象**：`ActiveVaultStore.select(vaultId)`（切库）**已经会写** `preferences.setDefaultVaultId(vaultId)`。

证据（`app/.../session/ActiveVaultStore.kt:105-109`）：

```kotlin
fun select(vaultId: String) {
    _activeVaultId.value = vaultId
    scope.launch { preferences.setDefaultVaultId(vaultId) }   // ← 切库即覆盖默认库
}
```

**后果**：用户「临时切去看一眼另一个库」，会**静默永久改掉默认库**。
下次冷启动进的就是那个临时看的库 ⇒ 违反用户第 ④ 条，且**新老用户都会踩**。

**正解：拆成两个键**

| 键 | 语义 | 写入时机 |
|---|---|---|
| `activeVaultId` | 本次会话当前看哪个 | 切库时（`select()`） |
| `defaultVaultId` | 冷启动先开哪个 | **仅用户在设置里明确改时** |

⚠️ 注意：`VaultixPreferences.defaultVaultId` / `setDefaultVaultId` **已存在**
（`core/datastore/.../VaultixPreferences.kt:225` 与 `:348`）——
**不是「待新增」，是「待拆分」**。同时 `ActiveVaultStoreTest.kt:91` 有一条
`coVerify { preferences.setDefaultVaultId("vault-b") }` 固化着**当前（错误）行为**，改后必须同步修。

---

## §4 快速解锁：KDBX 方案定稿

### 4.1 为什么「开关点了必然失败」（#93 的精确性质）

用户原问：「为什么是无效的，照理说是生效的吧？」

**答**：它**不是「没生效的开关」，是「谎报状态的开关」**。
开关值**真写入了** `preferences`，但**保险箱里是空的**。

根因（`VaultRepositoryImpl.kt:380-381`）：

```kotlin
override suspend fun enrollLocalUnlock(vaultId: String, cipher: Cipher): Boolean {
    val key = sessions.keyOf(vaultId) ?: return false   // ← KDBX 恒 false
```

`VaultSessionManager.keyOf()` 要的是「会话里在手的**对称密钥**」；
而 KDBX 会话（`KdbxSessionStore`）持的是**整库明文**、**没有可包裹的密钥**
（`KdbxOpener.kt:28-33` 的 `KdbxSession` 不含主密码、不含 keyfile 字节）。

### 4.2 传播路径（两处 UI 行为不一致）

| 位置 | 行为 |
|---|---|
| 设置页 `quickUnlockVaults` | **遍历所有库（含 KDBX）** ⇒ 渲染出开关（谎报） |
| 库列表横幅 `QuickUnlockBanner` | 走 `enrollLocalUnlock` ⇒ **恒 false** ⇒ 指纹按钮永不渲染 |

⇒ 用户看到「设置里勾选有效、实际用不了」。

### 4.3 定稿方案：包裹「主密码 + keyfile」

**技术可行性（已验证）**：

- `LocalUnlockKeyStore.wrap(cipher, bytes: ByteArray)` / `unwrap()` ——
  **对 `ByteArray` 一视同仁**，可直接包裹任意字节（`LocalUnlockKeyStore.kt:158-174`）；
- `KdbxOpener.open(bytes, password, keyFileBytes)` —— 三输入，主密码 + keyfile 齐全即可开库。

**为什么不能包裹「整库明文」**：与 `Kdbx.kt:18`「明文绝不落盘」**直接冲突**，
且需重构会话模型 ⇒ **不可取**。

### 4.4 用户拍板的两条流程决策 ✅

**D3（失效处理）= B「明确提示 + 自动重包」**：
> 指纹过了但打不开 ⇒ 提示「主密码可能已变更」⇒ 用户输新密码成功后**自动重新包裹**
> ⇒ 下次指纹又能用。

**校验失败处理 = B「宽松」**（用户原话「宽松一点」）：
> 输错密码 ⇒ 提示「主密码不正确」，**输入框保留、就地重输**，不掉出流程。

⚠️ **新增一步「校验」是必须的**：`wrap` 只管包裹字节、**不管字节对不对**。
若用户输错密码照样 wrap 成功 ⇒ 下次指纹解出错密码 ⇒ 打开失败。
⇒ **wrap 之前必须用这组凭据实际解一次库**（复用现有校验路径）。

### 4.5 ⚠️ 「启用时当场输密码」的必然性（重要推理）

用户在设置页勾选时，KDBX 会话**已在内存**，但：

- `KdbxSession` **不持主密码**（`KdbxOpener.kt:28-33`）；
- `Kdbx.unlock()` **用完即弃**、不落盘（`Kdbx.kt:90-120`）。

⇒ **无法从当前会话取到可包裹的东西** ⇒ 必须**再问用户要一次主密码**。
⇒ 「勾选只是登记意愿、真正包裹等下次输密码」**不如「当场输密码即时闭环」**——后者
用户每个动作都有立刻可见的结果，且状态与现实立即对齐（**用户直觉优于原方案**）。

### 4.6 安全等级提醒（写给将来的自己）

| | Bitwarden | KDBX |
|---|---|---|
| 包裹物 | **对称密钥** | **主密码**（全权限） |
| 泄露后果 | 只够到本地缓存，可改密码止损 | **整个库失守，无远程撤销** |

⇒ Android 上「指纹 + Keystore 用户认证 KEK，密文绑设备」是**标准且足够强**的做法
（KeePassDX / KeePass2Android 同款）。**支持做**，但将来若加用户提示，措辞须更慎重。

### 4.7 用户提出的 UI 方案（**采纳，优于原方案**）

原方案「分两个入口」→ 用户提出：

> 「在快速解锁的设置里面增加一个**快速解锁的生效范围**，可以选取哪些库生效的。
> 相当于**指纹解锁管理多个密码库**。」

**为何更优**：一个用户只有**一把** Keystore 密钥 ⇒「快速解锁是一个**能力**，
作用于哪些库由用户勾选」是**正确的心智模型**。
⇒ 勾选 = 决定「这把钥匙串上挂几把钥匙」。

---

## §5 三个被推过的边界情形（定稿）

### 边界一：默认库 = KDBX 时，冷启动必须先输密码 / 指纹

**做不到「直接进库内容」** —— 那等于把明文落盘，违反 `Kdbx.kt:18`。

| 场景 | 冷启动要什么 |
|---|---|
| KDBX **未启用**快速解锁 | **必须输主密码** |
| KDBX **已启用**快速解锁 | **只需指纹** |

**关键澄清**：「首次输密码」是**一次性成本**（库的钥匙必须来自用户），
**不是每次都要付**。启用快速解锁后，两个库体验**完全对齐**：

```
Bitwarden：输主密码登录 → 启用快速解锁 → 以后只按指纹
KDBX     ：输主密码开库 → 启用快速解锁 → 以后只按指纹
```

### 边界二：「共用一个快速解锁」的精确含义

**共用 = 一把 KEK（全局唯一）**；
**不共用 = 每个库各自被包裹的东西**（Bitwarden 包对称密钥 / KDBX 包主密码 + keyfile，**必须两份**）。

心智模型：**一把钥匙串，上面挂两把不同的房门钥匙，开哪扇门用哪把。**

### 边界三：指纹的语义 = **打开「当前屏上显示的那个库」**（方案 B）

用户按指纹时：

| 方案 | 语义 | 取舍 |
|---|---|---|
| A | 开**默认库** | 想开另一个库时，最便捷的动作反而用不上 |
| **B ✅** | 开**当前屏上显示的那个库** | **指纹 = 身份确认，不带库的语义**。所见即所得 |
| C | 指纹后弹「选哪个库」 | 过度设计 |

**定稿 = B**。理由：**「默认库」决定第一屏显示谁；「指纹」只负责打开你正在看的那一个。**
两个机制职责**完全不重叠**，不会打架。

### 边界补充：两个库的「持久状态」**不对称**（勿被表面措辞骗）

用户说「bitwarden 的登录不会掉，kdbx 的密码不会变」——**听似对称，实现完全不对称**：

| | Bitwarden | KDBX |
|---|---|---|
| 背后 | **服务器**账号 | **本地文件** |
| 「登录」状态 | 令牌 + 账号信息，**可落盘** | —— |
| 关了 App 再开 | 仍「已登录、锁着」 | **会话彻底没了**（内存明文丢弃） |
| 开库要什么 | 生物识别 或 主密码 | **必须**主密码（或指纹包裹） |

⇒ 用户的「不预设 / 可灵活切换」意图**成立**，但要理解 KDBX 那侧的「状态」本质是
**「没有状态」**（文件在，但每次冷启动都是未打开）。

---

## §6 被否掉的方案（连同理由，避免新会话重复提议）

| 方案 | 否决理由 |
|---|---|
| 首次打开就「选文件夹 + 设密码」（KeePass 系 App 式） | ① 违反第 ① 条「平等挑选」（等于预设用户要 KDBX）；② KDBX 是「输入」已有库的密码，不是「设置」；③ 选文件天然含选目录，**多余的步骤** |
| 把 KDBX 库文件搬进 App 专属目录 | **会断同步**（用户在电脑端/网盘有同一份）⇒ 两个库各自演变，数据灾难。SAF `takePersistableUriPermission` 已解决「不用每次找文件」 |
| 「勾选 = 登记意愿，下次输密码时顺手包裹」 | 用户当场输密码**即时闭环**更优（见 §4.5） |
| 包裹「整库明文」以省掉主密码 | 与 `Kdbx.kt:18` 直接冲突 |
| 为「默认库」给老用户做迁移（A/B） | 用户选择「重新装」⇒ **取消迁移**，用新键名即可 |
| 「App 数据文件夹」托管库文件 | 同第 2 行；目录只做**导入导出中转** |

---

## §7 下一会话：第一批任务清单（用户已认可分批）

> **门槛纪律**：detekt → 编译 → **单测**，三门禁全绿才算完成
> （⚠️ detekt 先于编译，会**掩盖编译错误**，改完必须**再真跑一次 compile**）。

### 第一批（能立刻消掉真实痛点，优先做）—— ✅ **已全部落地（2026-09-14 第五十四轮）**

| 序 | 任务 | 对应 issue | 关键点 | 状态 |
|---|---|---|---|---|
| 1 | **拆「活跃库 / 默认库」两键** | §3 新发现 | `ActiveVaultStore.select()` 改只写 `activeVaultId`（**不再写 default**）；`defaultVaultId` 唯一写入点 = 设置页（`SettingsViewModel.setDefaultVault`）。**同步修 `ActiveVaultStoreTest`**（原断言固化的正是该 bug） | ✅ |
| 2 | **补「切换密码库」正面入口** | **#96** | `ItemsMoreMenu` 最上方新增「切换密码库」（`SwapHoriz`），**仅 >1 库时显示**（`MainShellViewModel.vaultCount`）；设置页 `switchableVaults` 改为**列全部库**、未解锁项标注并可点进去 | ✅ |
| 3 | **修 KDBX 行裸露 `content://` URI** | **#95** | `VaultListScreen` 新增 `vaultSubtitle()`：KDBX 走「本地文件 · 文件名」，Bitwarden 保留 account/origin | ✅ |
| 4 | **补重复添加同一 kdbx 的反馈** | **#94** | 新增 `KdbxAddOutcome`（Added/Updated/Failed），`addKdbxVault` 返回它；`AddKdbxScreen` 补 Snackbar。**D5 定稿 = 允许覆盖 + 成功反馈** | ✅ |
| 5 | **冷启动默认库优先 + 解锁页换库出口** | #96 / §1⑤ | `RootNavViewModel` 全锁分支改为 `VaultLocked(vaultId = 默认库.takeIf{在列表里} ?: vaults.first().id)`；`UnlockScreen` 新增 `onSwitchVault` | ✅ |

**门槛结果**：detekt ✅ / `:app:compileFullDebugKotlin` ✅ / `testFullDebugUnitTest` ✅
（app 模块 **162 项、0 失败**；`ActiveVaultStoreTest` 7/7、`RootNavViewModelTest` 10/10）。
⚠️ 构建备忘：`app` 是 **flavor 化**的，任务名是 `compileFullDebugKotlin` 而非
`compileDebugKotlin`；`./gradlew` 在本机 git-bash 会报 `GradleWrapperMain` 找不到，
改用已解压发行版直接调用。

### 第二批（体量大、风险高，单独一批）

| 序 | 任务 | 对应 issue | 关键点 | 状态 |
|---|---|---|---|---|
| 6 | **KDBX 快速解锁（生效范围 UI + `enrollLocalUnlockKdbx`）** | **#93** + §4 | 新增仓库方法 + 主密码输入对话框（宽松重试）+ **wrap 前必须校验** | ✅ |
| 7 | **SSH 键输入表单** | **#97** | `ItemFormDialog.kt` 原为 `SecureNote, SshKey -> Unit` ⇒ 只能建空壳；已补 `SshKeyFields` + 指纹**自动推导** | ✅ |

#### 第 6 项落地记录（2026-09-14 第五十五轮）

**新增文件**
- `data/repository/.../KdbxUnlockPayload.kt` —— 包裹物编解码。**必须长度前缀**：
  主密码与 keyfile 都是变长项，裸拼会出现
  `("ab", [0x63])` 与 `("abc", null)` 编出**同一串字节**的歧义。

**新增契约**（`domain/VaultRepository.kt`）
- `enrollLocalUnlockKdbx(vaultId, masterPassword, keyFileUri, cipher) → KdbxEnrollOutcome`
- `completeLocalUnlockKdbx(vaultId, cipher) → KdbxUnlockOutcome`
- 与 Bitwarden 侧**分成两个方法**而非重载：包裹物本质不同
  （密钥 vs 主密码 + keyfile），混用会解出错误语义。

**关键实现约束（都有注释钉住）**
1. ★ **先校验、后包裹**：`enrollLocalUnlockKdbx` 先用这组凭据**真的解一次库**
   （复用 `unlockKdbxInternal`），失败即返回且**不写任何东西**。
   `wrap` 只管封字节、不管字节对不对 —— 先包后校会得到「启用成功但躺的是错密码」。
2. ★ **keyfile 走字节不走临时文件**：`Kdbx.unlock` 新增可选 `keyFileBytes`
   参数（默认 null，向后兼容）。原方案要为此把 keyfile 落成临时文件，
   与「明文绝不落盘」冲突 —— 改签名比写盘干净。
3. **明文用完即擦**：包裹物明文、解出的 keyfile 字节都 `fill(0)`。

**解锁路径按库类型分流**（三处，缺一处就是错语义）
- `UnlockViewModel.completeLocalUnlock`
- `AutofillActivity.unlockAll`
- 库列表 / 设置页的**启用**路径

**「谎报状态」的根治**（§4.2 的病根）
- 设置页 `QuickUnlockManageDialog` 改为「**生效范围**」语义（用户 §4.7 的提法），
  逐库列出并标注 KDBX 需先输主密码。
- KDBX 分支改走 `startKdbxQuickUnlock` → 主密码框 → 校验 → 指纹（宽松重试）。
  横幅（`VaultListScreen`）同款。
- `QuickUnlockVaultUi` 增 `kind`：两条登记流程的差异必须在 UI 可见，
  否则用户不明白「为什么点 KDBX 会弹密码框」。

**D3（失效自愈）落地**：`KdbxUnlockOutcome.StaleCredentials`（指纹过了但
包裹物打不开）→ UI 文案改为「**主密码可能已在别处变更，请输入当前主密码**」，
走进主密码路径即可自愈；**不删快速解锁登记**（与 Bitwarden 侧「不可恢复即清」
取向不同 —— 那侧包裹物是密钥）。

**测试**：`KdbxUnlockPayloadTest` 9 项全绿（含两条★歧义/截断不变量）；
`ActiveVaultStoreTest` 增至 9 项。

#### 第 7 项落地记录（2026-09-14 第五十六轮）

**新增文件**
- `core/common/.../SshFingerprint.kt` —— OpenSSH 公钥指纹（`SHA256:<base64>`），
  算法与 `ssh-keygen -lf` **逐位一致**（ed25519 / RSA / ECDSA 三组对照）。

**新增逻辑**（`FormValues.kt`，纯函数、可单测）
- `sshValuesOf` / `buildSshKey` —— 与 `cardValuesOf` / `buildCard` 同款读写对称。
- `applyPublicKeyChange(values, newPublicKey)` —— 指纹跟随公钥的策略。
- 三个下标常量 `SSH_PRIVATE_KEY_INDEX` / `SSH_PUBLIC_KEY_INDEX` /
  `SSH_FINGERPRINT_INDEX`：**三处共用**，杜绝「公钥填进指纹框」这类错位。

**⚠️ 接口取舍：`ssh` 为什么是 `buildSnapshot` 的第 5 个参数**
`FormValues` 已占满 8 个参数，正卡在 detekt `allowedConstructorParameters: 8` 的
硬上限 —— 加第 9 个直接违规。这与当年 `ItemMeta` 被拆出去**同一个理由**，
沿用同一做法（而非放宽门禁）。

**★ 指纹派生策略：比较值，而不是记 `fingerprintEdited` 标志位**
判据 = 「当前指纹为空 **或** == 旧公钥的推导值」⇒ 判为自动、可覆盖；否则
视为用户自定义、原样保留。**比较法的好处是可自愈** —— 用户把手改的值又改回
推导值（或清空）时自动重回跟随，不会永久卡在「手动」。
- 推导失败 ⇒ 写**空串**而非保留旧值：宁可空着，也不能挂一个与当前公钥不符的
  指纹（会骗人的错信息，属 #84「三种空」纪律）。
- 初始化时：指纹为空而公钥可解析 ⇒ **补上**（导入条目常缺指纹）；
  已存有指纹 ⇒ **绝不覆盖**，避免静默重写数据。
- ⚠️ 已知边界：旧指纹若是**遗留 MD5 格式**，永不等于 SHA256 推导值 ⇒ 判为
  自定义而保留 ⇒ 改公钥后会陈旧。取舍：「尊重用户填过的值」优先。

**与原计划的两处有意偏离**
1. 原写「私钥默认掩码 + 可切换可见」⇒ **改为明文**：详情页 `SshKeySection`
   本就是明文 + 复制；表单若遮蔽，用户**没法确认自己粘对了没有**，
   而那正是本表单要解决的问题。两侧一致优于单侧"更安全"。
2. 原写字段名 `fingerprint` ⇒ 模型实为 `keyFingerprint`（对齐 Bitwarden
   `JsonSshKey`），按模型来。

**写路径无需改动**（原本就通）：`CipherMapper.overlaySshKey` 在
`item.type == SshKey` 时按 `item.sshKey` 重新加密覆盖 ⇒ 本问题是
**纯输入侧缺口**，不是数据链路问题。

**顺手清理**：`item_edit_type_fields_readonly` 已无引用（唯一使用点即被替换的
只读提示）⇒ 删除；SSH 字符串注释改为「详情分区与编辑表单共用」。

**测试**：`SshFingerprintTest` 11 项 + `FormValuesTest` 19 项全绿
（期望值取 `ssh-keygen -lf`，**不是**自算回填）。

### 待用户确认项 —— ✅ **均已拍板并落地（2026-09-14）**

- **D5**：重复添加同一 kdbx 文件 → **「允许覆盖 + 成功反馈」**（Snackbar 文案区分
  「已添加密码库」/「该文件已在列表中，已更新」）。
- **首次接入是否顺手设为默认库** → **是，且「仅在默认库为空时写」**。
  **已实现**（2026-09-14 第五十五轮）：
  - `VaultixPreferences.trySetDefaultVaultIfAbsent(id)` —— 判空 + 写在同一 `edit`
    事务内，避免两个库并发接入时都读到空；
  - 落点在**新库接入成功**这条业务路径（`registerVaultRow` 的新库分支 /
    `addKdbxVault` 的 `Added` 分支），**不在** `ActiveVaultStore.select()`。
    ⚠️ 这条边界是硬的：放进 `select()` 就退化成第一批修掉的那个 bug。
  - 绝不覆盖用户已设过的选择（这正是第一批任务 1 的同一种病，只换了触发点）。

---

## §8 顺带确认的两件事

1. **`VaultixPreferences.defaultVaultId` 已存在**（`:225` / `:348`）——
   **推翻**「需新增键」的旧认知。任务 1 是「**拆分**」不是「新增」。
2. **`ActiveVaultStore.pick()` 的兜底是 `unlocked.minOrNull()`**
   （`ActiveVaultStore.kt:97-102`）——按 **id 字符串字典序**，不是 `createdAt`。
   Bitwarden 的 id 是 UUID、KDBX 的是 `content://...` URI ⇒
   **谁排前面基本是随机的**。有了「默认库」后，这个兜底也应**先尊重默认库**，
   其次才是稳定排序。

---

## §9 相关文档索引

| 文档 | 内容 |
|---|---|
| `.ai/issues/05-KDBX本地库.md` | #59 / 64 / 69 / **93 / 94 / 95 / 96 / 96.1** 的根因与证据 |
| `.ai/issues/04-锁与解锁.md` | #60 锁/解锁模型规范化、#61、#73、#85 |
| `.ai/issues/06-界面与交互.md` | #84 三种「空」、#87 转场只允许 `translationX`、#88 覆盖安装后指纹失效 |
| `Docs/progress/quick-unlock-plan.md` | 快速解锁既有设计（21KB） |
| `Docs/progress/main-shell-migration.md` §0 | 「**单活跃库**」产品定义（Bitwarden 能力 + KDBX 能力，登录时二选一） |
| `.ai/SESSION-2026-09-14.md` | 本日完整流水（含真机实证链） |
