# 会话记录 · 2026-09-12（接力入口）

> 本文件是 **最新接力入口**。开工先读：
> `MEMORY.md`（约定/硬约束，**末节「第四十八轮长期约定」是本次的接力起手式**）
> → `ISSUES.md`（坑：**#35↔#43↔#39 的推翻链**必须先看懂；本次新增 #61~#65）
> → `Docs/progress/next-steps.md`（待办，最新在顶部）→ 本文件（最近进展 + 下一批起点）。
> 前序流水见 `SESSION-2026-09-11.md`。

---

# 🟢 最新：第四十八轮 —— #60 全清单交付（1b~4 + 第 2/3/4 批）

**四个提交，四批全部完成**（详见 `.ai/ISSUES.md` #61~#65 与 `Docs/progress/current-status.md` 末节）：

| commit | 内容 |
|---|---|
| `5682bc6` | 锁/解锁 1b~4：**查看层锁**（锁按钮不清密钥）/ 根导航判据 / 解锁页仅认证分支 / 超时真锁清标记 / **退出数据库** / **解锁即回填** |
| `e88bda2` | 第 2 批：顶栏**胶囊化**（🔍 + ⋮）+ **点库名展开分类筛选** |
| `93c814c` | 第 3 批：**站点图标**（`<服务器>/icons/<域名>/icon.png`，失败回退首字母） |
| `fcaf918` | 第 4 批：**KDBX 集成**（阶段 A 只读）+ 通行密钥/验证码 codec 补全 |

**门禁**：`333 tests, 0 failures` + 全模块 detekt 全绿（本地真跑）。

**一句话现状**：用户可见的「锁按钮语义 / 顶栏观感 / 站点图标 / KDBX 本地库」四件事
代码已全落地，**等真机验收**（验收清单见 `current-status.md` 末节，共 7 条）。

**下一批的候选**（本轮明确**未做**，按建议顺序）：

### 1. KDBX 阶段 B（写回）—— 最大的未完成块
**起点**：`.ai/ISSUES.md` #59（引擎选型）+ #64（集成层的模型差异）+ #60 后续批次第 4 批末段。
**要做什么**：刷新后能保存 → 原子替换原文件 + 写 `.kdbx.bak` 备份 + 保真度登记 + 往返测试。
目标格式 **KDBX 4.1**（kotpass 支持读也支持写）。

**铁律（写之前先记住）**：
- **插件字段（`KPEX_*`）与不认识的自定义字段一律原样保留** —— 这是 Bastion 的
  `KeePassFieldRegistry` 存在的理由；丢一个字段 = 用户的通行密钥失效；
- 写回必须**整体重建 + 原子替换**（先写临时文件再 rename），不能就地改；
- 往返测试要**逐字段相等**（照 `ISSUES.md` #22 的教训：Mapper 读 N 字段就必须能写回 N 字段）。

**需要新增的钩子**：`Kdbx` 门面里补 `save(vaultId, ...)`；
`KdbxSessionStore` 已持有 `KeePassDatabase`（kotpass 的 `encode` 可用）；
`KdbxMappedContent.groupPaths`（uuid → 路径）**就是为写回预留的**。

### 2. KDBX 的快速解锁（KEK 包裹）
现在每次解锁都要输主密码。可复用 Bitwarden 侧的 `LocalUnlockKeyStore` 思路，
但包裹的是「主密码 + keyfile 授权」而不是对称密钥。
⚠️ 先想清楚「换 keyfile 文件后怎么办」（`SourceUnavailable` 已有分类，可据此作废包裹）。

### 3. KDBX 回收站映射
当前只把回收站条目**计数**（`KdbxUnlockedContent.recycleBinCount`），不进回收站页。
`ItemRepositoryImpl.observeTrash` 对 KDBX 库**明确返回空** —— 要接的话在这里分支。

### 4. 真机验收反馈（最优先实际是这条）
上面 1~3 都是新功能；**先把 `current-status.md` 末节那 7 条验收跑一遍**，
有回归再谈新功能（尤其第 7 条：读路径分流动了 `observeItems` 入口）。

---

## 🔴 一句话现状（第四十三~四十七轮）

**通行密钥「注册/登录最终校验失败」的两个根因已依次定位并修复**：
① `rawId` / `userHandle` 被二次 Base64 解码（`1d2d242`）；
② **`clientDataJSON` 回传空占位符**（`aa5fa27`，推翻第三十轮的错判）。
两者 CI 全绿（`34679687783` / `34686274203`）。
另完成：启动死锁修复、Bastion 主界面骨架 + 活跃库真源收敛、沙箱构建环境打通。

## 本轮修复链（6 个提交）

| commit | 内容 | CI |
|---|---|---|
| `22d7273` | **fix(nav)** 全新安装启动死锁（preview 版进不去、一直转圈） | ✅ |
| `7a99635` | **feat(shell)** Bastion 主界面骨架 + 活跃库真源收敛（迁移阶段 1/2） | — |
| `d488dbf` | **fix(quality)** 修 CI detekt 4 处违规 | — |
| `8d7db41` | **fix(compile)** 补两处缺失 import | ✅ `34668557223` |
| `1d2d242` | **fix(passkey)** `rawId`/`userHandle` 二次 Base64 解码（登录校验失败） | ✅ `34679687783` |
| `aa5fa27` | **fix(passkey)** 回退 `clientDataJSON` 占位符（**W3C 规范为准**） | ✅ `34686274203` |

---

## 第三十五轮 · 启动死锁：4 跳根因 + 「无库 ≠ 锁定」的类型级修复（`22d7273`）

### 症状
真机：卸载旧版后新装 → **永久转圈进不去**。

### 根因链（缺一不可）

| # | 缺陷 | 说明 |
|---|---|---|
| ① | `RootNavViewModel` 旧状态机只有两态，把「一个库都没有」判成 `VaultLocked` | 缺 `Onboarding` 态 |
| ② | `VaultixApp` 用 `remember { resolveStartDestination(state) }` **固化首帧结论** | 而首帧 Flow 初始值恒为 `VaultLocked` ⇒ `startDestination` **锁死整个进程生命周期** |
| ③ | `UnlockViewModel` 无库可解析 → `vaultId` 恒空 | — |
| ④ | `UnlockScreen` 在 `vault == null` 时只画 `CircularProgressIndicator()` + `return@Column` | **全屏转圈且无出口** |

### 修法
- `RootNavState` 补 **`Splash`**（首帧占位、作初始值）与 **`Onboarding`**（无库）两态；
- `startDestination` 固定为 `SplashRoute`（只承担「未知」语义），落点改
  `LaunchedEffect(rootNavState)` + `navigateToRoot()` 换栈、`routedOnce` 防重复换栈；
- `UiState` 增 `noVaultToUnlock` 区分「首帧未到」与「确认没库」，后者立刻走 `onNoVault` 逃生。
- 配 `RootNavViewModelTest` 4 例钉死映射。

### ★ 三条可复用教训
1. **`NavHost.startDestination` 在组合期间不应变化** —— `remember` 固化会把错误结论
   锁死整个进程生命周期。
2. **loading 分支必须有出口**，否则「异常态」表现为「永远加载中」，用户无从区分。
3. **状态机缺态时，缺失的那一类输入会被静默归到相邻态**（此处「无库」被吞进「锁定」）
   —— 新增状态前先列全输入域。

---

## 第三十六轮 · Bastion 主界面骨架 + 活跃库真源收敛（`7a99635`，迁移阶段 1/2）

> 用户前序指令：「修复，然后把 bastion 的 ui 都准备开始搬过来」。

### 落地内容
- **主界面骨架**：`ui/shell/MainShellScreen.kt` + `MainShellViewModel.kt`（Bastion 式 Tab 容器）；
  `ui/cardwallet/` 四件（`CardWalletScreen` / `CardWalletViewModel` / `CardBrandIcon` /
  `CardBrandLibraryLogo`，卡品牌图标库 ~850 行）。
- **阶段 2「★ 全局活跃库真源」整格做掉**：新增 `session/ActiveVaultStore.kt`（含 `resolve()`），
  **7 处消费点**全部只读它（autofill / CP / 保存 / 手动填充 / 设置页 / 两个 ViewModel）。
  详见 `Docs/progress/main-shell-migration.md`。
- 配套门禁修复（`d488dbf` 修 detekt 4 处 + `8d7db41` 补 2 处 import）。

### ★ 关键设计点：`ActiveVaultStore.resolve()`
`activeVaultId` 由 `init` 协程异步填充，而系统冷启动常常**只拉起
`VaultixAutofillService` / `VaultixCredentialProviderService`**（主界面从未打开）
→ 首帧未到时读到 `null`。若按「null 就退化成遍历全部」兜底，
**等于重新引入本类存在意义所要消灭的问题**（候选重复 / 回写目标不确定）。
⇒ `suspend fun resolve()` **每次真实重算**（`preferences.defaultVaultId` +
`observeUnlockedVaultIds`），并把取值规则抽成 `pick()` 供流与重算共用，避免口径漂移。

### ★ 多库是「筛选维度」而非「导航层」（用户纠正）
`grep -rn "vaultId" reference/bastion/.../ui/main/` **零命中** ⇒ Bastion 主界面
（含所有 Tab）**完全没有 `vaultId` 概念**。多库的真实处理：`VaultV2Pane.kt` 的
`UnifiedCategoryFilterSelection` 把「库」降级为与「文件夹」「分类」**平级的过滤维度**。
⇒ Vaultix 把 `vaultId` 从**路由参数**降级为 **Tab 容器内的筛选状态**；
解锁后直接进 `MainShellRoute`；多库管理移入设置页。
**教训：不要把「数据维度」误判为「导航结构」—— 先 grep 竞品主界面有没有该参数。**

---

## 第三十七轮 · `rawId` / `userHandle` 被二次 Base64 解码（`1d2d242`，P0）

### 用户报告
「通行密钥能够读取到，能够进入登录，在**最终校验**的时候提示错误。」

### 根因：注册侧与登录侧口径不对称
注册侧（`PasskeyCreateActivity`）三处**自洽且正确**：
- `credentialId = WebAuthn.base64Url(key.credentialId)` 落库；
- `authData` 里放**原始 32 字节**；
- `rawId = base64Url(原始 32 字节)`。

登录侧把**已经是 b64url 文本**的 `cred.credentialId` **又解码了一次**，再编码回去：

```kotlin
// 旧实现（已证伪）
val idBytes = decodeBase64UrlOrStandard(cred.credentialId) ?: cred.credentialId.toByteArray(UTF_8)
buildGetResponseJson(credentialId = idBytes, ...)     // 内部又 base64Url()
```

规范要求 `rawId` **逐字节等于** RP 在 `allowCredentials` 里持有的 ID，该路径两处失配：
- **标准 Base64（含 `+` `/`）存储的 ID 被规范化成 b64url（`-` `_`）** → 文本不同。
  实测：`2z85N/AMiSf…qf/i5FU=` 被回传成 `2z85N_AMiSf…qf_i5FU`，做字符串比对的 RP 直接拒。
- 解码抛异常时被 `?:` 兜底成 `text.toByteArray(UTF_8)` → 把 Base64 文本本身当 ID 发出去。

### 为何此前测试全绿（关键）
`PasskeyResolution.idMatches`（候选过滤）把**两边都解码**后比较字节，**自洽**
→ 候选正常列出，**麻痹了测试**；而断言回传路径单边解码，**无人覆盖**。

### `userHandle` 是同陷阱的第二实例
注册侧 `userHandle = json.optJSONObject("user")?.optString("id")` **原样落库**（未解码），
登录侧却又 `decode` → `base64Url` 编码回去。同样改为原样回传。

### 修法：以「存储态文本」为准
| 文件 | 改动 |
|---|---|
| `core/common/WebAuthn.kt` | 新增 **`rawIdFromStored()`**（合法 Base64 则**原样返回**；脏数据退化为 UTF-8 重编码，不崩）+ **`buildGetResponseJsonFromStoredId()`**（`id`/`rawId`/`userHandle` 全走原样文本）；旧 `buildGetResponseJson` 加「优先用新函数」KDoc 警示 |
| `passkey/PasskeyGetActivity.kt` | 改用 `buildGetResponseJsonFromStoredId`；删掉 `idBytes` 与 `userHandle` 的两次多余 decode；补 `rawId=` / `storedIsBase64=` 诊断日志 |
| `core/common/WebAuthnTest.kt` | 新增 2 例回归锁（`stored credential id is echoed verbatim as rawId`、`rawIdFromStored tolerates blank input`） |

### ★ 三条教训
1. **「能列出候选」不能证明 ID 正确** —— 过滤与回传是两条独立路径，
   前者宽容（双边解码）会掩盖后者的严格（逐字节比对）。
2. **WebAuthn 里凡「ID / userHandle」都是不透明字节串**，任何"解码—再编码"往返
   都是在赌编码形态不变，而 `+`/`/` ↔ `-`/`_` 的差异足以致命。
3. **改签名/回传逻辑必须配端到端（RP 视角）回归锁**，仅测"能生成 JSON"不够。

---

## 第三十八轮 · `clientDataJSON` 回传空占位符（`aa5fa27`，P0，**推翻第三十轮**）

> ⚠️ **本轮推翻 `SESSION-2026-09-11.md` 第三十轮 / `ISSUES.md` #35 的结论。**
> 完整条目见 **`ISSUES.md` #43**，此处只留索引与决策要点。

### 用户报告与要求
实测 **GitHub 注册通行密钥**报
`Two-factor authentication ... Security key authentication failed.`
并给出修复原则：**「按照标准改对，不要失误，不要自己乱加。应该是有标准的才对。」**

### 错在哪（一句话）
第三十轮依据官方文档 `set a placeholder value for clientDataJSON` 把浏览器流程的
`clientDataJSON` 改成 `ByteArray(0)`。**该句有前置条件**，原文是：
> **If you retrieve an origin**, use the `clientDataHash` ...

`If you retrieve an origin` 特指经 `CallingAppInfo.getOrigin(privilegedAllowlist)` +
**特权应用名单**拿到 origin 的场景（Google Password Manager 走那条路，
challenge 校验由**系统侧**完成）。Vaultix 的 `CallingAppOrigin` 明确采用
「**自证式读取**」、**不走特权名单** ⇒ **不适用占位符**。

而规范层面 RP **一定**解析明文逐项校验（W3C WebAuthn L2 §7.1 / §7.2），
`C.challenge` 必须等于 `base64url(options.challenge)` ⇒ **空数组连 JSON 解析都过不了**。

### ✅ 决策表（关键区分，勿再混淆）

| 事项 | 浏览器流程 | 原生 App 流程 |
|---|---|---|
| **签名**覆盖 | `authData ‖ clientDataHash`（系统给的） | `authData ‖ sha256(自建 JSON)` |
| **回传** `clientDataJSON` | **自建真实 JSON** | **自建真实 JSON**（同一份） |
| `androidPackageName` | **不写** | 可写 |

> 回传字段供 RP **读明文校验**；签名哈希保证与浏览器一致 —— **二者互不冲突**。

### 三家对照（**更正第三十轮的误判**）
Bitwarden / Keyguard / Bastion **三家都回传真实 JSON**，只有 Vaultix 走了占位符歪路。
第三十轮把 Bastion / Keyguard 标为「反例」是**错的** —— 当所有参考实现都跟你不同时，
**先怀疑自己**。

### ★ 四条教训（本轮最贵）
1. **官方文档的限定条件不能只读后半句。** `If you retrieve an origin` 决定了整条建议
   是否适用。
2. **与规范冲突时以规范为准。** 规范是 RP 的实现依据，任何平台建议都不能推翻它。
3. **「所有参考实现都跟我不同」时，先怀疑自己。**
4. 用户那句「**应该是有标准的才对**」是对的：**遇协议层争议先拉规范原文逐字比对**，
   不要在实现之间靠印象猜测。

### 验证（`aa5fa27`）
`:core:common:testDebugUnitTest` **95 用例 0 失败** /
`testFullDebugUnitTest` **129 用例 0 失败** / `detekt` 通过 /
`:app:compileFullDebugKotlin` **BUILD SUCCESSFUL** /
**CI `34686274203` = success**。

---

## ⚙️ 沙箱构建环境打通（长期收益）

> 本轮把沙箱从「**完全跑不了 Gradle**」恢复到「**可真实编译 / 跑单测 / 跑 detekt**」。
> 完整清单见 `ISSUES.md` #44，此处只留要点。

| # | 问题 | 修法 |
|---|---|---|
| 1 | `/root/.gradle/init.gradle` **语法错误**（`mavelCentral()` 拼写错 + url 未加引号）→ 所有 Gradle 调用初始化即失败 | 重写为 `beforeSettings { ... }`；**必须同时注入 `pluginManagement` 与 `dependencyResolutionManagement`**（项目设了 `FAIL_ON_PROJECT_REPOS`） |
| 2 | AGP 插件解析失败 | 镜像**必须在 `pluginManagement` 层**注入（`settingsEvaluated` 太晚） |
| 3 | `sdkmanager` 报 `Failed to find package 'platforms;android-37'` | **platform 37 目录名带扩展版本号（`android-37.0`），在线 manifest 里根本没有 `platforms;android-37`**（最高 android-36）⇒ 只能直取 `platform-37.0_r01.zip` |

附带：hosts 补 `dl.google.com → 113.108.239.161`；wrapper 分发包经 `ghfast.top` 镜像
（140MB）装入缓存；daemon OOM 加 `-Xmx3g` + `-Xmx2g`；`api.github.com` 用
`curl --resolve ...:20.205.243.168` 直连（响应含控制字符需 `json.loads(..., strict=False)`）。

⚠️ 唯一未完成：`assembleFullDebug` 的 **native 符号剥离**（沙箱缺 NDK，与代码无关，
CI 上正常）→ 替代验证用 `:app:compileFullDebugKotlin`。

---

## ⏳ 真机待验证（下一优先）

1. **GitHub 注册通行密钥**（此前报 `Security key authentication failed`）→ 应通过；
   再验证**登录**（`rawId` 修复的目标场景）。
2. `logcat` 应为：
   ```
   PK create clientDataJson type=webauthn.create origin=... cdjLen=NN jsonMatchesBrowserHash=false
   PK assertion ready sigLen=... cdjLen=NN browserFlow=true rawId=... storedIsBase64=true
   ```
   ⚠️ `jsonMatchesBrowserHash=false` 属**预期**（系统那份哈希来自浏览器），
   **不再是故障信号**。
3. 回归确认：密码自动填充、启动（全新安装不应再转圈）、设置页切换活跃库后填充目标跟随。

## 第四十轮 · 通行密钥「登录末步校验失败」根治：`rawId` 缺 UUID 分支（`8fd64f5`，P0）

### 症状
Edge 里 GitHub 通行密钥：**候选能列出、能选、生物识别通过，最后一步校验报错**。

### 排查路径（★ 方法论可复用，本轮最值钱的部分）
1. 真机抓 log（`scripts/adb-log.sh`，注意 `pair` 端口 ≠ `connect` 端口、两套 adb 会抢 5037）：
   ```
   PK assertion ready sigLen=70 authDataLen=37 cdjLen=131 browserFlow=true
      rawId=5698f18a-41e0-e865-0104-c989aee7dc18 storedIsBase64=true
   ```
   同时系统侧 `CredentialManager: onUiEntrySelected → onFinalResponseReceived from
   VaultixCredentialProviderService → finishing session`。
2. ⇒ **我们侧完全成功、系统已完成交付**，问题只能出在「断言内容被 RP 拒绝」。
   范围一下从"整条链路"压到"断言里的某个字段"。
3. 唯一可疑字段 = `rawId`，且它是 **UUID 文本形态**。
4. 三方源码交叉印证（**一致**）：Bitwarden `rawId = rawId.base64EncodeForFido2Response()`（由**字节**编码）、
   Keyguard `PasskeyCredentialId.encode`（UUID → **16 字节**）、Bastion `toWebAuthnId`。
   ⇒ 三家都是「先归一到字节再 base64url」，**UUID 文本必须走 16 字节分支**。

### 根因
`WebAuthn.rawIdFromStored` 只有「能 base64 解码就原样返回」一条路。而 **UUID 文本的字符集
`0-9a-f-` 恰好全落在 base64url 字母表内**、长度 36 又是 4 的倍数 ⇒ 被**误判成"可解码"**
（现场日志的 `storedIsBase64=true` 就是这个误判）⇒ **原样把 GUID 文本当 rawId 发出**。
RP 解出 **27 字节** ≠ 它注册时持有的 **16 字节** ⇒ 判为未知凭证。

### 修法
`rawIdFromStored` 先试 `UUID.fromString → 16 字节 → base64Url`（22 字符），再 fallback 原逻辑；
新增 `describeStoredIdForm`（`blank/uuid/base64/text`）取代误导性的 `storedIsBase64` 日志。

### ★ 三条教训
1. **库里 `credentialId` 有两种形态**：Vaultix 自建 = `base64Url(32字节)`（43 字符）；
   Bitwarden 同步 = **UUID 文本**（36 字符）。两者混在同一库里，只按「能不能 base64 解码」判形态**必错**。
2. **先证伪「自己失败了」**：先用系统侧日志证明"我们没失败"，再压到字段级 —— 这是本轮一刀命中的原因；
   此前几轮在 clientDataJSON / origin 维度反复横跳，恰恰因为没做这一步。
3. **UUID 文本能通过 base64url 解码**是经典陷阱（长度 36 = 4×9）。凡"启发式判形态"都要显式考虑它。

### 验证
✅ **真机重登 GitHub 通过（用户确认）** —— 长期开放的「github 通行密钥不可用」**正式结案**。

---

## 第四十一轮 · Bitwarden「填充辅助」完整搬运 + 用户名字段识别 + TOTP 无条件复制（`9fca5ab`）

### 1. 填充辅助（Fill Assist）完整照搬
- 上游机制：规则来自**服务端** `/api/config` 的 `environment.fillAssistRules`
  （bitwarden.com 当前值 = `https://github.com/bitwarden/map-the-web/releases/latest/download`），
  `manifest.json` → `forms.v1.json`（schema 主版本必须 `1`），本地缓存 6h、按 `cid` 判重；
  受 feature flag `fill-assist-targeting-rules`（**官方当前 false，未 GA**）+ 设置开关双重门控。
- 规则形态：`host(去 www.) → category → 字段键 → List<SelectorClause(tag,id,name,type,role)>`；
  login 类别 = `account-login / account-creation / account-update`，card = `payment-card`；
  **规则表只有 27 个站点**（无中国大陆站点），25KB，**许可证 GPL-3.0（与本项目同）**。
- 落地（新增 `app/.../autofill/fillassist/` 5 文件）：模型 / CSS 选择器子集解析 / manifest+forms 解析 /
  OkHttp 拉取 + 磁盘缓存 + 6h 节流 / `HtmlInfo` 匹配器（属性**精确相等**、拒绝无约束子句、
  同时命中 username/email 时**优先 username**）。规则地址直接用 Bitwarden 公开地址 ⇒ 后续更新自动吃到。
- 接入：`AssistStructureParser.parse(structure, fillAssistRules)` —— 按**页面主机**取规则；
  **有规则时以规则为准**（命中即 HIGH 强信号，未命中**丢弃该节点**、不退回启发式）；
  无规则（绝大多数站点）行为完全不变。
- 单测：`FillAssistRulesTest`。

### 2. 用户名字段识别 —— QQ 登录页账号框「既不弹候选也填不进去」
- 两个症状的根因是同一条：QQ 登录页是**标签页**结构（QQ号/手机号/邮箱），非当前标签的输入框
  仍在 View 树里但**不可见**，已被文本启发式判成 USERNAME ⇒ `promoteUsernameField` 的旧判据
  「**没有任何** USERNAME 字段就跳过」被这些**看不见**的框挡掉 ⇒ 可见的 QQ 号框永远是 UNKNOWN
  ⇒ 既不是填充目标（`fillTargets` 要 ≥MEDIUM）也取不到（`targetIdsFor` 按语义取）。
- 修法：①判据改为「没有**可见**的 USERNAME 才升格」；②升格时 `strength → MEDIUM`。
- 上游依据：Bitwarden `AutofillParserImpl.updateForMissingUsernameFields`（把密码框之上紧邻的
  `Unused` 提升为 `Login.Username`）。

### 3. TOTP 改为**无条件**复制（对齐 `AutofillCompletionManagerImpl`）
此前只在「页面没有验证码框」时才复制；上游是**每次填充成功后都复制**（仅受开关门控）。

### 4. 设置页通行密钥区
- **不再误报「未启用」**：`CredentialProviderStatus.isEnabled` 原先 `!raw.isNullOrBlank() && contains(...)`，
  但 **Android 16+ 起 `Settings.Secure` 对第三方 App 受限**（Bitwarden `ContextExtensions` 注释点名），
  `credential_service` 读到 null ⇒ 恒判「未启用」（荣耀 Android 17 实测功能其实正常）。
  改为与 `AutofillStatusChecker.isSystemEnabled` **同一取向：读不到 → 按"已启用"处理**。
- 参照 Bastion `PasskeySettingsScreen`（**该页不显示启用状态**，只给「系统设置」入口），
  删掉「已保存的通行密钥」「通行密钥能做什么」两行冗余文案。

---

## 第四十二轮 · 误弹检测**完全**对齐 Bitwarden + 精准填充比对 + 下拉观感 + 卡包点击 + CI 报错清理（`59b57e8`）

### 1. 误弹检测：两层都对齐
- 上游词表（`ViewStructureUtils.kt`）：`SUPPORTED_RAW_USERNAME_HINTS = [email, phone, username]`
  （★**没有 `login`**）、`SUPPORTED_RAW_PASSWORD_HINTS = [password, pswd]`、
  `IGNORED_RAW_HINTS = [search, find, recipient, edit]`；文本比对前会**去掉非字母**（`user_name`→`username`）。
- **分类层**（`HintClassifier`）：①新增**否定词优先**（EN + 中文「搜索/查找/收件人/编辑」）——
  命中即放弃；②用户名关键词收窄（去掉 `login`/`登录`/`user name`）；③`normalize()` 转小写 + 去 ASCII
  分隔符、**保留 CJK**。→ 当初 `id="login-search"` 那类搜索框误弹，现在在**分类阶段**就被拦掉。
- **策略层**（`AutofillFillTargetPolicy`）：**撤销 `strength != LOW` 门槛**，判定收敛为
  「**可见 + 凭据语义**」一条 —— 与上游「分类结果即证据（`Unused` 直接剔除）」一致。
  ⚠️ 撤销的前提是分类层的否定词已经兜住原场景；**没有否定词就不能撤这一层**。
- 测试同步改写（原「弱信号不算数」用例已不成立）。

### 2. 精准填充：⚠️ **更正此前的误判**
> 上一轮曾判断「上游只填**焦点所在表单**、需给 `ParsedField` 引入**表单容器**建模」——
> **这是误读**。复核：`AutofillParserImpl.traverse` 返回的是**每个 window 一份**
> `ViewNodeTraversalData`（`(0 until windowNodeCount).map { ... }`），`selectCandidateAutofillViews`
> 只是「保留**含焦点视图的那个 window**、剔除 `Unused`（含 Identity）」，随后 `fillLoginPartition`
> 填该 window 内**全部** Login 视图。
> ⇒ **上游同样是「整页按语义填」**，与我们的 `targetIdsFor(hint)` 行为一致，**无需表单容器建模**。

逐条比对（**已对齐**）：逐字段站点校验（`field.website == cipher.website`）、邮箱字段只在用户名是
合法邮箱时才填（`isValidEmail()`）、密码/验证码/卡号各自取值、`Username` 不设值形态闸、
无候选 → `Unfillable → onSuccess(null)`。
**真实剩余差异（产品取舍，非精度缺陷）**：①上游把 `Identity` 从候选中整体排除（我们仍给身份建议）；
②上游按「焦点视图**类型**」决定兑现哪种分区，我们**同时**给登录/卡片/身份建议。

### 3. 卡包条目点不动（用户反馈）
根因：`CardWalletRow` **完全没有 `clickable`**，且 `MainShellScreen` 里**已有的** `onOpenItem`
只传给了密码 Tab、**没传给卡包 Tab**。修法：补 `clickable` + `CardWalletScreen` 增 `onOpenItem` + 壳里传下去。

### 4. 填充下拉观感（用户反馈）
`layout/autofill_dataset_item.xml` 原为「两行文字」，在系统面板里又窄又空 →
改为「**40dp 应用图标 + 标题 + 副标题**」横向排列，`minHeight=56dp`、内边距 `16/10`；
`AutofillDatasets.presentation` 增 `iconRes`（默认 `R.mipmap.ic_launcher`）并 `setImageViewResource`。

### 5. 用户报的 CI「Annotations：1 error」—— 两处 non-blocking 单测长期报红
`Run unit tests (non-blocking)` 是 `continue-on-error`，所以**从不阻塞出包**、长期无人发现，
但会一直污染 CI 信号。两处均已修（详见 `ISSUES.md` #45）：
- `VaultixCryptoTest.removePkcs7PaddingStripsTrailingRandomPadding` —— **陈旧用例**；
- `ActiveVaultStoreTest.selectTakesEffectImmediatelyAndPersists` —— **真竞态**。

---

## ⏳ 接力要点（本轮结束时）

**已完成并出包**：`59b57e8`（preview = `dev-59b57e8…`，`app-full-debug.apk` 29.83MB），
CI run `34693235033` **success 且单测全绿**（Annotations 不再有 error）。

**真机待验证（按优先级）**：
1. **搜索框不再乱弹** —— 撤销 `strength` 门槛后的**唯一回归风险点**，最该盯；
2. QQ 登录页账号框能弹候选且一键填账号+密码；
3. 卡包条目可点击进详情；
4. 填充下拉变为「图标 + 标题 + 副标题」。

**下一步（待定）**：`Docs/progress/main-shell-migration.md` **阶段 3 = 观感**（Tab 转场动画 / 卡片样式）
—— 阶段 1（骨架）+ 阶段 2（活跃库真源）已完成，可开工。铁律：不搬
`UnifiedCategoryFilterSelection` / `DedupEngine`（多后端产物）。

## 环境备注

- 沙箱已可跑 Gradle（见上），但仍缺 NDK ⇒ **APK 产物与最终验收以 CI 为准**。
- 沙箱参考源码：Bastion 在仓内 `reference/bastion/`；
  Bitwarden `/tmp/bw-ref/android-main/`、Keyguard `/tmp/keyguard-ref/`（**仅沙箱有效**）。
- 本机（Windows / WorkBuddy）环境事实见 `ISSUES.md` #42 / `SESSION-2026-09-11.md` 第三十四轮。

---

## 第四十三轮（2026-09-12 20:40–21:20）：用户一次报四件事 + 阶段 3 动效（单提交）

> 用户原话：「先查看本地的 AI 记忆……然后我反馈几个实际中遇到的问题：1、QQ 能填但 Edge 上
> 又变成只填密码、账号框不出条目；2、填充图标太丑，别人家都是精心设计过的小图标；
> 3、覆盖安装 + 重启后指纹解锁按钮不生效；4、填充辅助在 bitwarden 上有独立开关，我这个 APP 没有。
> 搬运 bastion 的外观和动效的改动连通这些修复一起做，这样一个提交推送就能解决多个问题了。」

### ① Edge 账号框不出候选也填不进去（P0）→ 根因有真机实证，非推测
- **证据来自上一轮留存的真机日志**（`build/adb-capture/device-log.txt`，不必重新连设备）：
  `fillRequest pkg=com.microsoft.emmx webDomain=github.com fields=221 hints={UNKNOWN=215, PASSWORD=5, USERNAME=2}`。
  GitHub 登录页只有 1 个密码框，却被判出 5 个 PASSWORD、221 个字段 ⇒ 分类被展示节点污染。
- **机理**：浏览器 WebView 给整棵 DOM 建虚拟节点并分配 `autofillId`（`traverse` 全收）；
  分类信号含 `node.text` ⇒ `<label>Password</label>` / 「Username or email address」被判成
  PASSWORD / USERNAME ⇒ 假 USERNAME 挡住 `promoteUsernameField` ⇒ 真账号框永远 UNKNOWN ⇒
  `hasUsernameField=false` ⇒ Dataset 只有密码值 ⇒ 点账号框没有对应字段值 → 浏览器不显示那条候选。
- **修**（对齐上游 `toAutofillView`）：新增 `EditableNodePolicy.isEditableNode()` 准入闸
  （`htmlInfo.tag == "input"` / EditText className / 判不出则放行 / 标准 hint 放行）；
  `BrowserUrlBars.formSignalOf()` 取代 `textSignalOf`（**去掉 `node.text`**，补 `autocomplete`/`label`/`hint`/`autofill` 属性键）。
- **诊断增强**：`fillRequest` 日志追加 `seq=`（字段序列 + `(hid)`），区分「真账号框 UNKNOWN」与「被假 USERNAME 占位」。
- 单测 `EditableNodePolicyTest` 7 例。

### ② 填充下拉图标（观感）
- 40dp 彩色启动图标 → **20dp 单色语义矢量**（地球 / 卡片 / 人像 / 盾牌品牌标），
  `setColorFilter` 按系统明暗上色，行距 12/6、最小高 48dp（对齐上游 `autofill_remote_view.xml`）。
- 类别经新增的 `AutofillIntents.EXTRA_CATEGORY` 带到认证回灌，避免二次验证后图标跳变。

### ③ 指纹解锁「覆盖安装 + 重启后不生效」= 三个独立缺陷（用户直觉正确：应用从不清注册）
- **A 死锁**：`onError` 只处理「用户取消」⇒ `ERROR_TIMEOUT/CANCELED/HW_UNAVAILABLE/LOCKOUT`
  无人复位 `submitting` ⇒ 指纹 + 主密码按钮全灰、转圈不散 ⇒ 整页死锁。**这是「按钮在、点了没反应」的直接解释**。
  修：任何错误都复位；`ERROR_CANCELED` 归入取消；自动弹窗改到 `withResumed {}` 之后（重启后首帧发起的弹窗最易被系统取消）。
- **B 自毁**：`UserNotAuthenticatedException` 被当成「密钥已废」→ 清掉用户注册（与 `Docs/03` 要求相反）。已移出。
- **C 钥匙丢了顺手砸锁**：`obtainKey()` 别名缺失时**静默新建 KEK** ⇒ 认证成功 + `AEADBadTag` ⇒ 走自毁分支。
  已拆 `loadKey()`（只读）/ `obtainOrCreateKey()`（仅启用路径，先删失效别名再建），并新增三态 `kekStatus`
  （**`containsAlias` 不能当健康检查**：失效时平台让它静默返回 false）。
- **D CI 假覆盖安装**：`ci-debug.yml` 仅 push 时注入固定密钥 ⇒ 手动触发的包用一次性 debug key 签，
  盖不上 preview 包 ⇒ 只能卸载重装（数据 + Keystore 全丢，表现恰是「指纹解锁被清除」）。已改
  `event_name != 'pull_request'` + 按磁盘 `release.jks` 是否存在注入签名。

### ④ 填充辅助独立开关
- 新增 `fill_assist_enabled` 偏好 + 设置页「填充行为」开关（文案取上游 `values-zh-rCN`）；
  关闭后完全不读规则表、也不再 6 小时节流刷新。**不照搬**上游 feature flag（自建 Vaultwarden 不返回，会永远关着）。

### ⑤ 阶段 3 观感（Bastion 外观/动效）
- 新增 `ui/shell/TabTransitions.kt`（移植 Bastion `NavTransitions` 的 tab 部分：进入 fadeIn + 1/16 屏高上移 220ms，
  退出纯 fadeOut 120ms，缓动 CubicBezier(0.6,0,0.4,1)）+ `MainShellScreen` 用
  `AnimatedContent + SizeTransform(clip=false) + SaveableStateHolder` 接线 ⇒ **Tab 切换有过渡且切回不丢滚动位置**。
- 验证码倒计时移植 Bastion `rememberTotpSmoothProgress`（秒级数据 + 绘制层 1s 线性动画，翻转 `snap()`）。
- 阶段 3 剩余：卡片样式细化 / FAB 行为按需取用 / 设置页分组复核（见 `main-shell-migration.md` §3）。

### 验证与门禁
- 本地真跑：`:app:compileFullDebugKotlin` ✓、`:app:testFullDebugUnitTest` ✓、
  `:data:repository:testDebugUnitTest` ✓（含改写的 `userNotAuthenticated_isRecoverable` 回归锁）、
  全模块 `detekt` ✓ —— 一次 `BUILD SUCCESSFUL`（4m32s），改动后再跑一次 compile+detekt 仍绿。
- **待真机验收**：①Edge 账号框能弹能填；②搜索框仍不乱弹（撤强度门槛 + 本轮改信号后的双重回归点）；
  ③下拉图标小而克制；④覆盖安装 + 重启后指纹解锁可用；⑤设置页出现「启用填充辅助」；
  ⑥Tab 切换有淡入上移、切回不丢位置。

### 教训
- **真机日志要留**：上一轮把 `device-log.txt` 留在 `build/`，本轮**没有连设备就拿到了根因**（221 个字段那条）。
  「留证据」比「再复现一次」便宜得多。
- **「观感改进」也可能是退步**：上一轮把启动图标放大到 40dp 是自认为的改进，实机上又大又花 ⇒
  视觉规格应**照抄成熟实现的数值**（上游 20dp/maxWidth），而不是自己拍。
- **用户对「不该被清除」的直觉值得当第一判据**：应用确实没有任何清注册的升级/重启逻辑 ⇒ 顺着
  「读侧撒谎 + 自毁 + 死锁」三条去找，比怀疑「是不是被清了」有效得多。

---

## 第四十四轮（2026-09-12 晚）：观感与交互批次（承接第四十三轮，用户连续追加要求）

用户在本轮连续追加了 8 项要求，均已落地（同一提交）：
1. **条目卡片外框**（`ui/common/EntryCard.kt`）：M3 默认 Card + 12dp 圆角 + 16dp 内边距 +
   标题 SemiBold + 6dp 行距（规格照 Bastion `PasswordEntryCard`）；密码/验证码/**卡包**三列表统一
   （卡包此前是裸 Row，连外框都没有）。
2. **密码条目分组**（`ui/items/ItemsGrouping.kt` + 顶栏菜单）：不分组（默认）/ 按类型 / 按文件夹 /
   按首字母；偏好键 `items_group_mode`；分组头可折叠（箭头旋转动画）。
3. **按住后滑动删除**（`ui/common/PressAndSwipeToDelete.kt`）：三个列表都接；
   `detectDragGesturesAfterLongPress` + `consume()` 避开与点击的冲突；软删除进回收站。
4. **沉浸式顶栏**（`ui/common/ExpressiveTopBar.kt` + 4 个 Tab 接线 + 壳 `contentWindowInsets = 0`）：
   大标题 32→16sp、栏高 72→48dp、收起后栏背景透明、动作胶囊缩放；状态栏与右上按钮都沉浸。
5. **验证码页统一进度条**（`ui/totp/UnifiedProgressBar.kt`）：整页一条 + 通行密钥入口挂尾部；
   删掉行内进度条（降功耗）。
6. **验证码行点击 = 复制**，行内补「编辑」按钮（否则改不了条目）。
7. **详情页去动态验证码**：只提示「含验证码 / 已绑定 N 个通行密钥」，改到验证码页看（降功耗）。
8. **2FA 第二步页面完全不响应**：`isOtpOnly` → 不弹「没有匹配的密码」。
另：填充下拉行加 12dp 圆角描边外框（`autofill_item_frame.xml` + values/values-night 描边色）。

**门禁**：本地真跑 `:app:compileFullDebugKotlin` / `:app:testFullDebugUnitTest` /
`:data:repository:testDebugUnitTest` / `:core:common:testDebugUnitTest` / `detekt` —— 全绿。

### 教训
- **「搬壳」≠「改外框」**：用户把卡片样式问题归因到「壳没搬完」，实际壳（导航/容器/转场）
  第四十三轮就搬完了；外框只是组件级数值。回答时先纠正归因，避免白搬一遍。
- **沉浸式顶栏有三个前置条件**（Box 叠加 + 列表顶部让位 + 外层 insets 归零），
  少一个就退化成「顶栏变短但内容仍被压着」——已写入 `ISSUES.md` #57，接力别踩。
- **改点击语义必须补回被挤掉的入口**：整行点击从「编辑」改成「复制」后，
  编辑入口若不显式补一个，条目就再也改不了（`ISSUES.md` #56）。

### ⏳ 待用户确认（Bastion 设置页清单）
用户要求「能看到的都搬过来，一步到位」。已在本轮给出**清单表**（见 `next-steps.md` 顶部）：
- 已有：自动锁定/剪贴板/主题/动态取色/OLED/防截屏/回收站清理/快速解锁/库管理/自动填充/通行密钥；
- **本轮可补**：设置页 SectionCard 观感与分组标题统一、条目分组模式入口、显示选项、列表密度；
- 需后端能力（**不搬**，否则是点不动的假开关）：密码建议/智能标题/屏蔽字段/通知时长/诊断导出/
  安全分析(HIBP)/附件与历史/Send；
- 架构决策不搬：私有本地库 / WebDAV / OneDrive / Bastion 自有分类。

---

## 59. M2 KDBX 落地：引擎选型与「为什么没搬 Keyguard」（2026-09-12，第四十六轮）

**用户诉求**：「Keyguard 的 KDBX 做得很好，能否搬过来移植？」——查证后结论是**不需要搬 Keyguard**，
而且搬它反而不划算。三条事实：

1. **Keyguard 本体是 `All Rights Reserved`**（仓库根 `LICENSE` 全文就一句 "All Rights Reserved"，
   README：「The source code is available for **personal use** only」）。即便拿到作者个人授权，
   把它并入**公开的 GPL-3.0 仓库**仍有再分发授权瑕疵；用户授权解的是「自己用」，解不了
   「GPL 要求我们有权以 GPL 再授权」这一层。
2. **Keyguard 的 KDBX 能力本来就来自 kotpass**：`D:\Vaultix-refs\keyguard-app\util\kdbx\` 是
   vendored 的 `app.keemobile.kotpass`，且其 `LICENSE-kotpass.txt` 明确 **MIT**
   （Copyright (c) 2021 Denis T.）。**MIT 与 GPL-3.0 兼容**，直接用上游即可。
3. 那份 vendored 副本被**改过**：`cryptography/PlatformCrypto.kt` 直接 import 了
   `com.artemchep.keyguard.util.foundation.crypto.randomBytes`（换成 Keyguard 自己的
   foundation/crypto 模块）。搬它 = 先拆 Keyguard 的补丁，纯属自找麻烦。

**定案**：`data:kdbx` 直接依赖 **`app.keemobile:kotpass:0.13.0`（Maven Central，MIT，纯 Kotlin/JVM，
唯一传递依赖 okio）**。Bastion（GPL-3.0，仓库内 `reference/bastion/`）的
`KeePassCredentialSupport` / `KeePassCodecSupport` **只作行为参考**并已在本模块注明溯源。

**本轮落地（M2 阶段 A 的引擎层，已全绿）**：
| 文件 | 职责 |
|---|---|
| `KdbxFormat.kt` | 文件头识别（签名 `03 D9 A2 9A 67 FB 4B B5` + 版本）——**先判格式再解密**，否则「选错文件」会被报成「密码错误」 |
| `KdbxCredentialCandidate.kt` | 凭据候选：keyfile 的 raw / XML `<Data>`(hex或base64) / 64 位 hex 文本 / sha256(raw) 四种形态；空密码时同时试 `key-only` 与 `empty-password+key` |
| `KdbxCipherProviders.kt` | 基础套件 + **Twofish**（KDBX 3.1 老库；不注册会「版本识别通过、解密失败」） |
| `KdbxOpener.kt` | 依次尝试候选 → 解密；结构化失败原因（NotKdbx / UnsupportedVersion / InvalidCredentials(attempted) / Unknown） |
| `KdbxItemMapper.kt` | kotpass → `VaultItem`/`VaultFolder`：**受保护字段取 `.content` 自动解密**；TOTP 多约定统一转 `otpauth://`（复用既有 `OtpUriParser`）；回收站整棵子树排除并计数；根分组不算文件夹（其子分组才是顶层） |
| `KdbxSessionStore` | 已解锁会话只存内存（锁库即丢），绝不落盘明文 |

**测试**：`KdbxReadPathTest` **10/10 通过**（真实 KDBX 字节往返：字段映射 / Hidden 自定义字段 /
TOTP→otpauth / 分组→文件夹 / 安全笔记判定 / 回收站排除 / 错误密码分类 / 非 KDBX 拒绝 / 版本识别 /
keyfile 形态）。⚠️ 加密路径的测试宁可多断言——读错一个字段用户不会立刻发现，后果却很重。

**下一步（阶段 A 集成层，尚未做）**：SAF 选文件 → 建 KDBX 库行（`vaults.kind=KDBX`、
`origin`=文件 URI，**无需迁移**）→ 主密码/keyfile 解锁 → `ItemRepositoryImpl` 读路径分流到会话
（UI/自动填充零改动）→ 可选的 local-unlock KEK 包裹 KDBX 凭据。之后才是阶段 B（写回：
原子替换 + `.kdbx.bak` 备份 + 保真度登记 + 往返测试）。

### ⏭ 接力（新会话从这里接）
锁/解锁规范化的**完整交接单在 `.ai/ISSUES.md` #60**（已查证事实 + 1b~4 逐步改动点 + 后续三批计划）。
本会话最后一次状态：`24b5b4f` 已加 ViewLocked 载体（行为零变化）、`6a3fb01` 写交接单；
待做 = #60 的 1b（锁按钮改调 viewLock）→ 4（autofill 解锁即回填）→ 顶栏胶囊化 + 站点图标 → KDBX 集成。
