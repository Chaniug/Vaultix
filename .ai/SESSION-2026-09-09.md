# 会话记录 · 2026-09-09

## 第十七轮（2026-09-09 凌晨 · Bastion 对齐·批次③ 回收站自动清理）

### 需求
按批次③ 清单落地回收站对齐：autoDeleteDays DataStore 设置 + 到期清理 + 剩余天数显示
（Bastion TrashViewModel 逻辑参考）。

### 落地
- `core/common/.../TrashCleanupPolicy.kt` 新建（纯函数，对齐 Bastion 语义去 -1 档）：
  - `shouldAutoCleanup(days) = days > 0`；`cutoffMillis(now, days) = now - days*86400000`
  - `deletedAtMillis(deletedDate)`：ISO-8601 容错解析（本地 `Instant.now().toString()` 毫秒 /
    服务端 Bitwarden 微秒-纳秒均可），非法返回 null
  - `isExpired`：严格小于 cutoff 才过期；未启用 / 不可解析恒 false（宁多留不误删）
  - `remainingDays = max(0, days - 整除天数SinceDelete)`；未启用返回 null（UI 不显示倒计时）
- `domain/ItemRepository.kt`：
  - 新 `data class TrashEntry(item, deletedDate)`——回收站行携带删除时间
  - `observeTrash` 返回改 `Flow<List<TrashEntry>>`；新接口
    `cleanupExpiredTrash(vaultId, autoDeleteDays): Int`
- `core/database/VaultixDaos.kt`：CipherDao 加 `getTrashByVault`（suspend 一次性快照，
  口径与 observeTrashByVault 一致）
- `data/repository/ItemRepositoryImpl.kt`：
  - `observeTrash` 独立实现（combine DAO 流 + unlockedIds，decodeTrashRows 保留行级
    deletedDate——本地软删只改列不重写密文，DTO 内 deletedDate 不可靠）
  - `cleanupExpiredTrash`：days<=0 直接 0；扫描 → 过期行逐条 DELETE 入队（与
    permanentDeleteItem 同口径，离线联网补推服务端同步删）→ deleteByIds 批删 →
    flush 一次 → 返回条数；runCatching 兜底失败静默 0（后台辅助动作不打断 UI）
  - data:repository 补 `implementation(projects.core.common)` 依赖
- `core/datastore/VaultixPreferences.kt`：`trashAutoDeleteDays`（键 `trash_auto_delete_days`，
  默认 30；0 = 不自动清空；不提供 Bastion 的 -1 禁用档）+ setter
- `app/.../trash/TrashViewModel.kt`：注入 VaultixPreferences；`trashRows`（combine 回收站流
  × 档位 → `TrashRowUi(item, remainingDays)`）；`autoDeleteDays` StateFlow；init 即执行到期
  清理（Bastion cleanupExpiredItemsNow 同位），实删才发 `UiEvent.AutoCleaned(count)`；
  `setAutoDeleteDays` 即存即生效；档位 `AUTO_DELETE_PRESETS = [0, 7, 30, 90]`
- `app/.../trash/TrashScreen.kt`：顶栏 Settings 图标 → `TrashAutoDeleteDialog`（radio 档位，
  即选即存）；行内副标题加「N 天后自动清理」（剩 0 天红色「即将自动清理」）；AutoCleaned
  → snackbar 报数
- `app/res/values/strings.xml`：新增 8 键（trash_auto_delete_title/hint/never/after_days、
  trash_row_expires_in/today、trash_auto_cleaned、action_done）

### 验证
- `TrashCleanupPolicyTest` 9 例：cutoff/严格压线边界/整除天倒计时/0 与负档 no-op/脏数据守卫/
  毫秒-微秒-整秒三种 ISO 格式解析
- `ItemRepositoryImplTest` 增 4 例：只删过期行（45 天删 vs 10 天删，30 天档位）+ DELETE 入队
  校验；0/负档位零副作用（不查库不入队）；DAO 异常静默 0；observeTrash 携带 deletedDate
- 全项目单测 0 失败；全模块 detekt 0 违规；assembleFullDebug + assembleOfflineDebug 双通过
  （Gradle daemon 两次 transient disappeared，清理 daemon 单 worker 重跑均成功）

### 工程经验（本轮沉淀）
- 本地软删只改 `CipherEntity.deletedDate` 列不重写密文 → 回收站元数据必须从行级取，
  DTO 里的 deletedDate 不可靠（同步下行才会写进 DTO）
- 自动清理要复用 permanentDeleteItem 的「DELETE 入队 + 删行」口径，不能只删本地行——
  否则离线时服务端永远不会删，下次同步又拉回来
- 测试时间常量别手算纪元毫秒（`1_760_000_000_000` 是 2025-10-09 不是 2026）——用
  `Instant.fromEpochMilliseconds(NOW - n*DAY)` 相对构造最稳
- Gradle daemon 偶发 disappeared（非 OOM，123Gi 空闲）：`--stop` 清 daemon +
  `--max-workers=2` 分步跑即可，与批次② 的 hiltJavaCompile transient 同类

### 下一步
批次④ 设置页（Bastion SettingsScreen 对照补缺；回收站档位入口可考虑收编进设置页）→
⑤ 通行密钥（PasskeysScreen 对照 Bastion 补缺）→ ⑥ 卡包；
P0：真机回归（f0f5df6 preview + 批次②五类型/导入 + 本轮回收站自动清理）→ M1 close-out

---

## 第十八轮（2026-09-09 凌晨 · 编辑密码查看修复 + 批次④ 设置页对齐）

### 需求
用户报告：「已有密码条目编辑的时候，看不到密码。只能看到随机生成的密码。」随后继续
下一步开发（批次④ 设置页，Bastion SettingsScreen 对照补缺）。

### Bug 定性与修复（153da7a）
- 定性：编辑表单（ItemFormDialog.LoginFields）密码框**只有骰子（随机生成）按钮、
  没有明文查看开关**——密码恒为掩码圆点；用户点骰子生成的随机密码反而是唯一
  能明文看到的内容。详情页有 showPassword 切换、编辑表单漏了（自定义字段 Hidden
  值却有同款眼睛）——不是数据丢失，是交互缺失。
- 修复：密码框 trailingIcon 补 Visibility/VisibilityOff toggle（visualTransformation
  跟随切换），新建/编辑均可用，对齐 Bitwarden 编辑页。

### 批次④ 落地
- `core/datastore/VaultixPreferences.kt`：新 themeMode（system/light/dark）+
  oledPureBlack 键；新 public `VaultixPreferencesDefaults`（TRASH_AUTO_DELETE_DAYS=30 /
  THEME_MODE=system）作为偏好默认值单一真值源（UI 层 stateIn 初始值引用它，
  TrashViewModel.DEFAULT_AUTO_DELETE_DAYS 同步收敛）
- `ui/theme/Theme.kt`：新 `ThemeMode` 枚举（from 容错未知值回退 SYSTEM）；
  VaultixTheme 签名改为 (themeMode, dynamicColor, oledPureBlack)，深色 + OLED 时
  base.copy(background/surface = Black)——对动态取色与固定色板同样生效
- `MainActivity.kt`：收集 themeMode/oledPureBlack 传入 VaultixTheme
- `ui/common/TrashAutoDeleteDialog.kt` 新建：对话框 + trashAutoDeleteLabel +
  TRASH_AUTO_DELETE_PRESETS 从 TrashScreen 提升为共用（回收站顶栏 / 设置页数据组
  共用同一偏好键）；TrashScreen/TrashViewModel 删本地副本与重复常量
- `ui/settings/SettingsScreen.kt`：外观组抽 `AppearanceSection`（主题模式行 +
  单选对话框、OLED 开关、动态取色开关），新 `DataSection`（回收站自动清理行）；
  抽 Section 同时控主函数长度（detekt LongMethod ≤150 门禁）；dynamicColor 经
  参数传入 Section（不重复 collect，单一数据源）
- `SettingsViewModel.kt`：themeMode/oledPureBlack/trashAutoDeleteDays 三个独立
  StateFlow + setter（不塞进 UiState——6 流 combine 要退化成 Array<Any?> 转型）

### 对照结论（Bastion SettingsScreen 8 组 vs Vaultix M1）
已落地：安全（自动锁定/剪贴板/防截屏/快速解锁/立即锁定）、外观（主题三态/OLED/
动态色）、数据（回收站档位）、关于（版本/源码/许可）。
M2 推迟：自动填充服务全套（M1 未做系统填充）、导入导出/备份、开发者日志、
界面布局定制（底部导航/卡片字段/favicon）、WebDAV/OneDrive 同步、清理全部数据、
生成器独立工具页。

### 验证
- 双 flavor 编译 + 全模块 detekt 0 违规 + 全项目单测 0 失败（分步跑防 daemon 崩溃）

### 工程经验（本轮沉淀）
- 「编辑时看到随机密码」类报告先查交互缺失再查数据链路：掩码无查看开关时，
  用户唯一能看到的明文是生成器回填值，极易误报为数据损坏
- Dialog 组件跨页共用（设置页 + 功能页同键）要连同 label 函数与档位常量一起
  提升，否则两处档位列表会漂移
- Edit 工具一次 old_string 匹配过宽曾误删函数声明行（QuickUnlockManageDialog），
  下一步操作前重读文件段确认边界——修复后编译通过
- detekt LongMethod 应对：设置页新增功能按「Section 私有组件」切分，比主函数
  塞满后重构成本低

### 下一步
批次⑤ 通行密钥（PasskeysScreen 对照 Bastion 补缺）→ ⑥ 卡包；
P0：真机回归（含本轮编辑密码眼睛、设置页主题/OLED、回收站清理）
