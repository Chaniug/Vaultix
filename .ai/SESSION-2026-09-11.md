# 会话记录 · 2026-09-11（接力入口）

> 本文件是 **2026-09-11 起的最新接力入口**。开工先读：`MEMORY.md`（约定/硬约束）
> → `ISSUES.md`（坑，重点看 #33 / #34 两条更正与根因）→ `Docs/progress/next-steps.md`
> （待办）→ 本文件（最近进展与待验证清单）。前序流水见 `SESSION-2026-09-10.md`。

## 🔴 一句话现状（2026-09-11 收尾）

**「通行密钥找不到 / 候选列表为空」根因已定位并修复**：不在注册期字段，而在
**discovery 链**上 —— ②未 trim、③rpId 未归一化、④allowCredentials 严格过滤无回退。
（原推断的「①解密残留 ISO10126 填充」**已用真实 JCE 实测否掉**，降级为纵深防御工具。）
同时**撤回第二十八轮的两个错误结论**（BE/BS 因果 + "必须重新注册"），并把
`signCount` **回退为恒 0**。

## 本轮修复链

| commit | 内容 |
|---|---|
| `8afac3d` | （第二十八轮）BE/BS 置位 + 响应 JSON 补字段 —— **保留**，但理由改为语义正确性 |
| `ae3a236` | （第二十八轮）文档记录 —— 其两条结论见下方更正 |
| `467b432` | （第二十八轮）CI 全绿与产物校验记录 —— **本地未推送**（token 只读） |
| 本轮（待推送） | 修复 discovery 四项 + 更正文档 + 补回归测试 |

> ⚠️ 本轮改动尚未提交/未跑 CI（沙箱无 Android SDK / JDK17）。**下一轮第一件事**：
> 提交 + 触发 CI + 校验产物 + 递包给用户真机验证。

## 关键更正（第二十八轮 → 第二十九轮）

1. **BE/BS 与「验签失败」无因果**：BE/BS 是**注册期存档字段**，
   RP 在**断言（登录）阶段不做 BE/BS 校验**（login 校验清单只有
   `type/challenge/origin/公钥/rpIdHash/UP(UV)/签名/signCount>stored`）。
   ⇒ 也**不存在「旧 passkey 必须重新注册」**。用户质疑正确，已全面撤回。
2. **`signCount` 必须恒 0**：规范校验是**严格大于**；Bitwarden 官方端每签一次会递增写回，
   同步下来的 counter 非零（`CipherMapper:444`）。「原样发送不递增」⇒ 第二次登录值不变
   ⇒ `new > stored` 不成立 ⇒ RP 判重放。**已回退为恒 0**（对齐 Bastion `newSignCount = 0L`）。

## 根因与机制备忘（防重复调查）

1. **Bitwarden 服务端加密曾有 ISO10126 说法 —— 但本项目 `decrypt` 用标准
   `PKCS5Padding` + `doFinal`，本就正确解填充。** 已用真实 JCE（JDK 20 SunJCE）实测：
   标准 `PKCS5Padding` 对 ISO10126 密文**直接抛 `BadPaddingException`**（不会静默泄漏）；
   反向用宽松 PKCS5 解析 ISO10126 **2000/2000 完全正确**。
   ⇒ 「ISO10126 残留」**不是根因**。`removePkcs7PaddingIfStrict` 仅保留为纵深防御。
   ⚠️ **教训：能用 20 行运行时实验否掉的假设，不要写进根因。**
2. **官方客户端读字段一律 `trim()`**；`CipherMapper.mapFido2` 此前一个都没做 ——
   这是**实际根因之首**（`rpId` 按精确字符串比对，不 trim 直接全量失配）。
   💡 副产品：Java `trim()` 去所有 `<= U+0020` 字符，而 PKCS#7 填充字节 `0x01..0x10`
   全在此范围 ⇒ `trim()` 本身就是填充残留的兜底（已验证 16/16）。
3. **rpId 必须两侧归一化**：`trim → trimEnd('.') → lowercase(Locale.ROOT) →
   IDN.toASCII(lower, USE_STD3_ASCII_RULES)`（对齐 Bastion `PasskeyRpIdNormalizer`）。
4. **`allowCredentials` 是提示不是授权门**：严格匹配**为空时回退到只按 rpId**
   （对齐 Bastion `resolvePasskeys` 的 `strictAllowCredentials = false`）。
5. **锁定态三家一致**：全库锁定 → 只返回 `authenticationActions`(unlock)，
   **不带 credentialEntries、直接 return**。Vaultix 已对齐，不是本次根因。
   三家**都没有**「库更新后延迟重试」的补偿 ⇒ 不需要加。

## 改动文件清单（本轮）

| 文件 | 改动 |
|---|---|
| `core/crypto/.../VaultixCrypto.kt` | 新增 `removePkcs7PaddingIfStrict` + `AES_BLOCK_SIZE`；`decryptToString` 剥填充并 `trim()` |
| `data/bitwarden/.../mapper/CipherMapper.kt` | `mapFido2` 全字段 `.trim()` |
| `app/.../passkey/VaultixCredentialProviderService.kt` | 新增 `normalizeRpId`/`isSameRpId`；rpId 两侧归一化；allowCredentials 为空时回退；逐级计数埋点 |
| `app/.../passkey/PasskeyGetActivity.kt` | `signCount` 回退为恒 0（含回退说明注释） |
| `core/common/.../WebAuthn.kt` | KDoc 更正 BE/BS 因果边界（保留置位，理由改为语义正确性） |
| `core/crypto/.../VaultixCryptoTest.kt` | 新增 4 条回归测试（ISO10126 剥离 / 严格 PKCS#7 / 非严格不误剥 / trim） |
| `core/common/.../WebAuthnTest.kt` | 新增 signCount 恒 0 回归锁；更正 BE/BS 测试 KDoc |
| `Docs/progress/next-steps.md` / `.ai/ISSUES.md` / `.ai/MEMORY.md` | 更正 + 记录本轮 |

## ⏳ 真机待验证（下一优先）

1. 装含本轮改动的包 → 在 Edge 触发通行密钥登录 → **候选列表应出现条目**。
2. 若仍为空：`logcat` 抓 tag `VaultixAutofill` 的
   `CP GET resolve rpId=... total=... unusable=... rpIdMiss=... allowedMiss=... matched=...`，
   按逐级计数判定卡在哪一级；配套 `CP GET resolve EMPTY: storedRpIds=...` 看库里实际存的域名。
3. 顺带验证：①通行密钥登录验签通过（signCount 回退后应更稳）；②密码自动填充仍正常。

## 环境备注（长期）

- 沙箱**无 Android SDK / JDK 17**，`gradlew` 跑不起来 ⇒ **构建与 CI 只能在 GitHub Actions**。
- `api.github.com` 对本环境 `ghu_` 型 token **一律 401**（只能用于 git 传输）；查 CI 状态
  改读网页端 `/Chaniug/Vaultix/actions/runs/<id>/job_groups_batch` 与 job 页
  `<check-step data-name/data-conclusion>` 属性。
- Release 资产走 `release-assets.githubusercontent.com`，已固定 `/etc/hosts`
  `185.199.108.133`；`github.com → 140.82.112.3`、`api.github.com → 140.82.113.6`、
  `raw.githubusercontent.com → 140.82.113.3`。
- 参考源码：Bastion 在仓内 `reference/bastion/`；Keyguard `/tmp/keyguard-ref/keyguard-app-master/`；
  Bitwarden `/tmp/bw-ref/android-main/`。
