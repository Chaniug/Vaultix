# 00 · 项目总览

## 1. 产品定位

Vaultix 是一款 Android 平台的开源密码管理器，**用一个应用同时管理两类密码库**：

| 库类型 | 形态 | 典型用户 |
|---|---|---|
| **Bitwarden 库** | 连接 bitwarden.com、自建 Vaultwarden 或 Bitwarden 官方自托管服务端，在线同步 | 已在用 Bitwarden 生态、需要多端实时同步的用户 |
| **KDBX 库** | 本地 `.kdbx` 文件（**仅 KDBX 4.x，写入恒为 4.1**），可选通过 WebDAV / 云盘 / Syncthing 同步文件 | KeePass 老用户、坚持数据不出本地的用户 |

两类库在 UI 层统一呈现：同一个主列表、同一套详情页、同一个自动填充入口，用户只在"库来源"上做区分。

## 2. 设计原则

1. **端到端加密，服务端零知识**：所有加解密只在本机发生，主密码、主密钥永不上传；Bitwarden 库只需上传/下载密文。
2. **离线优先**：KDBX 完全离线可用；Bitwarden 库离线时展示本地缓存的密文快照，恢复网络后再同步。
3. **平台原生**：优先使用 Android 官方能力（Autofill Framework、Credential Manager、BiometricPrompt、Android Keystore），不重复造轮子。
4. **Material 3 原生观感**：Material You 动态取色、M3 Expressive 组件、大屏自适应，与原生系统应用视觉一致。
5. **可审计**：所有密码学路径集中在 `core:crypto`，无自研算法，参数可复现，附测试向量。

## 3. 功能范围（V1）

### 3.1 核心

- 多库并存：同时挂载多个 Bitwarden 账号与多个 KDBX 文件，可整库锁定/解锁
- 条目类型：登录、安全笔记、信用卡、身份信息
- 组织：文件夹 / 群组（KDBX 支持嵌套树，Bitwarden 为单层文件夹，见 [02](./02-统一领域模型.md)）
- 搜索：全局搜索（顶部胶囊常驻 + 列表下拉展开）、按库/文件夹/收藏过滤、按 URI 匹配（用于自动填充）
- 跨库聚合视图「全部」：Bitwarden 与 KDBX、本地库统一展示并可切回单库
- 收藏、回收站 / 软删除、条目历史（KDBX 原生；Bitwarden 通过 `passwordHistory` 与服务端 `revisionDate` 近似支持）
- TOTP（RFC 6238）/ HOTP（RFC 4226）验证码生成与扫码录入（`otpauth://` URI）
- **独立验证器页**：条目绑定型 + 独立验证码统一管理，支持 Google Authenticator 迁移二维码批量导入
- **安全中心**：后台增量扫描，13 类告警（弱密码/已泄露/重复使用/缺 2FA/HTTP/缺 Passkey/重复条目/URI 过宽/条目不完整/即将过期等），三级严重度、未读角标、逐条忽略、直达修复
- 密码生成器与密码短语生成器（EFF 词表）
- 自定义字段：文本、隐藏文本、布尔、关联字段
- 附件（KDBX 内层二进制；Bitwarden 需 Premium 账号，V1 只读）；图片/文本附件应用内预览
- 卡包与证件：卡号分段遮罩、正反面照片位（存为 KDBX 附件，其他 KeePass 客户端可读）
- 可自定义底栏（Dock）：默认 密码库 / 验证器 / 安全中心 / 生成器 / 设置，可排序与开关
- 5 套主题：自然 / Material You / 暗色 / 纯黑 / RG 护眼

### 3.2 平台集成

- Autofill Framework（填写用户名/密码/TOTP/信用卡）
- Credential Manager 作为 **Provider**（Android 14+ 提供密码与 Passkey 凭据）
- 生物识别 / 设备凭证解锁（指纹、人脸、PIN）
- 自动锁定：超时、切后台、锁屏、手动
- 剪贴板自动清除、防截屏、Recent 卡片脱敏
- 快捷设置磁贴（快速锁定）、应用快捷方式（搜索/生成密码）
- Passkey（FIDO2/WebAuthn）本地存储与填充 —— V1.5

### 3.3 数据流转

- 导入：Bitwarden JSON / CSV、KeePass XML、Chrome/Edge CSV、1Password CSV
- 导出：加密的 Vaultix 备份、Bitwarden JSON、CSV、KDBX
- KDBX 高级：密钥文件（Key File）、数据库设置（Argon2id 参数、压缩、外层加密算法）

## 4. 非目标（V1 明确不做）

| 非目标 | 原因 |
|---|---|
| 服务端实现 | 复用 bitwarden.com 或 Vaultwarden，不重复建设 |
| 旧格式 / 旧协议兼容 | **不支持** KDBX 3.1 及更早、AES-KDF、Argon2d、Salsa20/RC4 保护流、Twofish；Bitwarden 侧不兼容缺少 `userDecryption` 的旧服务端与旧版 EncString 类型。遇到时给出转换/升级指引，不做静默降级 |
| iOS / 桌面端 | 专注把 Android 体验做深 |
| 自研加密算法 | 一律使用平台与成熟库提供的 AES-GCM / ChaCha20 / Argon2 / PBKDF2 |
| 组织（Organization）与 Send、Bitwarden 企业管理 | V1 只做个人库；集合（Collection）只读展示 |
| 云备份到自有服务器 | 交给用户选择的文件同步方案 |

## 5. 运行时权限清单

| 权限 | 用途 | 必要性 |
|---|---|---|
| `INTERNET` | Bitwarden 同步、图标下载 | 仅 Bitwarden 库需要；离线版可裁剪为可选 flavor |
| `USE_BIOMETRIC` | 生物识别解锁 | 必需（声明式，无需运行时申请） |
| `POST_NOTIFICATIONS` | 同步状态、剪贴板清除提示 | 运行时申请，可拒绝 |
| `CAMERA` | 扫码录入 TOTP | 运行时申请，仅扫码时 |
| `READ_EXTERNAL_STORAGE` / 照片权限 | 选择 KDBX 文件与密钥文件 | Android 13+ 改用系统文件选择器（SAF），**不申请存储权限** |
| `HIDE_OVERLAY_WINDOWS` | 防浮层劫持 | 声明即可（Android 12+） |
| `BIND_AUTOFILL_SERVICE` / `BIND_CREDENTIAL_PROVIDER_SERVICE` | 提供填充能力 | 系统授予 |

> 原则：KDBX 离线场景所需权限为零。建议在 `productFlavors` 中提供 `offline`（无 `INTERNET`）与 `full` 两个变体，供 F-Droid 与注重隐私的用户选择。

## 6. 术语表

| 术语 | 含义 |
|---|---|
| **Vault（库）** | 一个独立的数据源实例，对应一个 Bitwarden 账号或一个 KDBX 文件 |
| **VaultItem（条目）** | 库内一条记录（登录/笔记/卡片/身份） |
| **Master Password（主密码）** | 用户记忆的唯一口令，用于派生主密钥 |
| **Master Key（主密钥）** | 由主密码 + 盐经 KDF 派生 |
| **Symmetric Key（对称密钥）** | 实际加密条目数据的密钥（Bitwarden：账号密钥；KDBX：由 masterSeed + 变换密钥派生） |
| **EncString** | Bitwarden 密文字符串格式：`type.iv|ct|mac` |
| **Protected Value** | KDBX 中受内层流保护（内存加密）的字段 |
| **Cipher** | Bitwarden API 中条目的 JSON 对象名 |
| **Entry / Group** | KDBX XML 中的条目与群组 |
| **KDF** | 密钥派生函数（Bitwarden：Argon2id / PBKDF2；KDBX：仅 Argon2id） |
| **Autofill** | Android 自动填充框架 |
| **Credential Manager** | Android 14+ 统一凭据框架，取代部分 Autofill 场景 |
